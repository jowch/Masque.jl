var documenterSearchIndex = {"docs":
[{"category":"section","location":"legend/#Legend","page":"Legend","text":"LegendInteractable turns a Makie.Legend's entries into hit regions that link back to the plot(s) they label: hovering an entry highlights the linked trace(s), and clicking fires the usual bond.","title":"Legend"},{"category":"section","location":"legend/#Auto-extraction","page":"Legend","text":"masque(fig) picks up any Legend the same way it picks up a Colorbar — no extra call needed:\n\nbegin\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    lines!(ax, xs, ys1; label = \"a\")\n    lines!(ax, xs, ys2; label = \"b\")\n    axislegend(ax)\nend\n\n@bind ev masque(fig)   # entry \"a\" hovers/clicks -> :lines; \"b\" -> :lines_2\n\nThe link is resolved from Makie.get_plots on each entry's elements — the same linkage Makie's own click-to-toggle legend uses — so it works for any auto-generated legend (axislegend, Legend(fig, ax), merge = true, duplicate labels, …) with no extra configuration. An entry whose plot became a layer kind that can't be highlighted (currently just heatmap/image :grid — see Selection for the full selected=-able kind list) is dropped from the auto path with a warning, rather than failing the whole build.","title":"Auto extraction"},{"category":"section","location":"legend/#Hovering-and-clicking","page":"Legend","text":"Hovering a legend entry highlights the traces it links to, using the same wash/ring recipe as selected= (see Selection). A spec is a layer id — every element of that layer — or id:k pinning element k (1-based). Auto-extracted series! entries use the pin, so each swatch lights one series rather than every trace packed into the parent :lines layer. A bare targets = :series still highlights the whole layer. Clicking reports the usual LegendEvent, layer = :legend (or :legend_2, … for a second legend). entry.label, entry.group, and entry.targets are the entry's fields — targets is the list of those specs (as strings) the entry links to, group is the entry's group title (nothing for an ungrouped legend). entry.index is which entry, not a row of a table.\n\nev === nothing ? \"hover/click a legend entry\" : \"linked layers: $(ev.targets)\"","title":"Hovering and clicking"},{"category":"section","location":"legend/#Custom-legends","page":"Legend","text":"A legend built from bare LineElement/MarkerElement/PolyElement has nothing to auto-link — those elements carry no plot reference unless you give them one. Two ways to supply the link:\n\n# Makie's own kwarg: pass the plot the element stands for\nLineElement(color = :red, plots = some_plot)\n\n# or tell LegendInteractable directly, keyed by entry label\nLegendInteractable(leg; targets = Dict(\"a\" => :lines, \"b\" => [:lines_2, :scatter]))\n\ntargets also accepts a Vector — one entry per legend entry, in order (top-to-bottom, matching a titled/grouped legend's flattened entrygroups) — Symbol/Vector{Symbol}/ nothing. A Dict key matching no entry label, a wrong-length Vector, or (either form) a target naming an unknown layer or an unselectable kind raises ArgumentError at build time — unlike the auto path above, an explicit targets= is the caller's own claim, so a bad one fails loud rather than being dropped.\n\nA legend with an empty link list is still a set of hit targets. Hover leaves the other layers as they are, and a click reports a LegendEvent.","title":"Custom legends"},{"category":"section","location":"legend/#Tooltip","page":"Legend","text":"Hovering a legend entry does not show a tooltip. The label is already drawn in the row, and a card there covers the entries around it. Pass a masque\"...\" template when you want a card (fields: label, group, targets). Omitting tooltip and tooltip = false both leave the card off. Moving keyboard focus to an entry still announces that entry's label — see Keyboard and screen readers.\n\nLegendInteractable(leg; tooltip = masque\"$(label) — $(group)\")","title":"Tooltip"},{"category":"section","location":"interactables/#Interactables","page":"Interactables","text":"Declare interactables explicitly (geometry in data space):\n\nPointInteractable — scatter-style points\nSegmentInteractable — a whole line (lines! / stairs! / series!), a per-segment polyline, or disjoint segment pairs\nRectInteractable — bars (list) and heatmap cells (compact grid)\nPolygonInteractable — arbitrary polygons\nAxisInteractable — the whole axis: click anywhere → data (x, y) (linear + log)\nColorbarInteractable — a Makie.Colorbar block: hover/click anywhere on the bar inverts the cursor to the bar's data value\nLegendInteractable — a Makie.Legend block: hover/click an entry to highlight the plot(s) it labels — see Legend\nTextInteractable — text!/annotation! labels as click-to-pick buttons (bounding-box hit regions)\nThresholdInteractable — a draggable horizontal/vertical line; drag for a live readout, commit the data value on mouse-up\nROIInteractable — a draggable + resizable rectangle; drag the interior to move, a corner to resize both edges, or the middle of a side to resize just that one edge; commit the data-space bounds on mouse-up\nViewInteractable — drag-to-pan (2D Axis) / drag-to-orbit (Axis3); the wheel zooms a 2D view about the cursor; commits nothing (a camera isn't an analysis value); Shift+drag wins over ROI/threshold\nRegionInteractable / FunctionInteractable — custom interactions, no JavaScript required; see Custom interactions\n\nAxisInteractable, ThresholdInteractable, and ROIInteractable are 2D-only: they raise an ArgumentError on Axis3 or PolarAxis, and need a linear or log axis scale (categorical is fine for AxisInteractable/ThresholdInteractable, not for ROIInteractable). See Troubleshooting for the exact error text. Every other kind works on Axis3 and PolarAxis too — see 3D axes and PolarAxis at the end of this page.\n\nexamples/demo.jl is a runnable gallery of the core element/axis/view kinds above plus the selection round-trip; see Legend for a runnable LegendInteractable example.\n\nPan, zoom, and 3D rotation via a slider re-render through the same @bind loop as everything else: change limits (2D) or azimuth/elevation (Axis3) and rebuild the widget — masque re-projects the overlay, so hit regions stay correct.\n\nDragging with ViewInteractable is different: it commits nothing. A camera is operational state, not an analysis value the notebook reads. On both backends, in-drag frames stream over a with_js_link gesture channel instead of a bond — the picture and hit regions repaint live, with no cell re-execution. :cairo ships a PNG; :webgl ships a freshly serialized scene onto the canvas already on the page. Shift+drag wins over a ROIInteractable/ThresholdInteractable on the same axis on both backends. examples/view_manip.jl demonstrates both.","title":"Interactables"},{"category":"section","location":"interactables/#Constructors","page":"Interactables","text":"Every constructor in the table below takes an Axis (or, for ColorbarInteractable, a Makie.Colorbar, or for LegendInteractable, a Makie.Legend) and geometry in data space, and all of them accept id — the Symbol the event reports as layer. The element kinds — PointInteractable, SegmentInteractable, RectInteractable, PolygonInteractable, and (documented on their own pages) TextInteractable and RegionInteractable — also accept payloads (one entry per element, auto-generated with a 1-based index if omitted) and tooltip (nothing / masque\"...\" / false, see Tooltips). The whole-axis and drag kinds — AxisInteractable, ColorbarInteractable, ThresholdInteractable, ROIInteractable, ViewInteractable — report one client-computed value per event rather than a discrete element, so they take only id plus their own keywords below; passing payloads=/tooltip= to one of these is a MethodError. LegendInteractable sits between the two: it does report one payload per (entry) element like the element kinds, but the payload shape is fixed (; label, group, targets), not user-supplied — see Legend. FunctionInteractable is the outlier: it takes neither an Axis nor id as constructor arguments at all — see Custom interactions. The table below lists the keywords beyond id/payloads/tooltip for the constructors it covers.\n\nConstructor Geometry Extra keywords Bond\nPointInteractable(ax, points; radius = nothing, radius3d = nothing, id = :points) points :: Vector{(x, y)} (or (x,y,z) for a 3D axis) radius — px highlight radius. Omitted: the drawn radius of the one Scatter on ax with these positions, otherwise Makie's default :circle at the theme markersize (≈0.3525×markersize). radius3d — per-point data-space half-extents on a 3D axis (overrides radius) ElementEvent; default fields index (1-based), x, y[, z]\nSegmentInteractable(ax, vertices; mode = :polyline, unit = :segment, tol = 6, id = :segments) connected/disjoint vertices mode — :polyline (connected path) or :pairs (disjoint pairs); unit — :segment (one element per edge; the default) or :line (the whole path is one element; requires mode = :polyline); tol — hit-test slack around an edge, in logical px, scaled to DPI like radius (default 6) ElementEvent; default field segment_index (1-based) for :segment, index (1-based) for :line\nRectInteractable(ax; rects, clamp_to_viewport = false, id = :rects) rects = [(xc, yc, w, h), …] — explicit boxes (e.g. bars) clamp_to_viewport — clamp a rect that spans past the axis edge instead of letting it overflow ElementEvent; default field index (1-based)\nRectInteractable(ax; grid, id = :rects) grid = (xedges, yedges, values) — a heatmap shipped as edges, not N rects — GridCellEvent (i, j 1-based; A[cell])\nPolygonInteractable(ax, rings; id = :polygons) rings :: Vector{Vector{(x, y)}} — one or more filled rings — ElementEvent; default field index (1-based)\nAxisInteractable(ax; id = :axis) the whole axis: a click anywhere returns the data coordinate — AxisEvent (x, y)\nColorbarInteractable(cb; id = :colorbar) takes a Makie.Colorbar block, not an Axis; hit region bounded to the colorbar's pixel bbox — ColorbarEvent (value)\nLegendInteractable(leg; targets = nothing, id = :legend) takes a Makie.Legend block, not an Axis; one hit region per entry, bounded to that entry's row targets — a Dict{label => id(s)} or one entry per legend entry; default auto-resolves from the plots each entry's elements were built from LegendEvent (label, group, targets) — see Legend\nThresholdInteractable(ax; orientation = :horizontal, value, id = :threshold) a draggable line (:horizontal = constant-y, dragged vertically; :vertical = constant-x); live readout while dragging, commit on mouse-up orientation, value (initial position; a number or a ThresholdEvent) ThresholdEvent (value), on release\nROIInteractable(ax; bounds = (xmin, xmax, ymin, ymax), selects = nothing, id = :roi) a draggable + resizable box; move (interior) / resize (a corner resizes two edges, the middle of a side resizes just that one); commit on mouse-up selects — another layer's id; if set, the box reports that layer's selection instead of its own bounds (circles/grid layers only — see Multi-element selectors). bounds= accepts a 4-tuple or a BoundsEvent BoundsEvent on release; with selects, a Vector{ElementEvent} (points) or one GridWindowEvent (grid)\nViewInteractable(ax; id = :view) drag-to-pan (2D) or drag-to-orbit (Axis3); the wheel zooms a 2D view about the cursor; the axis frame stays put while the data inside it slides; commits nothing; Shift+drag forces view over ROI/threshold — none — a camera isn't an analysis value (§12.3); both backends stream a live gesture-channel preview instead","title":"Constructors"},{"category":"section","location":"interactables/#From-a-plot-object","page":"Interactables","text":"Pass the plot object a plot! call returns and the geometry is pulled from it — no need to repeat coordinates you already gave Makie. These produce the same interactable the explicit constructor would, so everything above (payloads, selected, tooltips) still applies. The ax argument is passed because a plot has no back-reference to its own axis.\n\nbegin\n    p = scatter!(ax, xs, ys; markersize = 14)\n    pt = PointInteractable(ax, p)   # the usual call: radius from the marker's drawn extent\nend\n\nPointInteractable(ax, points) (no plot object) uses that same radius when exactly one Scatter on ax has those positions in the same order. Pass radius= when the lookup would be ambiguous, or when there is no scatter to hug. A marker with no readable bbox (a character, an image) keeps markersize / 2.\n\n@bind sel masque(fig, pt)\n\nMasque introspects far more plot kinds than the handful shown as explicit constructors above. Every one below produces one of the same four interactables — same payloads/selected/ tooltip contract, plot-specific default payload and id:\n\nProduces From these plots\nPointInteractable Scatter (radius from the marker's drawn extent — ≈0.3525×markersize for the default :circle, markersize for a Circle/Rect geometry marker, markersize/2 fallback otherwise); MeshScatter (data-space radius3d on a 3D axis)\nSegmentInteractable Lines, Stairs (one :lines element, the whole path); Series (one :lines layer, one element per series); LineSegments, Errorbars, Rangebars (:pairs); Wireframe (rendered edges); Arrows3D (shaft start→end); HLines/VLines (the rendered span)\nRectInteractable Heatmap/Image (compact grid); BarPlot (dodge/stack/auto-width honored); Hist, Waterfall, CrossBar, Spy, HSpan, VSpan\nPolygonInteractable Poly (one ring or many); Band, Density (filled curve); Contourf (filled levels); Violin; Voronoiplot (cell polygons)\nTextInteractable Text directly; Annotation via its inner Text (its only constructor takes a Makie.Text, so annotation! labels are only reachable through this path or masque(fig), never a hand-written TextInteractable)\nPoint + Segment (two layers) Stem (id = points, id_stems = the stems); ScatterLines (id = points, id_line = the line)\nRect or Polygon (box body only) BoxPlot — a rect unless the box is notched, then a polygon; whiskers/outliers are decorative, not hit-tested\n\nid on all of these, and payloads on most, take the same keywords as the explicit constructors; defaults are the plot's own name in lowercase (:scatter, :lines, :hist, :crossbar, …) — except Heatmap/Image (:cells), BarPlot (:bars), and LineSegments (:segments). masque(fig)/auto_interactables use these same ids, so an ev.layer check against :heatmap or :barplot will never match — use :cells/:bars, and for selected=, :bars. The Heatmap/Image method is also the one exception that takes no payloads keyword at all, since grid cells resolve {i, j, value} client-side, same as the explicit grid = (...) form — and, being a :grid-kind layer, its :cells elements can't be pre-highlighted via selected= at all (see Selection). Any plot type not listed here — or here and in the Constructors table above — needs a custom interaction; see Custom interactions.","title":"From a plot object"},{"category":"section","location":"interactables/#Zero-config:-masque(fig)","page":"Interactables","text":"Skip the constructors entirely — masque(fig) walks every Axis, Axis3, PolarAxis, Colorbar, and Legend block, introspects each supported plot, and overlays the lot:\n\nbegin\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    scatter!(ax, xs, ys)\n    heatmap!(ax, X, Y, Z)\nend\n\n@bind ev masque(fig)   # both plots interactive; ev.layer tells you which was clicked\n\nLayer ids are the plot kind (:scatter, :hist, :band, …, from the table above — note the heatmap! above gets :cells, not :heatmap; see the exceptions listed there) plus :colorbar for any Colorbar block and :legend for any Legend block, suffixed _2, _3, … when a kind repeats within one figure. Unsupported plot types are skipped with a @warn, not an error.\n\nauto_interactables returns the same Vector{AbstractInteractable} masque(fig) builds, so you can grab it, tweak ids/payloads or append custom interactables, then pass it back:\n\nints = let\n    ints = auto_interactables(fig)\n    push!(ints, RegionInteractable(ax; regions = ..., payloads = ...))\n    ints\nend\n\n@bind ev masque(fig, ints)","title":"Zero-config: masque(fig)"},{"category":"section","location":"interactables/#3D-axes-and-PolarAxis","page":"Interactables","text":"Axis3 gets the point/segment kinds above with 3D-valid payloads: Scatter carries {index, x, y, z}, a Lines is one whole-line element whose default payload is {index} (the path is still projected from the 3D vertices), MeshScatter gets depth-correct per-marker hit radii from its data-space markersize, Wireframe's rendered edges are hoverable, and Arrows3D shafts hit as start→end segments — all projected once, in Julia, at build time, so it works the same way static on :cairo and live on :webgl (see Backends). PolarAxis gets the same discrete point/segment overlays on both backends; continuous θ/r readout is not shipped yet.\n\nAxisInteractable, ThresholdInteractable, ROIInteractable, and orbit-mode ViewInteractable don't work on either — see Troubleshooting for why and what to use instead. LScene isn't supported on either backend at all; see Troubleshooting.","title":"3D axes and PolarAxis"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"Full docstrings for every exported name, grouped by area. Usage guidance and worked examples live on the other pages; this page is the reference.","title":"API Reference"},{"category":"section","location":"api/#Entry-point","page":"API Reference","text":"","title":"Entry point"},{"category":"section","location":"api/#Interactable-constructors","page":"API Reference","text":"","title":"Interactable constructors"},{"category":"section","location":"api/#Tooltip-macro-and-type","page":"API Reference","text":"See Tooltips for usage and §10 for the wire format.","title":"Tooltip macro & type"},{"category":"section","location":"api/#Custom-interaction-interface","page":"API Reference","text":"The pieces Custom interactions build on:","title":"Custom-interaction interface"},{"category":"section","location":"api/#Backend-abstraction","page":"API Reference","text":"CairoBackend and WebGLBackend are the two concrete backends, but they're defined inside Masque's package extensions (ext/MasqueCairoMakieExt.jl, ext/MasqueWGLMakieExt.jl) rather than in Masque itself — they only exist once CairoMakie/WGLMakie is loaded, so Documenter can't resolve @docs for them without loading both weak dependencies into the docs build just to document two structs. They're documented in prose instead: see Backends for what each does, and masque's docstring above for the backend= keyword both accept.","title":"Backend abstraction"},{"category":"module","location":"api/#Masque","page":"API Reference","text":"Masque\n\nOverlay JS interactivity — hover tooltips, click-to-select, drag-to-pan/rotate — on a static or live Makie Figure for use in a Pluto notebook.\n\nDeclare AbstractInteractables (or call masque(fig) for zero-config auto-extraction via auto_interactables) and bind the result with @bind; the bond reports the current selection — nothing when nothing is selected, otherwise an InteractionEvent. Needs a rendering backend loaded: using CairoMakie for a static image with a JS hit-test overlay, or using WGLMakie for a live browser-GPU canvas (animation, large/live data, 3D) — both expose the same masque/@bind contract.\n\nExamples\n\nusing Masque, CairoMakie\nfig = Figure(); ax = Axis(fig[1, 1])\nscatter!(ax, [1, 2, 3], [1, 4, 9])\n@bind sel masque(fig)   # zero-config: auto-extracts the scatter\n\n\n\n\n\n","title":"Masque"},{"category":"function","location":"api/#Masque.masque","page":"API Reference","text":"masque(fig, interactables; kwargs...) -> MasqueWidget\nmasque(fig, interactable; kwargs...)    # single-interactable convenience\n\nOverlay fig with JS hit-testing and return a Pluto @bind source. fig is not mutated.\n\nThe bond is nothing until the first commit, unless selected= restored one. A click is one InteractionEvent. A selects ROIInteractable aimed at points makes every element commit a Vector{ElementEvent} (a click is a one-element vector; an empty box is ElementEvent[]). Aimed at a grid, the brush is one GridWindowEvent.\n\nKeywords\n\nselected — the selection's starting value, 1-based. One index on a point layer mounts as that ElementEvent; 1 and [1] are the same. Several indices highlight those marks and leave the bond nothing (a point layer holds one event). On a selects point brush, 1 and [1] mount as a one-element vector, and [] mounts as ElementEvent[]. Also accepts the event itself, or a NamedTuple / Dict keyed by layer id when the figure has more than one seedable layer. A bare index is an ArgumentError in that case. Works on :circles/:rects/:polygons/:segments/:polyline/:lines; any other kind, or an out-of-range index (0 included), raises ArgumentError naming 1:n. Clicking replaces the selection, so this is only needed to carry one through a rebuild — and it must come from a cell that doesn't read this widget's own bond, which Pluto rejects as a cyclic reference.\nbackend — CairoBackend() (static image) or WebGLBackend() (live canvas), each with its own keywords. Defaults to whichever of CairoMakie / WGLMakie is loaded, Cairo if both, ArgumentError if neither.\nmax_width — target display width in px (Pluto's column). Default 700.\ntooltip_bg, tooltip_color, tooltip_accent, tooltip_font, tooltip_font_size, tooltip_radius, tooltip_caret — tooltip card styling; each defaults to the built-in style. See the Tooltips page for the full system, including the --masque-tip-* CSS escape hatch.\n\nExamples\n\nusing Masque, CairoMakie\nfig = Figure(); ax = Axis(fig[1, 1])\npts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\nscatter!(ax, first.(pts), last.(pts))\n@bind sel masque(fig, [PointInteractable(ax, pts; payloads = [\"a\", \"b\", \"c\"])])\n\n\n\n\n\nmasque(fig; selected=nothing)\n\nAuto-extract interactables from fig (see auto_interactables) and overlay them — the zero-config path. Equivalent to masque(fig, auto_interactables(fig)); unsupported plot types are skipped with a warning. For control over ids/payloads, build the vector yourself.\n\n\n\n\n\n","title":"Masque.masque"},{"category":"function","location":"api/#Masque.auto_interactables","page":"API Reference","text":"auto_interactables(fig) -> Vector{AbstractInteractable}\n\nIntrospect a Makie Figure: for every supported plot in every Axis, Axis3, or PolarAxis, build the interactable its explicit constructor would. On Axis3, only Scatter/Lines/ LineSegments/MeshScatter/Wireframe/Arrows3D are supported; on PolarAxis, only Scatter/Lines/LineSegments/ScatterLines/Series. Other kinds are skipped with a warning. Layer ids are the plot kind (:scatter, :lines, …), suffixed _2, _3, … when a kind repeats. Returns the same concrete vector you could pass to masque yourself — edit or extend it freely.\n\nEach interactable inherits its constructor's default per-element payloads, so the zero-config path on a very large plot allocates one payload per element; construct with a lean payloads= yourself for huge data.\n\n\n\n\n\n","title":"Masque.auto_interactables"},{"category":"type","location":"api/#Masque.InteractionEvent","page":"API Reference","text":"InteractionEvent\n\nAbstract type of every masque @bind value that is not nothing. Concrete subtypes carry the fields of that commit: an element pick, a legend entry, a grid cell or window, an axis or colorbar click, a threshold, or ROI bounds. A selects ROI over points yields a Vector{ElementEvent} instead of one event. Field names on the struct win over a payload key of the same name; on an ElementEvent or LegendEvent, other names forward to the payload. See bondtype and transform_bond.\n\n\n\n\n\n","title":"Masque.InteractionEvent"},{"category":"type","location":"api/#Masque.ElementEvent","page":"API Reference","text":"ElementEvent(layer, index, payload)\n\nOne element of a point, bar, polygon, segment, polyline, or list-of-rects layer. index is 1-based. Other fields (e.g. city) are read from payload. Use as a row index: df[pick, :], xs[pick].\n\n\n\n\n\n","title":"Masque.ElementEvent"},{"category":"type","location":"api/#Masque.LegendEvent","page":"API Reference","text":"LegendEvent(layer, index, payload)\n\nOne legend entry. index is which entry (1-based), not a row of the author's table. entry.label and entry.targets come from the payload. df[entry, :] does not treat the entry number as a row.\n\n\n\n\n\n","title":"Masque.LegendEvent"},{"category":"type","location":"api/#Masque.GridCellEvent","page":"API Reference","text":"GridCellEvent(layer, i, j, value)\n\nOne heatmap or image cell. i and j are 1-based column and row of the author's (ncols, nrows) matrix. A[cell] is A[cell.i, cell.j]. value is the shipped cell value, or nothing when values were not sent.\n\n\n\n\n\n","title":"Masque.GridCellEvent"},{"category":"type","location":"api/#Masque.GridWindowEvent","page":"API Reference","text":"GridWindowEvent(layer, i1, i2, j1, j2, xmin, xmax, ymin, ymax)\n\nA brushed window on a grid. Ranges are 1-based inclusive: A[win] is A[win.i1:win.i2, win.j1:win.j2]. A miss has an empty i1:i2 (and j1:j2).\n\n\n\n\n\n","title":"Masque.GridWindowEvent"},{"category":"type","location":"api/#Masque.AxisEvent","page":"API Reference","text":"AxisEvent(layer, x, y)\n\nAn axis click at data coordinates (x, y). Not an array index.\n\n\n\n\n\n","title":"Masque.AxisEvent"},{"category":"type","location":"api/#Masque.ThresholdEvent","page":"API Reference","text":"ThresholdEvent(layer, value)\n\nA threshold drag release at data coordinate value. Pass back as value= to restore the line.\n\n\n\n\n\n","title":"Masque.ThresholdEvent"},{"category":"type","location":"api/#Masque.ColorbarEvent","page":"API Reference","text":"ColorbarEvent(layer, value)\n\nA colorbar click at data coordinate value.\n\n\n\n\n\n","title":"Masque.ColorbarEvent"},{"category":"type","location":"api/#Masque.BoundsEvent","page":"API Reference","text":"BoundsEvent(layer, xmin, xmax, ymin, ymax)\n\nA bounds-only ROI release. Pass back as bounds= to restore the box. A selects ROI is not a BoundsEvent.\n\n\n\n\n\n","title":"Masque.BoundsEvent"},{"category":"function","location":"api/#Masque.bondtype","page":"API Reference","text":"bondtype(interactable) -> Type\n\nThe type of one commit from this interactable. Default ElementEvent. A widget that also carries a selects ROI aimed at points returns a Vector{ElementEvent} for every element commit; that is a property of the call, not of the point layer's bondtype.\n\n\n\n\n\n","title":"Masque.bondtype"},{"category":"function","location":"api/#Masque.transform_bond","page":"API Reference","text":"transform_bond(interactable, layer, index, js_payload) -> InteractionEvent\n\nBuild one event from a wire hit. index is already 1-based for an element kind, or nothing for axis / colorbar / threshold / ROI / grid (those read the browser payload). Custom interactables implement this (and bondtype) when the default ElementEvent is not enough; a type that only wants ElementEvent writes hitlayers and inherits the default.\n\n\n\n\n\n","title":"Masque.transform_bond"},{"category":"type","location":"api/#Masque.PointInteractable","page":"API Reference","text":"PointInteractable(ax, points; id=:points, payloads=<auto>, radius=nothing, radius3d=nothing, tooltip=nothing)\nPointInteractable(ax, p::Makie.Scatter; id=:scatter, payloads=nothing, radius=nothing)\nPointInteractable(ax, p::Makie.MeshScatter; id=:meshscatter, payloads=nothing, radius=nothing, radius3d=nothing)\n\nScatter-style points, hit-tested as circles. Produces one :circles HitLayer.\n\nArguments\n\npoints — data-space points, each a 2- or 3-element point/tuple (Axis3 scatters use 3).\nid — the layer id; becomes InteractionEvent.layer on a hit.\npayloads — one entry per point (ArgumentError if the length doesn't match points), or a DataFrame with one row per point once DataFrames is loaded. Default: (; index, x, y), or (; index, x, y, z) for 3-coordinate points — index is 1-based.\nradius — highlight and click-target radius in px (scaled to the rendered image's DPI), the same for every point. Default nothing: use the drawn radius of the one Scatter on ax with these positions (see below). If none matches, or more than one does, assume Makie's default :circle at the theme markersize (radius ≈0.3525×markersize; the theme default markersize is 9). Pass a number to override. A matched marker with no readable bbox (a Char, an image, a per-element vector of markers) keeps the markersize / 2 bound.\nradius3d — per-point data-space half-extents (Vector{Makie.Vec3f}), for markers whose on-screen size is camera/depth-dependent (e.g. meshscatter). When set, overrides radius with an axis-aligned pixel-radius approximation projected per point — it can underestimate the true silhouette (worst case ~29%, at adversarial azimuth/elevation). Must have one entry per point (ArgumentError otherwise).\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError; not meaningful).\nlabel — an optional screen-reader announcement prefix for this layer (e.g. \"Scatter\"), used by the overlay's keyboard navigation (\"Scatter, element 3 of 10: …\"). Default nothing (no prefix). Not the same thing as a label payload key (see the PointInteractable examples elsewhere in this file) — that's per-element tooltip data.\ncolors — an optional tooltip accent colour; see HitLayer's colors field. Default nothing (no accent). Not derived automatically here — only PointInteractable(ax, p::Makie.Scatter) resolves it, from p's own colour.\n\nFrom a plot object\n\nPointInteractable(ax, p::Makie.Scatter) is the usual call. It reads points from p's converted data and derives radius from the marker's drawn extent, not the full markersize square — Makie's default :circle marker draws a disc of diameter ≈0.705·markersize (radius ≈0.3525·markersize), so that's what ships; other default_marker_map() symbols/BezierPaths use their own bbox in the same way, a GeometryBasics Circle/Rect marker draws at the full markersize (radius = markersize / 2), and anything else (a Char glyph, an image, a per-element vector of markers) falls back to markersize / 2 as a conservative bound. This requires markerspace = :pixel (the default); pass radius= explicitly for any other markerspace, or it errors. The points constructor above takes the same radius when exactly one Scatter on ax has the same positions (including a scatterlines!/stem! child scatter); it does not resolve colors. The overlay adds its own hit-test slack on top, so the smaller radius doesn't make small markers harder to click. The scatter constructor also resolves colors from p.color[]: a single colour (including a bare numeric value mapped through colormap) ships as one CSS string; a numeric (colormap-driven) or explicit per-point colour vector ships as a shared palette + one index per point; anything else (e.g. no colour, or unresolvable) omits colors — no accent, not an error. A value outside colorrange is clamped to the nearest palette end rather than resolved through lowclip/highclip, so such a point's accent can differ slightly from its marker's actual (clipped) colour. PointInteractable(ax, p::Makie.MeshScatter) derives radius3d from p's data-space markersize (a Vec3f, a Real, or a per-element vector of either); pass radius=/radius3d= explicitly if it can't be derived.\n\nExamples\n\npts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\nscatter!(ax, first.(pts), last.(pts); markersize = 14)\nPointInteractable(ax, pts)   # radius ≈ 0.3525 * 14, from that scatter\n\np = scatter!(ax, xs, ys; markersize = 14)\nPointInteractable(ax, p)   # the usual call; same radius, and colors from p\n\n\n\n\n\n","title":"Masque.PointInteractable"},{"category":"type","location":"api/#Masque.SegmentInteractable","page":"API Reference","text":"SegmentInteractable(ax, vertices; mode=:polyline, unit=:segment, id=:segments, payloads=nothing, tol=6, tooltip=nothing)\nSegmentInteractable(ax, p; id=<kind-specific>, payloads=nothing, tol=6)   # from a plot object\n\nLines / polylines or disjoint segment pairs. Produces one :polyline, :lines, or :segments HitLayer.\n\nArguments\n\nvertices — data-space points, each a 2- or 3-element point/tuple.\nmode — :polyline (default): vertices is one connected path. :pairs: vertices is disjoint pairs (v1,v2), (v3,v4), …, length(vertices) ÷ 2 segments. Any other value raises ArgumentError.\nunit — what one element is. :segment (default): a :polyline path is hit per edge (length(vertices) - 1 elements) and :pairs is hit per pair. :line: the whole path is one element (kind :lines) — the pointer still has to land within tol of some edge, and a NaN gap stays a gap in that one line. :line requires mode = :polyline (ArgumentError otherwise). This is how lines! / stairs! / a scatterlines! line are built; the raw vertex constructor stays per-segment unless you pass unit = :line.\nid — the layer id; becomes InteractionEvent.layer on a hit.\npayloads — one entry per element (per segment when unit = :segment, one entry when unit = :line); ArgumentError if the length doesn't match. Default: (; segment_index) (1-based) for :segment, (; index) (1-based) for :line.\ntol — hit-test slack around a segment, in logical px (scaled to the rendered image's DPI like PointInteractable's radius). Must be finite and positive (ArgumentError otherwise). Shipped in the manifest as a per-layer \"tol\" field; the overlay's client-side default (SEG_TOL in frontend/src/geometry.ts, 8 image px) applies only when this field is absent.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\nlabel — an optional screen-reader announcement prefix for this layer (see PointInteractable). Default nothing.\n\nFrom a plot object\n\nSegmentInteractable(ax, p) reads vertices from p (no mode/unit/tooltip keyword — those are fixed by the plot type):\n\np default id hit vertices from\nMakie.Lines :lines one :lines element, the whole path converted data\nMakie.Stairs :stairs one :lines element, the whole staircase the child Lines' pre-expanded step polyline\nMakie.Series :series one :lines layer, one element per series each child line (or a ScatterLines child's line)\nMakie.LineSegments :segments :pairs, per segment converted data\nMakie.Wireframe :wireframe :pairs, per edge the child LineSegments' edges (incl. mesh-triangulation diagonals)\nMakie.Arrows3D :arrows3d :pairs, per shaft processed startpoints/endpoints (post-align/lengthscale); default payload (; index, x, y, z, u, v, w) from points/directions\nMakie.Errorbars :errorbars :pairs, per bar each bar's low→high endpoints\nMakie.Rangebars :rangebars :pairs, per bar each bar's low→high endpoints\nMakie.HLines :hlines :pairs, per line each line spanning the axis's current data range (re-resolved on limit changes)\nMakie.VLines :vlines :pairs, per line each line spanning the axis's current data range (re-resolved on limit changes)\n\nA series! element's default payload is (; index, label) when the child plot's label is a non-empty string (Makie's own default is \"series k\"), otherwise (; index).\n\nExamples\n\np = lines!(ax, xs, ys)\nSegmentInteractable(ax, p)                       # one element: the whole line\n\nSegmentInteractable(ax, [(0,0), (1,1), (2,0)]; mode = :polyline)          # per segment\nSegmentInteractable(ax, [(0,0), (1,1), (2,0)]; mode = :polyline, unit = :line)  # one element\n\n\n\n\n\n","title":"Masque.SegmentInteractable"},{"category":"type","location":"api/#Masque.RectInteractable","page":"API Reference","text":"RectInteractable(ax; rects, id=:rects, payloads=nothing, tooltip=nothing, clamp_to_viewport=false)\nRectInteractable(ax; grid, id=:rects, payloads=nothing, tooltip=nothing)\nRectInteractable(ax, p; id=<kind-specific>, payloads=nothing)   # from a plot object\n\nAxis-aligned rectangles: an explicit list (bars, boxes) or a compact heatmap/image grid. Pass exactly one of rects/grid — passing both, or neither, raises ArgumentError at construction. Produces one :rects or :grid HitLayer.\n\nArguments (list form: rects=)\n\nrects — data-space boxes [(xc, yc, w, h), …] (center + width/height).\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :rects.\npayloads — one entry per rect; ArgumentError if the length doesn't match. Default: (; index), 1-based.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\nclamp_to_viewport — clamp each rect's pixel bounds to the axis viewport (inward rounding, so integer quantization never expands past the edge) before shipping geometry. Used internally by the HSpan/VSpan introspection methods below; rarely needed directly. Default false.\nlabel — an optional screen-reader announcement prefix for this layer (see PointInteractable). Default nothing. Accepted for both forms, but inert on a :grid layer — it's still stored and shipped in the manifest, but has no effect, since a :grid layer is not keyboard-navigable (element count is unbounded).\n\nArguments (grid form: grid=)\n\ngrid — (xedges, yedges, values): xedges/yedges are cell-edge vectors (length ncols+1/nrows+1), values an (ncols, nrows) Matrix of per-cell values. Shape mismatch raises ArgumentError. id/tooltip as above; payloads is unused (cell (i, j, value) is resolved client-side from values). If a cell renders under ~1 screen px, values is dropped from the manifest to bound its size (hover then shows (i, j) only; a @warn notes it) — clicks still carry the cell index.\n\nFrom a plot object\n\nRectInteractable(ax, p) builds rects/grid and default payloads from p:\n\np default id form notes\nMakie.Heatmap / Makie.Image :cells grid edges from converted coordinate/coordinate-free ranges\nMakie.BarPlot :bars list reads the laid-out child Poly (dodge/stack/auto-width honored); payload (; low, high, value)\nMakie.Spy :spy list cell size from the child Scatter's data-space markersize (length-2 vector or scalar; other shapes error)\nMakie.Hist :hist list payload (; value, low, high) (value is a count only for normalization = :none)\nMakie.Waterfall :waterfall list payload (; low, high, value)\nMakie.CrossBar :crossbar list payload (; midpoint, low, high)\nMakie.HSpan :hspan list spans the full x-range of ax's current limits; clamp_to_viewport = true; payload (; low, high) (y-bounds); re-resolved on limit changes\nMakie.VSpan :vspan list spans the full y-range of ax's current limits; clamp_to_viewport = true; payload (; low, high) (x-bounds); re-resolved on limit changes\n\nExamples\n\nRectInteractable(ax; rects = [(0.0, 0.0, 1.0, 1.0)], payloads = [(; label = \"a\")])\n\nxedges = 0:0.5:2; yedges = 0:1:3; vals = rand(4, 3)\nRectInteractable(ax; grid = (xedges, yedges, vals))\n\np = heatmap!(ax, X, Y, Z)\nRectInteractable(ax, p)\n\n\n\n\n\n","title":"Masque.RectInteractable"},{"category":"type","location":"api/#Masque.PolygonInteractable","page":"API Reference","text":"PolygonInteractable(ax, rings; id=:polygons, payloads=nothing, tooltip=nothing)\nPolygonInteractable(ax, p; id=<kind-specific>, payloads=nothing)   # from a plot object\n\nArbitrary filled polygons, hit-tested even-odd. Produces one :polygons HitLayer.\n\nArguments\n\nrings — Vector{Vector{point}}, one or more rings, each a Vector of 2- or 3-element data-space points/tuples (one polygon per ring; a ring need not be closed — the hit-test closes it implicitly).\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :polygons.\npayloads — one entry per ring; ArgumentError if the length doesn't match. Default: (; index), 1-based.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\nlabel — an optional screen-reader announcement prefix for this layer (see PointInteractable). Default nothing.\n\nFrom a plot object\n\nPolygonInteractable(ax, p) builds rings and default payloads from p:\n\np default id rings from notes\nMakie.Poly :poly converted geometry one ring, or many for a multi-ring Poly\nMakie.Band :band lower curve + reversed upper curve always exactly one open ring\nMakie.Density :density its descendant Band's KDE fill same shape as Band\nMakie.Contourf :contourf each filled level's exterior ring only (holes excluded — a v1 limitation: an annular band over-covers its hole at the boundary) payload (; low, high), the band edges nearest each polygon's fill color\nMakie.Violin :violin each violin's outline payload (; x), the nearest category to the ring's geometric center\nMakie.Voronoiplot :voronoiplot each cell's exterior ring cells come back in tessellation order (no cheap cell→generator map), so default payload is (; index) only\n\nExamples\n\nPolygonInteractable(ax, [[(0,0), (1,0), (1,1), (0,1)]])\n\np = poly!(ax, points)\nPolygonInteractable(ax, p)\n\n\n\n\n\n","title":"Masque.PolygonInteractable"},{"category":"type","location":"api/#Masque.AxisInteractable","page":"API Reference","text":"AxisInteractable(ax; id=:axis)\n\nThe whole axis as one hit region: a click or hover anywhere returns the data coordinate under the cursor. No per-element geometry — the browser inverts pixels→data live via the shipped AxisTransform (no Julia round-trip on hover). Produces one :axis HitLayer with geometry = nothing.\n\nArguments\n\nax — a Makie.Axis (linear, log, or categorical). id — the layer id; becomes InteractionEvent.layer on a hit. Default :axis.\n\nPayload on hit (client-side): (; x, y).\n\nmasque raises ArgumentError at build time if ax is an Axis3 (a screen pixel is a ray, not a data point — continuous readout is undefined), a PolarAxis (continuous θ/r inversion isn't shipped to JS yet), or either scale isn't client-invertible (supported: identity, log10, log; categorical axes are fine).\n\nExamples\n\nAxisInteractable(ax)\n\n\n\n\n\n","title":"Masque.AxisInteractable"},{"category":"type","location":"api/#Masque.ColorbarInteractable","page":"API Reference","text":"ColorbarInteractable(cb; id=:colorbar)\n\nA Makie.Colorbar block as one hit region, bounded to its pixel bounding box: a click or hover anywhere on the bar inverts the cursor position to the bar's data value, client-side — like AxisInteractable but scoped to the colorbar and 1-D. Produces one :axis HitLayer with geometry set to the colorbar's pixel bbox.\n\nArguments\n\ncb — a Makie.Colorbar (not an Axis). id — the layer id; becomes InteractionEvent.layer on a hit. Default :colorbar.\n\nPayload on hit (client-side): (; value).\n\nmasque raises ArgumentError at build time if the colorbar's value-axis scale isn't client-invertible (supported: identity, log10, log).\n\nExamples\n\ncb = Colorbar(fig[1, 2], plotobj)\nColorbarInteractable(cb)\n\n\n\n\n\n","title":"Masque.ColorbarInteractable"},{"category":"type","location":"api/#Masque.LegendInteractable","page":"API Reference","text":"LegendInteractable(leg; id=:legend, targets=nothing, events=(:click,:hover), tooltip=nothing)\n\nA Makie.Legend block whose entries are hit regions linking to the plot layer(s) they stand for: hover/click an entry to highlight the layer(s) named in targets. Produces one :rects HitLayer, one rect per legend entry.\n\nArguments\n\nleg — a Makie.Legend.\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :legend.\ntargets — how each entry links to other layers, resolved once at construction:\nnothing (default) — masque(fig) links each entry to the layers of the plots its elements were built with (Makie.get_plots). That lookup runs during auto extraction. Calling LegendInteractable yourself, or a hand-built entry whose elements carry no plots, leaves the link list empty. The entry stays a hit target: hover leaves the other layers as they are, and a click reports a LegendEvent.\na Dict{<:AbstractString} keyed by entry label — Symbol or Vector{Symbol} of layer ids for that entry. A key matching no entry label raises ArgumentError.\na Vector with one entry per legend entry (nothing/Symbol/Vector{Symbol}), in entry order (top-to-bottom, matching leg.entrygroups[] flattened). A wrong-length Vector raises ArgumentError.\nEvery id named here must belong to another layer in the same masque() call whose kind supports pre-highlight (ArgumentError from build_manifest otherwise — see HitLayer's links field). A spec may be a layer id (every element of that layer) or id:k pinning element k (1-based); auto-extracted series! entries use the pin.\ntooltip — nothing (default) and false show no card. The entry's label is already drawn in the row, and a card there covers the entries around it. masque\"...\" shows that template (payload fields: label, group, targets). tooltip = true is rejected (ArgumentError). Focusing an entry still announces its label when no template is set.\nevents — the pointer events this layer responds to. Default (:click, :hover).\n\nA custom legend built from LineElement/MarkerElement/PolyElement without plots= has nothing to auto-link — pass plots= on the element (Makie's own kwarg) or use targets= here.\n\nmasque raises ArgumentError at build time if leg is handed to AxisInteractable, ViewInteractable, ThresholdInteractable, or ROIInteractable instead — those need a Makie.Axis, not a Legend.\n\nExamples\n\nl1 = lines!(ax, xs, ys1; label = \"a\")\nl2 = lines!(ax, xs, ys2; label = \"b\")\nleg = axislegend(ax)\nLegendInteractable(leg)   # hit targets only; `masque(fig)` is what fills in the links\n\nLegendInteractable(leg; targets = Dict(\"a\" => :lines, \"b\" => [:lines_2, :scatter]))\n\n\n\n\n\n","title":"Masque.LegendInteractable"},{"category":"type","location":"api/#Masque.TextInteractable","page":"API Reference","text":"TextInteractable(ax, p::Makie.Text; id=:text, payloads=nothing, tooltip=nothing)\n\nClick-to-pick text labels (from text!/annotation!), hit-tested as bounding-box rects. Has no explicit-geometry constructor — this from-a-plot-object form is the only way to build one. Produces one :rects HitLayer, one box per string.\n\nArguments\n\np — a Makie.Text plot (for annotation!, pass its descendant Text, e.g. via auto_interactables).\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :text.\npayloads — one entry per string; ArgumentError if the length doesn't match. Default: (; text, index, x, y) — text is the string, index 1-based, (x, y) its data-space anchor.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\n\nGeometry is each string's axis-aligned bounding box (Makie.string_boundingboxes), not projected data coordinates — a rotated label gets its expanded axis-aligned box. Boxes are read lazily in hitlayers, not at construction, so a TextInteractable can be built before the figure is finalized.\n\nExamples\n\np = text!(ax, \"hello\"; position = (1.0, 2.0))\nTextInteractable(ax, p)\n\n\n\n\n\n","title":"Masque.TextInteractable"},{"category":"type","location":"api/#Masque.ThresholdInteractable","page":"API Reference","text":"ThresholdInteractable(ax; orientation=:horizontal, value, id=:threshold)\n\nA draggable horizontal or vertical line: drag for a live client-side readout, and the pixel position inverts to a data-space scalar via AxisTransform on mouse-up. Produces one :threshold HitLayer.\n\nArguments\n\nax — a Makie.Axis.\norientation — :horizontal (constant-y line, dragged vertically) or :vertical (constant-x line, dragged horizontally). Any other value raises ArgumentError. Default :horizontal.\nvalue — the line's initial data-space position (a y-value for :horizontal, x-value for :vertical). Required, no default.\nid — the layer id; becomes InteractionEvent.layer on commit. Default :threshold.\n\nPayload on commit (client-side): the scalar data coordinate.\n\nmasque raises ArgumentError at build time if ax is an Axis3 (a screen pixel is a ray, not a data value — inversion is undefined), a PolarAxis (continuous inversion isn't shipped), or the dragged axis's scale isn't client-invertible (:horizontal needs the y-scale, :vertical the x-scale; supported: identity, log10, log).\n\nExamples\n\nThresholdInteractable(ax; orientation = :horizontal, value = 5.0)\n\n\n\n\n\n","title":"Masque.ThresholdInteractable"},{"category":"type","location":"api/#Masque.ROIInteractable","page":"API Reference","text":"ROIInteractable(ax; bounds, id=:roi, selects=nothing)\n\nA draggable and resizable rectangle: drag the interior to move it, a corner to resize both edges, or the middle of a side (no drawn grip there) to resize just that one edge; on mouse-up its two opposite pixel corners invert to data-space bounds via AxisTransform. An AbstractSelector — with selects set, it also brushes a compatible layer, reporting the contained elements. Produces one :roi HitLayer.\n\nArguments\n\nax — a Makie.Axis.\nbounds — initial (xmin, xmax, ymin, ymax) in data space. Requires xmin < xmax and ymin < ymax (ArgumentError otherwise); length must be 4 (ArgumentError otherwise).\nid — the layer id; becomes InteractionEvent.layer on commit. Default :roi.\nselects — the id of a :circles or :grid layer to brush: on mouse-up, elements whose geometry falls inside the ROI are reported. masque raises ArgumentError at build time if selects names a layer absent from the same call, or one of an unsupported kind.\n\nPayload on commit (no selects): a BoundsEvent. With selects set, the bond is a Vector{ElementEvent} for a :circles target (one per contained element), or one GridWindowEvent for a :grid target — see InteractionEvent. bounds= accepts a BoundsEvent or a (xmin, xmax, ymin, ymax) tuple.\n\nmasque raises ArgumentError at build time if ax is an Axis3 (a screen pixel is a ray, not a data point), a PolarAxis (continuous inversion isn't shipped), a categorical axis (bounds need numeric limits), or either scale isn't client-invertible (supported: identity, log10, log).\n\nExamples\n\nROIInteractable(ax; bounds = (0.0, 1.0, 0.0, 1.0))\n\n# brush a scatter layer named :scatter\nROIInteractable(ax; bounds = (0.0, 1.0, 0.0, 1.0), selects = :scatter)\n\n\n\n\n\n","title":"Masque.ROIInteractable"},{"category":"type","location":"api/#Masque.ViewInteractable","page":"API Reference","text":"ViewInteractable(ax; id=:view)\n\nDrag-to-pan (2D Axis) or drag-to-orbit (Axis3). Produces one :view HitLayer covering ax's whole viewport; it sorts after Tier-0 :threshold/:roi layers so an ordinary drag on those wins without a modifier — Shift+drag forces the view gesture even over a ThresholdInteractable/ROIInteractable hit.\n\nArguments\n\nax — a Makie.Axis (pan) or Makie.Axis3 (orbit). id — the layer id, used to route gesture-channel frame requests back to this axis. Default :view.\n\nCommits nothing. A camera is operational state, not an analysis value a notebook reads (docs/dev/architecture/12-gesture-channel.md §12.3) — the bond masque returns never carries a :view InteractionEvent. Drag frames stream over a with_js_link gesture channel instead, on both backends: Julia mutates ax.limits[] (pan) or ax.azimuth[]/ax.elevation[] (orbit) and ships a hit manifest for every frame the camera moves. :cairo ships that frame as a PNG; :webgl ships a freshly serialized scene applied to the canvas already on the page. In-drag frames render at px_per_unit = 1; the release frame renders at the widget's own resolution. Because nothing commits, ax's camera stays wherever the gesture left it until the cell re-runs (a fresh Figure/Axis resets it); to persist a view across re-renders, bind it explicitly with the Ref + @bind pattern (§12.8).\n\nmasque raises ArgumentError at build time if ax is a PolarAxis (continuous θ/r view gestures aren't shipped), a Colorbar's value axis (no pan/orbit view applies), a categorical 2D axis (pan needs numeric limits to shift), or (2D only) either scale isn't client-invertible (supported: identity, log10, log). Axis3 has no scale/categorical restriction — camera angles are read live from ax at render time.\n\nExamples\n\nViewInteractable(ax)\n\n\n\n\n\n","title":"Masque.ViewInteractable"},{"category":"type","location":"api/#Masque.RegionInteractable","page":"API Reference","text":"RegionInteractable(ax; regions, payloads, id=:region, tooltip=nothing, events=(:click, :hover))\n\nDeclarative mixed hit regions in data space — circles, rects, and polygons in one call, no JavaScript required. Grouped into up to three HitLayers (one per geometry kind present), so a single call can mix shapes freely.\n\nArguments\n\nregions — a Vector, each element one of:\n(:circle, (cx, cy), r) — r in data units\n(:rect, (cx, cy), w, h) — w, h in data units\n(:polygon, [(x, y), …]) — a ring of points\nAny other first element raises ArgumentError.\npayloads — one entry per region, matched 1:1 by position (ArgumentError on a length mismatch); no auto-generated default (unlike the other built-ins, this keyword is required).\nid — the base layer id. The generated layers are Symbol(id, :_c) (circles), Symbol(id, :_r) (rects), Symbol(id, :_p) (polygons) — only the kinds actually present are emitted. InteractionEvent.layer and selected= keys use these suffixed ids, not id itself. Default :region.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress; applies to every generated layer. tooltip = true is rejected (ArgumentError).\nevents — the pointer events all generated layers respond to. Default (:click, :hover).\n\nExamples\n\nRegionInteractable(\n    ax;\n    regions = [(:circle, (0.0, 0.0), 1.0), (:rect, (3.0, 0.0), 2.0, 1.0)],\n    payloads = [(; label = \"circle\"), (; label = \"rect\")],\n)\n\n\n\n\n\n","title":"Masque.RegionInteractable"},{"category":"type","location":"api/#Masque.FunctionInteractable","page":"API Reference","text":"FunctionInteractable(f; events=(:click, :hover))\n\nFull-control escape hatch for a geometry kind the other built-ins don't express: f(ctx) -> Vector{HitLayer} is called at manifest-build time and its result is used verbatim.\n\nArguments\n\nf — a function (ctx::InteractionContext,) -> Vector{HitLayer}. Project data-space points with data_to_image_px(ctx, ax, point), and look up an axis's transform id with Masque.axis_id(ctx, ax) (not exported) when constructing a HitLayer.\nevents — the pointer events reported by Masque.events(::FunctionInteractable); f is free to give its HitLayers different events per layer if it wants. Default (:click, :hover).\n\nExamples\n\nFunctionInteractable() do ctx\n    q = data_to_image_px(ctx, ax, (1.0, 2.0))\n    [HitLayer(:custom, :circles, [q[1], q[2], 10], [(; label = \"manual\")], Masque.axis_id(ctx, ax), (:click, :hover))]\nend\n\n\n\n\n\n","title":"Masque.FunctionInteractable"},{"category":"type","location":"api/#Masque.Markup","page":"API Reference","text":"Markup\n\nA parsed masque\"...\" tooltip template: ordered segments (each a literal String, emitted verbatim as HTML, or a Field placeholder) plus the unique fields referenced. Built by @masque_str — see that macro for template syntax; construct a Markup yourself only if you need parse_template/check_fields directly.\n\nPass a Markup as an interactable's tooltip= keyword to render it client-side, from the hovered element's payloads[] entry, with no round-trip to Julia. At masque()/build_manifest time every field the template references is checked against the layer's actual payload keys (when payloads are NamedTuples); a missing field raises ArgumentError with a \"did you mean?\" suggestion, before any figure is rendered.\n\n\n\n\n\n","title":"Masque.Markup"},{"category":"macro","location":"api/#Masque.@masque_str","page":"API Reference","text":"masque\"...\"\n\nBuild a Markup tooltip template for an interactable's tooltip= keyword. Placeholders are resolved in the browser, from the hovered element's payloads[] entry — $(field) is not Julia string interpolation, and there is no runtime masque(string) form.\n\nSyntax\n\nEverything outside $(...) is literal HTML, inserted as-is (author-trusted, not sanitized) — escape your own </& as you would in @htl.\n$(field) — the hovered element's payload field field (an identifier), HTML-escaped at hover time.\n$(field:spec) — same, formatted by a d3-format spec before escaping (e.g. $(population:,), $(pct:.1%)). spec is checked structurally at macro-expansion (valid d3-format grammar); its runtime meaning (precision, trim, sign) is the browser's format() to interpret.\n\\$ — a literal dollar sign.\n\nA template with no $(...) at all is a fixed label, e.g. masque\"<em>static</em>\".\n\nErrors\n\nMalformed syntax (a bare $, unbalanced/empty $(), a non-identifier field name, or a structurally invalid d3-format spec) raises TemplateValidationError at macro-expansion — before masque() is ever called, with a caret pointing at the offending span. A field that parses fine but is absent from the payload at masque()/build_manifest time raises ArgumentError instead (see Markup).\n\nSee the Tooltips page of the documentation for the full template/tooltip system (styling, security model, wire format).\n\nExamples\n\ntooltip = masque\"<b>$(name)</b> — $(population:,) people\"\nPointInteractable(ax, pts; payloads = [(; name = \"a\", population = 1200)], tooltip)\n\n\n\n\n\n","title":"Masque.@masque_str"},{"category":"type","location":"api/#Masque.AbstractInteractable","page":"API Reference","text":"AbstractInteractable\n\nSupertype for everything masque can turn into hit-testable JS layers. The built-in kinds (PointInteractable, SegmentInteractable, RectInteractable, PolygonInteractable, AxisInteractable, ColorbarInteractable, LegendInteractable, ThresholdInteractable, ROIInteractable, TextInteractable, ViewInteractable, RegionInteractable, FunctionInteractable) cover most needs; implement this interface for anything else.\n\nInterface\n\nOnly hitlayers is exported — every other method below is a non-exported function of the Masque module. Extend them as Masque.validate(::MyType, ctx) = …, etc.; a bare validate(::MyType, ctx) = … at top level defines an unrelated function that masque never calls, and masque(fig, MyType()) will build without error while silently ignoring it.\n\nRequired:\n\nhitlayers(i, ctx::InteractionContext) -> Vector{HitLayer} — see hitlayers (exported).\n\nOptional (default shown; all non-exported — extend as Masque.<name>):\n\nMasque.validate(i, ctx::InteractionContext) -> Union{Nothing,String} — return an error message if i can't be built against ctx (masque raises it as ArgumentError), else nothing. Default: always valid.\nMasque.events(i) -> Tuple — the pointer events this interactable's layer(s) respond to (:click, :hover, :drag). Default: (:click, :hover).\nMasque.tooltip_spec(i) — nothing for the auto name/value table, a Markup (built with masque\"...\") template, or false to suppress. Default: nothing.\nMasque.hoverstyle(i) -> NamedTuple — one (; stroke, width) hover outline style per layer (the manifest ships one style per layer, not per element). Default: (; stroke = nothing, width = 2) — stroke = nothing means the overlay draws its own split highlight: a brightening color-dodge fill plus a flat chrome edge stroke (#7a7a7a on a light figure, #c8c8c8 on a dark one; the stroke is not blended into the mark), instead of a stroke colour, so every layer brightens without Masque resolving the element's colour; a CSS colour string overrides it verbatim for that layer (no blend, single unblended element, the outline is exactly that colour). colors (see HitLayer) no longer affects the hover/selection outline at all — it only drives the tooltip's accent border.\nMasque.hit_tol(i) -> Union{Nothing,Real} — logical-px hit-test slack for :segments/ :polyline/:lines layers, shipped in the manifest as image px (round(Int, hit_tol(i) * ctx.scaling)). nothing (default) omits the field; the overlay then falls back to its own fixed slack. Only SegmentInteractable sets this.\n\nAbstractSelector subtypes additionally implement Masque.selects/Masque.compatible_kinds.\n\n\n\n\n\n","title":"Masque.AbstractInteractable"},{"category":"type","location":"api/#Masque.AbstractSelector","page":"API Reference","text":"AbstractSelector\n\nSupertype for interactables that highlight elements on another layer — today just ROIInteractable's selects mode, brushing a :circles/:grid layer. Subtypes additionally implement these non-exported functions (extend as Masque.selects(::MyType) = …):\n\nMasque.selects(i) -> Union{Nothing,Symbol} — the target layer id, or nothing.\nMasque.compatible_kinds(i) -> Tuple — the target HitLayer.kinds this selector accepts; masque raises ArgumentError at build time if selects names a layer of an unlisted kind.\n\n\n\n\n\n","title":"Masque.AbstractSelector"},{"category":"type","location":"api/#Masque.HitLayer","page":"API Reference","text":"HitLayer\n\nThe unit masque serializes to the browser: one geometry kind for one interactable, plus the data needed to resolve a pointer hit to an element index and its payload. Built by hitlayers; user-facing mainly when writing a FunctionInteractable.\n\nFields\n\nid::Symbol — the layer id; becomes InteractionEvent.layer on a hit.\nkind::Symbol — one of :circles, :polyline, :lines, :segments, :rects, :grid, :polygons, :axis, :threshold, :roi, :view. geometry's layout depends on it:\n:circles — flat Real[], (cx, cy, r) per element (image px)\n:rects — flat Real[], (cx, cy, w, h) per element (image px)\n:polyline / :segments — flat Real[], (x, y) per vertex — one connected path hit per segment, or disjoint pairs, respectively (image px)\n:lines — Vector{Real}[], one flat (x, y)-per-vertex polyline per element (image px; NaN is a gap inside that line, not another element). A lines! / stairs! / scatterlines! line is one entry; a series! is one entry per series\n:polygons — Vector{Real}[], one flat (x, y)-per-vertex ring per element (image px)\n:grid — a Dict with \"xedges\", \"yedges\", \"ncols\", \"nrows\", optional \"values\"\n:axis — nothing (whole-axis readout, AxisInteractable) or flat Real[x, y, w, h] (the colorbar's pixel bbox, ColorbarInteractable); not element-indexed\n:threshold / :roi / :view — a small Dict (orientation/position, drag bbox + hit half-size handle — the overlay paints the grip at a fixed 7 CSS px — or viewport + camera, respectively); not element-indexed\npayloads::Vector{Any} — one JSON-serializable entry per element, positional (payloads[k] binds element k); empty for the element-count-free kinds above.\naxis::Symbol — the id of this layer's AxisTransform in InteractionContext.transforms (see axis_id).\nevents::Tuple — the pointer events this layer responds to (:click, :hover, :drag).\nlabel::Union{Nothing,String} — an optional announcement prefix for screen readers (e.g. \"Scatter\"), used by the keyboard-navigation overlay (\"Scatter, element 3 of 10: …\"). Not to be confused with a label payload key (e.g. (; label = \"a\") in the examples below) — that's per-element tooltip data; this is one string per layer. nothing (default) omits it from the manifest.\ncolors — an optional tooltip accent colour for this layer's elements: a single CSS colour string (uniform across the layer), or (; palette, index) with palette::Vector{String} (CSS colours) and one 0-based index per element into it (colormapped/categorical data). nothing (default) omits it from the manifest — no accent border on the tooltip. Built by PointInteractable's plot-object constructor when the source plot's colour is resolvable; not derived automatically for a bare-points/vertices interactable.\nlinks — an optional Vector{Vector{Symbol}}, one entry per element: other layers this element highlights on hover/click (e.g. a legend entry linking to the trace(s) it labels). Each id is a layer id (every element of that layer) or id:k pinning element k (1-based) of that layer — auto-extracted series! legend entries use the pin so each swatch lights one trace. nothing (default) omits it from the manifest. Built by LegendInteractable; every id it names must belong to another layer in the same masque() call whose kind supports pre-highlight (build_manifest raises ArgumentError otherwise, for explicitly-given targets — the auto-extracted path drops an unsupported target with a @warn instead of failing the whole build). An id:k pin whose k is out of range is the same kind of error as an unknown layer.\n\n\n\n\n\n","title":"Masque.HitLayer"},{"category":"type","location":"api/#Masque.InteractionContext","page":"API Reference","text":"InteractionContext\n\nBackend-produced bridge handed to every interactable. project is the backend's data→image-px closure (so projection is not hard-wired to Makie). transforms are serialized to JS; ids maps an axis object to its transform id.\n\n\n\n\n\n","title":"Masque.InteractionContext"},{"category":"type","location":"api/#Masque.AxisTransform","page":"API Reference","text":"AxisTransform\n\nOne axis expressed declaratively, in the artifact's pixel space, so JS can invert pixels↔data for AxisInteractable and live hover-coordinate readout.\n\n\n\n\n\n","title":"Masque.AxisTransform"},{"category":"function","location":"api/#Masque.data_to_image_px","page":"API Reference","text":"data_to_image_px(ctx::InteractionContext, ax, p) -> Point2f\n\nProject a data-space point p (a 2- or 3-element point/tuple) on axis ax to image-pixel coordinates (top-left origin, y-down), applying ax's transform and camera the same way Makie renders it. The one coordinate primitive every AbstractInteractable's hitlayers method calls — never re-derive projection by hand. ax must be a Makie.Axis, Makie.Axis3, or Makie.PolarAxis that is part of the figure masque rendered (this function does not itself validate that — a mismatched ax silently projects against the wrong scene; use axis_id(ctx, ax) if you need the fail-loud registration check).\n\nExamples\n\nq = data_to_image_px(ctx, ax, (1.0, 2.0))   # -> Point2f in image px\n\n\n\n\n\n","title":"Masque.data_to_image_px"},{"category":"function","location":"api/#Masque.hitlayers","page":"API Reference","text":"hitlayers(interactable, ctx::InteractionContext) -> Vector{HitLayer}\n\nBuild the HitLayer(s) an interactable contributes to the manifest — the only method every AbstractInteractable subtype must implement. Project data-space geometry with data_to_image_px(ctx, ax, point); never re-derive projection. Most built-ins return a single HitLayer; RegionInteractable can return several (one per region kind), and FunctionInteractable delegates entirely to a user function of ctx.\n\n\n\n\n\n","title":"Masque.hitlayers"},{"category":"type","location":"api/#Masque.AbstractBackend","page":"API Reference","text":"AbstractBackend\n\nSupertype for a Masque rendering backend. Concrete backends live in package extensions — CairoBackend (static image, from the CairoMakie extension) and WebGLBackend (live browser-GPU canvas, from the WGLMakie extension) — and implement render, context, _ppu, and make_widget. masque resolves one automatically from whichever extension is loaded, or takes one explicitly via its backend= keyword.\n\n\n\n\n\n","title":"Masque.AbstractBackend"},{"category":"section","location":"troubleshooting/#Troubleshooting","page":"Troubleshooting","text":"","title":"Troubleshooting"},{"category":"section","location":"troubleshooting/#Errors-from-masque()-/-interactable-constructors","page":"Troubleshooting","text":"","title":"Errors from masque() / interactable constructors"},{"category":"section","location":"troubleshooting/#\"masque(fig)-needs-a-rendering-backend-loaded\"","page":"Troubleshooting","text":"Cause: neither CairoMakie nor WGLMakie is loaded in the session. Fix: add using CairoMakie (static, the default) or using WGLMakie (live, animation / large data / 3D) before calling masque.","title":"\"masque(fig) needs a rendering backend loaded\""},{"category":"section","location":"troubleshooting/#\"tooltip-true-is-not-meaningful\"","page":"Troubleshooting","text":"Cause: an interactable was built with tooltip = true. Fix: omit tooltip entirely for the auto name/value table (the default), pass masque\"...\" for a template, or tooltip = false to suppress it. true isn't one of the three valid forms — see Tooltips.","title":"\"tooltip = true is not meaningful\""},{"category":"section","location":"troubleshooting/#\"got-N-payloads-for-M-elements\"-/-\"payloads-must-match-points\"","page":"Troubleshooting","text":"Cause: payloads doesn't have one entry per geometry element. Most constructors report this as \"got N payloads for M elements\"; PointInteractable has its own check and says \"payloads must match points\" instead — same underlying mistake, different wording. Fix: match lengths 1:1, or omit payloads to get the auto-generated default.","title":"\"got N payloads for M elements\" / \"payloads must match points\""},{"category":"section","location":"troubleshooting/#\"mode-must-be-:polyline-or-:pairs\"","page":"Troubleshooting","text":"Cause: SegmentInteractable(...; mode = ...) got something other than the two valid symbols. Fix: use :polyline (connected path) or :pairs (disjoint segment pairs).","title":"\"mode must be :polyline or :pairs\""},{"category":"section","location":"troubleshooting/#\"unit:line-applies-only-to-mode:polyline\"","page":"Troubleshooting","text":"Cause: SegmentInteractable(...; unit = :line) was combined with mode = :pairs (or any mode other than :polyline). Fix: drop unit (the default :segment is one element per edge or pair) or keep mode = :polyline. lines! / stairs! / series! already pass unit = :line.","title":"\"unit=:line applies only to mode=:polyline\""},{"category":"section","location":"troubleshooting/#\"orientation-must-be-:horizontal-or-:vertical\"","page":"Troubleshooting","text":"Cause: same shape of mistake, for ThresholdInteractable(...; orientation = ...). Fix: use :horizontal (constant-y, dragged vertically) or :vertical.","title":"\"orientation must be :horizontal or :vertical\""},{"category":"section","location":"troubleshooting/#\"ROIInteractable:-bounds-must-be-(xmin,-xmax,-ymin,-ymax)\"-/-\"need-xmin-xmax-and-ymin-ymax\"","page":"Troubleshooting","text":"Cause: bounds isn't a 4-tuple, or the min/max order is backwards. Fix: pass bounds = (xmin, xmax, ymin, ymax) with xmin < xmax and ymin < ymax.","title":"\"ROIInteractable: bounds must be (xmin, xmax, ymin, ymax)\" / \"need xmin < xmax and ymin < ymax\""},{"category":"section","location":"troubleshooting/#\"regions/payloads-length-mismatch\"-/-\"unknown-region-kind\"","page":"Troubleshooting","text":"Cause: RegionInteractable's regions and payloads don't line up 1:1, or a region tuple's first element isn't :circle, :rect, or :polygon. Fix: check the region tuple shapes against Custom interactions.","title":"\"regions/payloads length mismatch\" / \"unknown region kind\""},{"category":"section","location":"troubleshooting/#A-scale-related-ArgumentError-from-AxisInteractable/ColorbarInteractable/ThresholdInteractable/ROIInteractable/ViewInteractable","page":"Troubleshooting","text":"Cause: these interactables invert a pixel back to a data value in the browser, which only works for identity, log10, or log axis scales. Any other Makie scale (Makie.pseudolog10, Makie.Symlog10, a custom ReversibleScale, …) fails loud at masque() time instead of silently reporting the wrong coordinate. The exact wording differs per kind — AxisInteractable/ColorbarInteractable say \"... is not invertible client-side\"; ThresholdInteractable/ROIInteractable/ViewInteractable say \"needs (a) client-side invertible ... scale(s)\". Fix: switch the axis to one of the supported scales, or use an element interactable (PointInteractable, SegmentInteractable, …) instead of a continuous-readout one.","title":"A scale-related ArgumentError from AxisInteractable/ColorbarInteractable/ThresholdInteractable/ROIInteractable/ViewInteractable"},{"category":"section","location":"troubleshooting/#\"continuous-pixeldata-readout-is-undefined-on-an-Axis3\"-/-\"on-PolarAxis\"","page":"Troubleshooting","text":"Cause: AxisInteractable/ThresholdInteractable/ROIInteractable/orbit ViewInteractable need a 2D pixel→data inverse; a 3D axis has none (a screen pixel is a ray) and PolarAxis continuous θ/r inversion isn't shipped yet. Fix: use element interactables (points/segments/polygons) on 3D or polar axes instead.","title":"\"continuous pixel→data readout is undefined on an Axis3\" / \"on PolarAxis\""},{"category":"section","location":"troubleshooting/#\"bounds-need-continuous-axes\"-/-\"pan-needs-continuous-numeric-axes\"","page":"Troubleshooting","text":"Cause: ROIInteractable/ViewInteractable pan needs numeric axis limits; a categorical axis has none. Fix: use AxisInteractable (reads the category) instead.","title":"\"bounds need continuous axes\" / \"pan needs continuous numeric axes\""},{"category":"section","location":"troubleshooting/#\"Masque's-CairoMakie-backend-supports-Makie.Axis,-Makie.Axis3,-and-Makie.PolarAxis\"-(LScene)","page":"Troubleshooting","text":"Cause: the figure contains an LScene block, which the :cairo backend refuses to render at all rather than silently dropping (this is Masque's own scoping guard, not a CairoMakie limitation). Fix: restart the session with using WGLMakie instead of CairoMakie — the :webgl backend renders LScene live, just without a hit-testing overlay for it (Masque builds no overlay for LScene on either backend; :cairo refuses to render the figure at all rather than silently drop it).","title":"\"Masque's CairoMakie backend supports Makie.Axis, Makie.Axis3, and Makie.PolarAxis\" (LScene)"},{"category":"section","location":"troubleshooting/#ArgumentError-from-selected-...","page":"Troubleshooting","text":"Cause: either the layer id doesn't support pre-highlight (grid, axis, threshold, roi, view layers can't be pre-highlighted — only circles/rects/polygons/ segments/polyline can) or an index is out of range for that layer. Fix: check the layer's kind against the supported list in Selection; indices are 1-based and must be in 1:n for that layer.","title":"ArgumentError from selected = ..."},{"category":"section","location":"troubleshooting/#An-error-mentioning-\"Makie-internals-changed?\"","page":"Troubleshooting","text":"Cause: an internal-invariant guard tripped — Masque introspects a live Makie plot object's internal fields, and a Makie/CairoMakie/WGLMakie version bump can move or rename one of them. This is not user misuse. Fix: file an issue with your Makie/CairoMakie/WGLMakie versions; pin to a known-good version in the meantime.","title":"An error mentioning \"Makie internals changed?\""},{"category":"section","location":"troubleshooting/#Not-errors,-but-surprising","page":"Troubleshooting","text":"","title":"Not errors, but surprising"},{"category":"section","location":"troubleshooting/#Neither-backend-loaded,-or-both-loaded","page":"Troubleshooting","text":"Loading neither CairoMakie nor WGLMakie raises the ArgumentError above; loading both is fine. See Choosing between them on the Backends page for the exact rule.","title":"Neither backend loaded, or both loaded"},{"category":"section","location":"troubleshooting/#\"Cyclic-references\"-from-Pluto","page":"Troubleshooting","text":"Cause: you fed a widget's own bond value into that same masque(...) call's selected= in one cell. Pluto detects the self-reference and refuses to run the cell. Fix: you don't need to. A click already updates the selection on its own, so selected= is only for a starting value your Julia code computes — and that has to come from a cell that doesn't read this widget's bond. See Persisting a selection across re-renders in Selection.","title":"\"Cyclic references\" from Pluto"},{"category":"section","location":"troubleshooting/#Nothing-happens-when-I-click","page":"Troubleshooting","text":"Check, in order: (1) is the bond actually read somewhere (@bind ev masque(...) with no cell reading ev looks like nothing happened); (2) did you click empty space — clicks that don't land on a hit region are a no-op, by design; (3) is the element you clicked actually interactive — an unsupported plot type in masque(fig) is skipped with a @warn in the notebook log, not an error, so it silently isn't clickable.","title":"Nothing happens when I click"},{"category":"section","location":"troubleshooting/#Tooltip-shows-[object-Object]","page":"Troubleshooting","text":"Cause: the auto-table (or a $(field) template) is rendering a payload value that is itself a nested Dict/array, not a scalar — this stringifies to [object Object] in JavaScript. Fix: flatten the field to a scalar (string/number) in Julia before it goes into payloads, or reference a pre-formatted string field with masque\"$(that_field)\" instead of the raw nested value.","title":"Tooltip shows [object Object]"},{"category":"section","location":"troubleshooting/#The-overlay-is-misaligned-with-the-figure","page":"Troubleshooting","text":"Cause: almost always a px_per_unit/max_width mismatch between what was rendered and what the user's display shows. Hit-testing itself re-reads the element's actual on-screen size at runtime, so page zoom or window resizing after the widget mounted is not the cause. Fix: re-run the cell that calls masque(...); if the misalignment persists, check that max_width on masque/the explicit backend struct matches the column width you expect.","title":"The overlay is misaligned with the figure"},{"category":"section","location":"troubleshooting/#Browser-console-errors-to-look-for","page":"Troubleshooting","text":"Open the browser devtools console. A serialization error there on :webgl usually means the installed WGLMakie version is outside Masque's pinned compat range — see Backends caveats. Any other console error alongside a widget that otherwise renders is worth reporting as a bug rather than assuming it's expected.","title":"Browser console errors to look for"},{"category":"section","location":"troubleshooting/#WGLMakie-canvas-is-blank","page":"Troubleshooting","text":"Cause: typically a version mismatch between the installed WGLMakie and the one Masque's :webgl extension was verified against, or the figure has no supported plot for masque(fig) to introspect (zero-config on an empty Axis3 renders the canvas but nothing is interactive — that's expected, not a bug). Fix: check the WGLMakie compat bound in Project.toml; confirm the figure actually has a plot call in it before masque(fig).","title":"WGLMakie canvas is blank"},{"category":"section","location":"troubleshooting/#A-WebGL-plot-says-its-GPU-context-was-released","page":"Troubleshooting","text":"Cause: the browser caps how many WebGL contexts a page can keep (16 on desktop Chrome and Safari, 8 on Android Chrome). Masque stays at 8 and releases an off-screen plot before the browser does. More than 8 plots on screen at once cannot all be live; the extras show this note. Hover and @bind still work. Fix: scroll so fewer :webgl plots are on screen at once, or use :cairo for a plot that is a static picture.","title":"A WebGL plot says its GPU context was released"},{"category":"section","location":"tooltips/#Tooltips","page":"Tooltips","text":"Hovering an element shows a tooltip — purely client-side, no Julia round-trip. The tooltip keyword is accepted by the element-kind constructors — PointInteractable, SegmentInteractable, RectInteractable, PolygonInteractable, TextInteractable, RegionInteractable — but not by the whole-axis or drag kinds (AxisInteractable, ColorbarInteractable, ThresholdInteractable, ROIInteractable, ViewInteractable) or by FunctionInteractable; see Interactables for the full breakdown. Its type governs what the browser renders:\n\nValue Type Browser behaviour\n(omitted / nothing) Nothing Auto name/value table built from the payload\nmasque\"...\" Markup Template interpolated against the hovered element's payload\nfalse Bool Tooltip suppressed entirely — the hover highlight still applies\n\nA legend is the exception to the omitted-tooltip row: hovering an entry shows no card unless you pass masque\"...\". See Legend.","title":"Tooltips"},{"category":"section","location":"tooltips/#The-default:-an-auto-table","page":"Tooltips","text":"When tooltip isn't set, the browser renders a name/value table straight from the element's payload — every field name and value, HTML-escaped:\n\nPointInteractable(ax, pts; payloads = [(; city = \"Lyon\", pop = 513_000)])\n# hovering shows a two-row table: city → Lyon, pop → 513000\n\nPointInteractable(ax, pts; payloads = [...], tooltip = false)","title":"The default: an auto-table"},{"category":"section","location":"tooltips/#masque\"...\"-templates","page":"Tooltips","text":"masque\"...\" is a string macro (exported; the underlying macro is @masque_str) that produces a Markup value:\n\ntooltip = masque\"<b>$(name)</b> — $(population:,) people\"\n\n$(field) is a placeholder resolved in the browser from the hovered element's payload entry at hover time — it does not read a Julia variable, and there is no Julia-object interpolation in templates.\n\nSyntax Meaning\n$(field) Value of payload field field; HTML-escaped\n$(field:spec) Same, formatted by a d3-format spec before escaping\n Literal dollar sign\n\nA fixed label with no placeholders is a template with no $() at all: masque\"<em>static label</em>\". The literal (non-$()) portions of the template are raw HTML — you're responsible for escaping < and & in literal text, same as @htl; $(field) interpolation and the auto-table are always HTML-escaped for you.\n\nBecause masque\"...\" requires a string literal, a runtime-computed string has to travel as a field inside the payload instead:\n\nbegin\n    payloads = [(; city, pop, label = \"$(city): $(pop) residents\") for (city, pop) in data]\n    tooltip  = masque\"$(label)\"   # label is pre-rendered per element in the payload\nend","title":"masque\"...\" templates"},{"category":"section","location":"tooltips/#Field-check","page":"Tooltips","text":"Two checks catch typos at different times. A malformed template ($(pop+1), an unclosed $(, an unknown d3-format type character) is a TemplateValidationError the instant the cell containing masque\"...\" parses — before masque() ever runs. A syntactically valid field that isn't actually a key in your payload is caught later, when masque() builds the manifest: an ArgumentError with a \"did you mean?\" suggestion for a close misspelling. This check runs whenever any payload in the layer is a NamedTuple — checked against the union of their field names — and is skipped only when none are (e.g. every payload is a Dict), in which case a missing $(field) just renders empty at hover instead.","title":"Field check"},{"category":"section","location":"tooltips/#Styling","page":"Tooltips","text":"","title":"Styling"},{"category":"section","location":"tooltips/#Dark-mode-follows-the-figure","page":"Tooltips","text":"The built-in tooltip's light/dark theme is derived from the figure's own background colour — not just the OS/browser prefers-color-scheme signal (the one stock Pluto uses for its own theme; official Pluto has no in-app light/dark toggle). No author action needed: a Figure(backgroundcolor = :gray12) on an otherwise-light Pluto page gets a dark tooltip, and a default white figure on a dark OS gets a light one — the tooltip matches the plot it's attached to.\n\nThis uses CSS relative-colour syntax (lch(from …)); browsers without it (older than ~2023) fall back to the previous behaviour — a static light tooltip, dark only via OS prefers-color-scheme — automatically, no author action needed either way.","title":"Dark mode follows the figure"},{"category":"section","location":"tooltips/#Accent-colour","page":"Tooltips","text":"When Masque can resolve the hovered element's own colour (currently: a scatter! plot's color=, uniform or colormapped), the tooltip gets a 3px accent border in that colour — the tooltip text itself stays neutral. Nothing to opt into; it's omitted (a plain 1px border, same as the other three sides) whenever the colour can't be resolved.","title":"Accent colour"},{"category":"section","location":"tooltips/#Figure-level-overrides","page":"Tooltips","text":"Pin any of these to lock the tooltip's look (this also opts that property out of dark-mode inversion — the author's deliberate choice):\n\nmasque(fig, interactables...;\n    tooltip_bg        = nothing,   # background  — CSS string or Makie color (:dodgerblue, RGBf(...))\n    tooltip_color     = nothing,   # text color  — CSS string or Makie color\n    tooltip_accent    = nothing,   # accent (emphasis / links)\n    tooltip_font      = nothing,   # font-family — String\n    tooltip_font_size = nothing,   # Real → appended with \"px\"\n    tooltip_radius    = nothing,   # Real → appended with \"px\"\n    tooltip_caret     = true,      # Bool — draw the caret pointing at the hovered element\n)\n\nnothing (the default for every kwarg except tooltip_caret) means \"use the built-in default\"; only the kwargs you actually set change anything.","title":"Figure-level overrides"},{"category":"section","location":"tooltips/#CSS-escape-hatch","page":"Tooltips","text":"The underlying --masque-tip-* custom properties inherit like any CSS custom property, so setting one on any ancestor of the cell overrides it without any Julia API:\n\n<style>\nmain { --masque-tip-bg: #1a1a2e; --masque-tip-color: #e0e0e0; }\n</style>\n\nA few properties are CSS-only — no Julia kwarg sets them, so this is the only way to change them. --masque-tip-bg/--masque-tip-color/--masque-tip-border no longer have one fixed light/dark pair of defaults — they're derived from the figure's background (see above) unless set explicitly here or via a tooltip_* kwarg; the \"Legacy fallback\" column is what a browser without CSS relative-colour syntax uses instead (static light, dark only via OS prefers-color-scheme):\n\nCustom property Legacy fallback (light / dark) Julia kwarg\n--masque-tip-bg #ffffff / #1e1e1e tooltip_bg\n--masque-tip-color #1a1a1a / #e8e8e8 tooltip_color\n--masque-tip-border rgba(0,0,0,0.1) / rgba(255,255,255,0.15) — (CSS only)\n--masque-tip-accent #6b7280 tooltip_accent\n--masque-tip-font system-ui, -apple-system, sans-serif tooltip_font\n--masque-tip-font-size 11px tooltip_font_size\n--masque-tip-radius 4px tooltip_radius\n--masque-tip-caret block (the caret's display) tooltip_caret (false → none)\n--masque-tip-padding 8px 12px — (CSS only)\n--masque-tip-shadow 0 2px 4px rgba(0,0,0,0.12), 0 8px 16px rgba(0,0,0,0.08) / 0 2px 4px rgba(0,0,0,0.4), 0 8px 16px rgba(0,0,0,0.3) — (CSS only)\n--masque-tip-maxwidth 320px — (CSS only)\n--masque-mark-border (unset — plain 1px border) — (set automatically from the hovered element's colors, see \"Accent colour\" above)","title":"CSS escape hatch"},{"category":"section","location":"tooltips/#Placement","page":"Tooltips","text":"The tooltip is anchored to the hovered mark, not the cursor: it's centred horizontally on the mark and placed above it with a small gap, so it doesn't sit under the pointer or obscure neighboring marks. The anchor point depends on the kind:\n\ncircles: the marker's centre; the box sits above its top edge.\nrects (bars): the bar's top-centre.\nsegments, per-segment polylines, and whole lines (lines! / series!): the point on the path nearest the cursor — the tooltip slides along the line as the pointer moves. Keyboard focus (no cursor) uses the segment's midpoint, or the point halfway along a whole line.\npolygons: the centroid, if it lies inside the polygon shape; otherwise the cursor point.\ngrid cells (heatmaps): the cell's centre; the box sits above the cell's top edge.\naxis, threshold, ROI, and view (continuous/drag interactions with no discrete mark): the cursor position, as before.\n\nIf the box would clip the surface's top edge, it flips to sit below the mark instead. If it would clip a side, it shifts to stay inside the surface and the caret moves with it, so the caret always points at the anchor even when the box itself isn't centred on it.","title":"Placement"},{"category":"section","location":"tooltips/#Caret","page":"Tooltips","text":"When tooltip_caret = true (the default), a small triangle points from the tooltip toward the anchor described above. For the mark-anchored kinds the caret sits at the box's bottom centre (or top centre, when flipped below); for the cursor-following kinds (axis/threshold/ ROI/view) it keeps the previous cursor-relative offset and edge-clamping behavior.\n\nSee §10 for the wire format behind all of this.","title":"Caret"},{"category":"section","location":"backends/#Backends","page":"Backends","text":"Masque has two backends. The interaction contract — masque, @bind, InteractionEvent, every interactable — is identical on both. The cost profile is not.","title":"Backends"},{"category":"section","location":"backends/#:cairo-(the-default)","page":"Backends","text":"using CairoMakie selects it. CairoMakie renders the figure to a PNG once; Masque computes a hit-region manifest in Julia and ships both to the browser. A small TypeScript overlay (assets/overlay.js) sits over the static image, hit-tests pointer events against the manifest, draws highlights/tooltips locally, and only sends a deliberate click back through @bind. Cost-wise: one render per masque() call, independent of how many elements are interactive — cheap for a plot you build once and let the user hover/click, expensive if you're re-rendering on every animation frame (each frame re-rasterizes the whole scene).\n\nBecause the image and manifest are both embedded in the output HTML, the inspection layer keeps working in an exported, offline static HTML — hover and tooltips still work with no Julia kernel at all. Only a click that should trigger a Julia recompute needs a live kernel; without one, the click is inert (nothing to @bind to).","title":":cairo (the default)"},{"category":"section","location":"backends/#:webgl-(WGLMakie)","page":"Backends","text":"Status: experimental / incubating. Verified end-to-end in a real Pluto notebook (render, server-free delivery, overlay, and the @bind round-trip).\n\nusing WGLMakie instead of CairoMakie switches masque to a browser-GPU backend: the figure renders live in a WebGL <canvas> on the client GPU, with Masque's usual overlay layered on top — no code changes beyond the using line:\n\nbegin\n    using Masque, WGLMakie\n    x, y, z = randn(200), randn(200), randn(200)\n    fig = Figure()\n    ax = Axis3(fig[1, 1])\n    scatter!(ax, x, y, z)\nend\n\n@bind ev masque(fig)   # a live WebGL canvas + Masque's overlay; ev is an InteractionEvent on click\n\nReach for :webgl for 3D you want to actually rotate live, animation / frequent re-renders (WebGL redraws a frame, :cairo re-rasterizes a whole PNG), and large or live-updating data where per-frame render cost dominates. For everything else — a figure you build once and let the user inspect — :cairo's static PNG is lighter (both backends' static-export behavior is verified; see below).\n\nexamples/webgl_demo.jl is a runnable gallery of the :webgl backend (CI runs it headlessly, same as demo.jl). For the numeric cost model (bundle size, per-frame payload, click latency at scale), see docs/dev/perf-findings.md and docs/dev/backend-comparison.md — this page describes the cost model in words, not numbers, so it can't drift out of sync with the measured ones.","title":":webgl (WGLMakie)"},{"category":"section","location":"backends/#What-exported-static-HTML-keeps-and-loses","page":"Backends","text":"On either backend, a Pluto notebook exported to static HTML keeps hover and tooltip inspection: the hit-test manifest is baked into the exported page, and so is the base — the PNG on :cairo, and on :webgl the serialized scene, which Masque's shim redraws on the reader's GPU without a server (both verified end-to-end against real Pluto exports; the page still loads Pluto's own frontend from a CDN). What's lost on either backend is anything that needs Julia to recompute: a click that should update ev and re-run downstream cells does nothing without a live kernel behind it.","title":"What exported static HTML keeps and loses"},{"category":"section","location":"backends/#Choosing-between-them","page":"Backends","text":"Masque resolves the backend from which extension is loaded — a missing using line raises an ArgumentError the first time masque runs; loading both CairoMakie and WGLMakie is fine, and then backend= picks one explicitly while an unqualified masque(fig) defaults to :cairo (so a Cairo-baked sysimage isn't blocked by a stray using WGLMakie).\n\nmax_width — the display width to target (Pluto's column, in px; CairoMakie renders at roughly 2 × max_width for a crisp static image) — is a keyword on masque itself, on both backends:\n\nmasque(fig, interactables; max_width = 900)\n\nCairoBackend/WebGLBackend are the two concrete backend types backend= actually takes, but neither is exported from Masque — they live in Masque's package extensions (ext/MasqueCairoMakieExt.jl, ext/MasqueWGLMakieExt.jl), reachable only via Base.get_extension, which is how masque itself resolves them internally. You only need this for a WebGL-only knob masque doesn't expose directly, px_per_unit (the canvas's device-pixel ratio — raise it for a sharper :webgl canvas at a proportional GPU/bandwidth cost):\n\nmasque(fig, interactables; backend = Base.get_extension(Masque, :MasqueWGLMakieExt).WebGLBackend(; px_per_unit = 3.0))\n\nBecause they're unexported extension types, CairoBackend/WebGLBackend aren't in the API Reference's @docs blocks — see the note there.","title":"Choosing between them"},{"category":"section","location":"backends/#Caveats","page":"Backends","text":"Version-coupled to WGLMakie's internals (serialize_scene shape, setup_scene_init signature). Pinned to a specific WGLMakie compat range; treat a WGLMakie version bump as a re-verification, not an automatic upgrade.\nThe WGLMakie JS bundle ships once per notebook, so each additional masque(fig) cell's own cost is just its own scene, not another copy of the bundle.\nAt most 8 :webgl plots hold a live WebGL context. Desktop Chrome and Safari allow 16, Android Chrome allows 8, and Firefox allows several hundred; 8 fits all of them and leaves room for another tab. A plot outside the viewport is not given a context until it scrolls into view. If more than 8 are on screen together, the extras show a note instead of the browser blanking an arbitrary canvas. :cairo does not use a WebGL context.","title":"Caveats"},{"category":"section","location":"selection/#Selection","page":"Selection","text":"","title":"Selection"},{"category":"section","location":"selection/#Reacting-to-a-click","page":"Selection","text":"There is exactly one selection. A masque(...) bond reports it directly: nothing when nothing is selected, otherwise one event whose type matches the interactable. A point, bar, polygon, or segment click is an ElementEvent: layer is the interactable's id, index is 1-based, and the row's fields are read on the event (ev.city). Clicking an element replaces the selection with that element, and a cell that reads the bond re-runs on every click:\n\n@bind ev masque(fig, PointInteractable(ax, pts; id = :scatter))\n\nev === nothing ? \"nothing selected\" : \"clicked #$(ev.index) in :$(ev.layer)\"","title":"Reacting to a click"},{"category":"section","location":"selection/#Linked-selection-across-plots","page":"Selection","text":"layer and index are plain data, and an ElementEvent is a row index, so one click can drive any number of downstream cells — filter a table, highlight a second plot, recompute a fit:\n\nrow = ev === nothing ? nothing : data[ev, :]","title":"Linked selection across plots"},{"category":"section","location":"selection/#selected-—-the-selection's-starting-value","page":"Selection","text":"selected= is nothing more than the selection's initial value. Pass it to any masque(...) call to say what's selected the moment the widget mounts, before any click:\n\nmasque(fig, PointInteractable(ax, pts; id = :scatter); selected = 2)\n\nOn a point layer, selected = 2 and selected = [2] mount as the same one ElementEvent a click on that point would produce. selected = [1, 3] highlights both marks and leaves the bond nothing: this interaction returns one element, so a set is not a value it can hold. The next click replaces the highlight with that one element.\n\nA selects ROI is the interaction that returns a vector. There, selected = 1 and selected = [1] both mount as a one-element Vector{ElementEvent}, and selected = [1, 3] mounts as those two. An explicit empty vector (selected = Int[]) mounts as ElementEvent[]. Omitting selected= leaves the bond nothing.\n\nIndices are 1-based. 0 is out of range. With one seedable layer a bare Int or AbstractVector{<:Integer} is enough. Two seedable layers need a name: selected = Dict(:scatter => [1, 3]) or selected = (; scatter = 1). A bare 1 across two layers throws, naming the layers. Unknown keys throw. Supported kinds: circles / rects / polygons (selected wash) and segments / polyline / lines (selected ring; a lines element is the whole path). Unsupported kinds (grid, axis, …) or out-of-range indices throw ArgumentError at build time — fail loud, like a wrong-length payloads=. Keys are layer ids: for the single-layer kinds that's the interactable's id, but RegionInteractable splits into suffixed layers (:id_c circles / :id_r rects / :id_p polygons) — key on those.\n\nThat kind list constrains hydration, not the gesture: clicking a heatmap/image cell selects that cell even though :grid can't be hydrated via selected=, and clicking a legend entry selects the traces it links to — every element of a named layer, or the one element an id:k spec pins — not the legend swatch itself (see Legend).","title":"selected= — the selection's starting value"},{"category":"section","location":"selection/#Persisting-a-selection-across-re-renders","page":"Selection","text":"Clicking an element replaces the selection immediately, in the browser — no round trip through Julia, no bond to feed back into anything. Clicking a different element moves it; the selection doesn't accumulate, since the echo mirrors the bond value and a click's bond value is a single ElementEvent. Enter/Space on a keyboard-focused element commits through the same path as a mouse click. A selects-ROIInteractable's release replaces the selection with everything its box enclosed, the same way — see Multi-element selectors below.\n\nThe selection resets when the widget remounts, by design: an index is only meaningful relative to the data that produced it, and a remount means the figure was rebuilt upstream — possibly with elements reordered, added, or removed. Restoring the old selection onto whatever now sits at that index would be silently wrong; dropping it is the honest behavior.\n\nIf you want a selection to survive a rebuild, say so explicitly with selected= (above) — give it the indices you believe still apply in the new figure. That's necessarily something only you, the notebook author, can assert; nothing in the manifest tells Masque whether index 3 in the rebuilt figure is \"the same\" point as index 3 in the old one.\n\nOne thing you have no reason to try: feeding a widget's own bond value back into that same call's selected=,\n\n# DOESN'T WORK — ev and masque(...; selected=...) are in the same cell, feeding each other\n@bind ev masque(fig, PointInteractable(ax, pts; id = :scatter); selected = Dict(:scatter => [ev.index]))\n\nis a Pluto reactive cycle: Pluto detects it and reports \"Cyclic references\" instead of running the cell. A click already updates the selection — and the bond that reports it — on its own, so this pattern buys you nothing.\n\nA click selects a single element, last pick wins — it's not built for accumulating a growing set across many clicks. For that, see examples/demo.jl's \"Selection round-trip\" cells: a persistent Ref accumulates clicked indices across reruns and feeds the growing set to a second figure's selected=. That's a different job from the selection above — accumulation instead of last-pick-wins — and it remains the right tool for it.","title":"Persisting a selection across re-renders"},{"category":"section","location":"selection/#Multi-element-selectors","page":"Selection","text":"ROIInteractable is an AbstractSelector: pair it with a selects = :scatter keyword pointing at another layer, and its drag box selects every element of :scatter it currently encloses, rather than the single {layer, index} an ordinary click reports. selects only works when the target layer is a circles or grid kind — i.e. built from PointInteractable or the grid form of RectInteractable — pointing it at any other kind fails loud. Over points the bond is a Vector{ElementEvent} — one entry per enclosed point, including a click on one of those points. Over a grid the bond is one GridWindowEvent, and A[win] is A[win.i1:win.i2, win.j1:win.j2]. A box that misses the grid has an empty i1:i2.\n\nbegin\n    scatter!(ax, first.(pts), last.(pts))\n    roi = ROIInteractable(ax; bounds = (0.0, 10.0, 0.0, 10.0), selects = :scatter)\nend\n\n@bind picked masque(fig, [PointInteractable(ax, pts; id = :scatter), roi])\n# picked isa Vector{ElementEvent} once you release a drag, or click one point\n\nReleasing the drag replaces the selection with every enclosed element, the same click-echo as an ordinary click — that Vector is \"the current bond value\" in the same sense a single click's InteractionEvent is. It replaces whatever was selected before, selected= hydration included: an ROI release always decides what's highlighted afterward, not a mix of the old selection and the new one.\n\nSee gallery/gallery.jl's \"Box-select scatter\" recipe for the full worked example. If you're building a custom interaction that should report more than one element per event the same way, see AbstractSelector on the API Reference page for the extension point.","title":"Multi-element selectors"},{"category":"section","location":"contributing/#Development","page":"Development","text":"The :cairo overlay is TypeScript, bundled to a committed assets/overlay.js:\n\ncd frontend\nnpm ci\nnpm run lint && npm run typecheck && npm test   # gate\nnpm run build                                    # → ../assets/overlay.js\n\nThe :webgl shim is a second, separate TypeScript project, bundled to a committed assets/masque-webgl.js:\n\ncd frontend-webgl\nnpm ci\nnpm run lint && npm run typecheck && npm test   # gate\nnpm run build                                    # → ../assets/masque-webgl.js\n\nCI is the source of truth for both bundles (it rebuilds and commits on main), so committing your local build is optional. Julia tests are split into three GROUPs so each process has a known loaded-backend set (Core = Cairo only; WebGL = WGL first, then both at the end; NoBackend = neither). GROUP defaults to Core:\n\njulia --project=. -e 'using Pkg; Pkg.test()'                 # GROUP=Core\nGROUP=NoBackend julia --project=. -e 'using Pkg; Pkg.test()' # neither backend\nGROUP=WebGL julia --project=. -e 'using Pkg; Pkg.test()'     # WGL then both\n\nJulia code is formatted with Runic (CI enforces it, and checks the whole repo, not just src/test):\n\njulia -e 'using Runic; exit(Runic.main([\"--inplace\", \"src\", \"test\", \"bench\", \"gallery\", \"examples\", \"docs\"]))'\n\nThis site is Documenter, built from docs/src/. Maintainer/design notes live in docs/dev/ and are not part of the Documenter sidebar.\n\njulia --project=docs -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'\njulia --project=docs docs/make.jl\n\nLocal make.jl builds HTML under docs/build/ and skips deploydocs (that runs on CI, which also owns the deploy to GitHub Pages on main and tags).","title":"Development"},{"category":"section","location":"contributing/#Live-verification","page":"Development","text":"Unit and frontend tests check the manifest and the JS in isolation; they don't prove the rendered widget behaves for a user. Any change that can alter what a user interacts with or sees — including a Julia-only change to the manifest shape, hit-test geometry, or hover text — needs to be checked live in a real Pluto + browser, on every supported backend, for every interactable kind it touches. This isn't optional polish; it's the actual gate before a user-facing change is done. The full playbook is docs/dev/live-interaction-checklist.md.","title":"Live verification"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"Clone the repo, start Pluto, and open examples/demo.jl from its landing page:\n\ngit clone https://github.com/jowch/Masque.jl\njulia -e 'using Pluto; Pluto.run()'\n\nEvery notebook below works the same way — each is self-contained (it Pkg.develops the local checkout and adds whatever Makie backend it needs), so it runs from a fresh clone with no setup beyond having Pluto installed. CI runs all of them headlessly on every change (examples/ci_run.jl), so they can't rot out of sync with the package API.\n\nEvery notebook below also has a static export you can open in the browser with no Julia installed (it still fetches Pluto's frontend from a CDN, so it needs network) — hover tooltips, highlights and (for :webgl notebooks) the live canvas all work; only clicks that @bind back to Julia need the notebook running, for which see the clone-and-run instructions above.","title":"Examples"},{"category":"section","location":"examples/#[examples/demo.jl](https://github.com/jowch/Masque.jl/blob/main/examples/demo.jl)","page":"Examples","text":"The main feature tour on :cairo — every built-in interactable kind, masque\"...\" tooltip templates and theming, the selection round-trip, and masque(fig) auto-extraction over bars, areas, polygons, a colorbar, and text labels.\n\nOpen the static export","title":"examples/demo.jl"},{"category":"section","location":"examples/#[examples/webgl_demo.jl](https://github.com/jowch/Masque.jl/blob/main/examples/webgl_demo.jl)","page":"Examples","text":"The same kind of kitchen-sink tour, on :webgl — every overlay path running live on a WebGL canvas instead of a static PNG.\n\nOpen the static export","title":"examples/webgl_demo.jl"},{"category":"section","location":"examples/#[examples/view_manip.jl](https://github.com/jowch/Masque.jl/blob/main/examples/view_manip.jl)","page":"Examples","text":"Pan, zoom, and 3D rotation via a slider using the @bind re-render model: a limits slider, azimuth/elevation sliders for Axis3, and selection surviving a view re-render. ViewInteractable drag (pan / orbit) is separate: it commits nothing, with a live gesture-channel preview on :cairo.\n\nOpen the static export","title":"examples/view_manip.jl"},{"category":"section","location":"examples/#[examples/view_manip_webgl.jl](https://github.com/jowch/Masque.jl/blob/main/examples/view_manip_webgl.jl)","page":"Examples","text":"The drag-to-pan / drag-to-rotate half of view_manip.jl, live-verified on :webgl.\n\nOpen the static export","title":"examples/view_manip_webgl.jl"},{"category":"section","location":"examples/#[examples/polaraxis_webgl.jl](https://github.com/jowch/Masque.jl/blob/main/examples/polaraxis_webgl.jl)","page":"Examples","text":"Discrete point hits on a PolarAxis, on :webgl — hover for a tooltip, click for an @bind event.\n\nOpen the static export","title":"examples/polaraxis_webgl.jl"},{"category":"section","location":"examples/#[gallery/gallery.jl](https://github.com/jowch/Masque.jl/blob/main/gallery/gallery.jl)","page":"Examples","text":"Recipes closer to real applications than the feature tour, built from the same interactables as demo.jl: a box-select scatter plot (drag a ROIInteractable to select every enclosed point — its bond is a Vector{ElementEvent}, one per selected point) and an image ROI with per-channel stats.\n\nOpen the static export","title":"gallery/gallery.jl"},{"category":"section","location":"#Masque.jl","page":"Home","text":"Masque lays a thin, client-side interactive layer over a Makie figure inside a Pluto notebook: hover shows a tooltip, a click round-trips to Julia through @bind. It renders through CairoMakie by default (a static, publication-quality image with a transparent JS overlay doing the hit-testing — no server, no WebGL), or through WGLMakie for a live, browser-GPU canvas when you need animation, large data, or live 3D — same masque/ @bind API either way.","title":"Masque.jl"},{"category":"section","location":"#When-to-use-it","page":"Home","text":" CairoMakie alone WGLMakie alone Masque\nOutput static, publication-quality live, GPU-rendered static + thin overlay (:cairo) or live (:webgl)\nInteractivity none rich (pan/zoom/rotate) light: hover tooltips, click-to-select, drag-to-pan/threshold/ROI\nNeeds a live Julia process no yes only for click → recompute\nSurvives offline / static HTML export yes no yes on both (verified — the inspection layer keeps working; on :webgl the canvas is redrawn client-side, no server needed)\n\nReach for Masque when you want a publication-quality static figure that also answers \"what's this point?\" on hover and \"which one did I click?\" in Julia — without standing up a WebGL scene. Reach for WGLMakie directly (no Masque) when you need free-form camera control, live data updates, or gestures Masque doesn't wire back to Julia (arbitrary free rotation, for example).","title":"When to use it"},{"category":"section","location":"#Install","page":"Home","text":"julia> ] add Masque\n\nYou'll also want Pluto, plus a Makie backend: CairoMakie for the default static path, or WGLMakie for animation / large data / live 3D — see Backends for what happens if you load neither or both.","title":"Install"},{"category":"section","location":"#Quick-start","page":"Home","text":"In a Pluto notebook:\n\nbegin\n    using Masque, CairoMakie\n\n    # your figure, as usual\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    pts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\n    scatter!(ax, first.(pts), last.(pts))\nend\n\n# declare what's interactable, bind the result\n@bind sel masque(fig, [PointInteractable(ax, pts; payloads = [\"a\", \"b\", \"c\"])])\n\n# react to clicks — `sel` is `nothing` until a click, then an ElementEvent\nsel === nothing ? \"click a point\" : \"you picked $(sel.index)\"\n\nHovering shows a tooltip (purely client-side, no Julia round-trip); clicking sets sel and re-runs downstream cells. Clicks on empty space are a no-op.\n\nEach of the three blocks above is a separate Pluto cell — Pluto runs exactly one top-level expression per cell, so any snippet on this site with more than one statement is wrapped in begin ... end (which counts as one expression) or split across cells the way it's shown here. Copy each fenced block into its own cell.\n\nUnder the hood, masque(...) sends the browser a manifest: the rendered image plus hit regions grouped into layers, one per interactable, keyed by its id. The value a @bind-ed variable holds — sel above — is called the bond value.","title":"Quick start"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Getting started — a slower walkthrough: explicit vs. zero-config, choosing a backend, what a bond value looks like\nInteractables — every built-in kind, its constructor, and its default payload\nSelection — reacting to clicks, linking plots, persisting a highlight\nLegend — hover/click a Makie.Legend entry to highlight the trace(s) it labels\nTooltips — masque\"...\" templates and styling\nCustom interactions — RegionInteractable / FunctionInteractable\nBackends — :cairo vs :webgl, and when to reach for which\nTroubleshooting — common errors and what causes them\nExamples — every runnable notebook in the repo\nAPI Reference — full docstrings","title":"Where to go next"},{"category":"section","location":"custom/#Custom-interactions","page":"Custom interactions","text":"For geometry the built-ins don't cover — no JavaScript required. Two tiers, from \"declare shapes\" to \"full control.\"","title":"Custom interactions"},{"category":"section","location":"custom/#[RegionInteractable](@ref)-—-declarative-mixed-regions","page":"Custom interactions","text":"Mixed-kind regions in data space, grouped into one layer per kind. Each region is one of:\n\n(:circle,  (cx, cy), r)            # r in data units\n(:rect,    (cx, cy), w, h)         # w, h in data units\n(:polygon, [(x, y), ...])          # a ring of points\n\nWorked example — three arbitrary shapes with a label each, hoverable and clickable:\n\nbegin\n    using Masque, CairoMakie\n\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    image!(ax, rand(100, 100))   # some backdrop the regions sit over\n\n    regions = [\n        (:circle, (20.0, 20.0), 8.0),\n        (:rect, (60.0, 60.0), 15.0, 10.0),\n        (:polygon, [(30.0, 70.0), (40.0, 90.0), (20.0, 90.0)]),\n    ]\n    payloads = [(; name = \"cell A\"), (; name = \"cell B\"), (; name = \"cell C\")]\nend\n\n@bind ev masque(fig, RegionInteractable(ax; regions, payloads, id = :cells))\n\npayloads must match regions 1:1. tooltip takes the same three forms as any other interactable (nothing / masque\"...\" / false) — see Tooltips. Because RegionInteractable groups by kind, its manifest layers are :cells_c (circles), :cells_r (rects), :cells_p (polygons) — key selected= on those, not on :cells itself.","title":"RegionInteractable — declarative mixed regions"},{"category":"section","location":"custom/#[FunctionInteractable](@ref)-—-full-control","page":"Custom interactions","text":"The escape hatch for a geometry kind none of the above express: f(ctx) -> Vector{HitLayer}. You do the projection yourself with data_to_image_px and emit one or more HitLayers.\n\nWorked example — a triangular hit region (not one of the built-in kinds), reusing the :polygon wire kind RegionInteractable would otherwise produce:\n\nbegin\n    using Masque, CairoMakie\n\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    lines!(ax, [0, 10, 5, 0], [0, 0, 8, 0])   # a triangle drawn by hand\n\n    function triangle_layer(ctx)\n        pts = [(0.0, 0.0), (10.0, 0.0), (5.0, 8.0)]\n        ring = Float64[]   # :polygons geometry is one flat [x1,y1,x2,y2,...] vector per ring\n        for p in pts\n            q = data_to_image_px(ctx, ax, p)\n            push!(ring, q[1], q[2])\n        end\n        HitLayer[\n            HitLayer(:triangle, :polygons, [ring], [(; label = \"the triangle\")],\n                Masque.axis_id(ctx, ax), (:click, :hover)),\n        ]\n    end\nend\n\n@bind ev masque(fig, FunctionInteractable(triangle_layer))\n\nf receives the InteractionContext for the whole figure (the same one built-in interactables use), so it can key geometry to any Axis in fig via Masque.axis_id(ctx, ax) (not exported — qualify it). Use this tier when the shape genuinely isn't a circle/rect/polygon/polyline — for anything expressible as one of those, RegionInteractable is less code.","title":"FunctionInteractable — full control"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"This walks through the same three steps as the Home quick start, slower, and covers the two ways to tell Masque what's clickable.","title":"Getting started"},{"category":"section","location":"getting-started/#1.-A-figure,-like-any-other","page":"Getting started","text":"Nothing Masque-specific yet:\n\nbegin\n    using CairoMakie\n\n    pts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    scatter!(ax, first.(pts), last.(pts))\n    fig\nend","title":"1. A figure, like any other"},{"category":"section","location":"getting-started/#2.-masque(fig)-—-zero-config","page":"Getting started","text":"Load Masque and call masque(fig) instead of just showing fig. Masque walks the figure, finds every plot it knows how to introspect (scatter!, lines!, heatmap!, barplot!, …), and overlays hit-testing for all of them automatically — no interactable to write:\n\nusing Masque\n\n@bind ev masque(fig)\n\nThis is the fastest way to get something clickable. ev is the bond value: nothing until you click a marker, then an ElementEvent. Unsupported plot types are skipped with a @warn, not an error.","title":"2. masque(fig) — zero-config"},{"category":"section","location":"getting-started/#3.-Explicit-interactables-—-when-you-want-control","page":"Getting started","text":"Zero-config is sugar over an explicit vector of interactables. Building it yourself gets you custom payloads, a chosen id, and non-default styling — on the same fig/ax from step 1, without plotting pts a second time. Replace the @bind ev masque(fig) cell above with this — Pluto rejects two cells that both bind ev:\n\nlabels = [\"a\", \"b\", \"c\"]\n\n@bind ev masque(fig, [PointInteractable(ax, pts; id = :points, payloads = labels)])\n\nThat points constructor takes the highlight radius from the scatter already on ax (the drawn marker, not a fixed radius of 9). When you still have the plot object, PointInteractable(ax, scatter) is the usual call: same radius, and it also reads color= for the tooltip accent.\n\nEvery built-in kind, its constructor, and its default payload are on the Interactables page. You can also start from auto_interactables(fig) (the same vector masque(fig) builds internally), tweak it, and pass it back — see Zero-config: masque(fig).","title":"3. Explicit interactables — when you want control"},{"category":"section","location":"getting-started/#4.-A-cell-that-reacts","page":"Getting started","text":"@bind re-runs every cell that reads ev whenever the bond value changes:\n\nev === nothing ? \"click a point\" : \"you picked $(labels[ev])\"\n\nThe embed below is the same plot as the README demo GIF (docs/dev/readme-demo/notebook.jl): eight cities, hover for population, click to bind. Every city is listed so the readout swaps like live @bind. That notebook binds pick (same as the GIF source); the walkthrough cells above bind ev. Hover still works on every mark. Continuous kinds (ROI, axis, view) stay overlay-only — that freeze is not for cities omitted from this plot.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gs-player\" title=\"README demo cities scatter with listed @bind snapshots\"\n        style=\"width:100%;height:480px;border:0;background:transparent;overflow:hidden;\"\n        scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n<script>\n(function () {\n  var pretty = /\\/$/.test(location.pathname) || /\\/index\\.html$/.test(location.pathname);\n  var el = document.getElementById(\"masque-gs-player\");\n  if (!el) return;\n  function isDocDark() {\n    var c = document.documentElement.className || \"\";\n    if (!c) return false;\n    if (/(^|\\s)theme--(documenter-light|catppuccin-latte)(\\s|$)/.test(c)) return false;\n    return /(^|\\s)theme--/.test(c);\n  }\n  function pushTheme() {\n    var doc = el.contentDocument;\n    if (!doc) return;\n    doc.documentElement.classList.toggle(\"pluto-dark\", isDocDark());\n  }\n  el.addEventListener(\"load\", pushTheme);\n  new MutationObserver(pushTheme).observe(document.documentElement, { attributes: true, attributeFilter: [\"class\"] });\n  el.src = (pretty ? \"../embeds/\" : \"embeds/\") + \"getting_started.html\";\n})();\n</script>\n\nBefore the first click, ev is nothing. After a click, ev is an ElementEvent. ev.index is 1-based, and labels[ev] is that row. A named payload forwards its fields, so (; city = \"Tokyo\") is read as ev.city. ev.layer is the interactable's id.\n\nOther commits are their own types, with the fields on the event: an axis click is an AxisEvent (ev.x, ev.y), a heatmap cell is a GridCellEvent (A[ev]), a threshold is a ThresholdEvent (ev.value). A selects ROI over points yields a Vector{ElementEvent}. See Selection.","title":"4. A cell that reacts"},{"category":"section","location":"getting-started/#5.-Choosing-the-backend","page":"Getting started","text":"Masque doesn't have a backend package to install separately — it picks its backend from whichever Makie package you using:\n\nusing Masque, CairoMakie   # :cairo — static PNG + overlay (the default)\n\nusing Masque, WGLMakie      # :webgl — live browser-GPU canvas\n\nEverything above (masque, @bind, InteractionEvent, every interactable) is identical on both — see Backends for when to reach for which, and what happens if you load neither or both.","title":"5. Choosing the backend"},{"category":"section","location":"accessibility/#Keyboard-and-screen-readers","page":"Keyboard and screen readers","text":"Every overlay is a keyboard-navigable, screen-reader-announced widget — no setup required. Tab to a plot, then use the keys below to move between its interactive elements.","title":"Keyboard and screen readers"},{"category":"section","location":"accessibility/#Keys","page":"Keyboard and screen readers","text":"Key Moves to\n→ / ↓ Next element\n← / ↑ Previous element\nHome / End First / last element\nPage Down / Page Up First element of the next / previous layer\nEnter / Space Select the focused element — dispatches the same @bind value a click would\nEscape Clear focus and leave the plot\n\nArrow keys and Home/End stop at the first and last element — they don't wrap around. Only elements from click- or hover-enabled element layers are reachable this way: scatter points, bars, polygons, lines, and line segments. A heatmap/image cell grid is not — hovering and clicking still work with a mouse, but a grid can have far more cells than anyone would want to arrow through one at a time, so keyboard focus skips it. Whole-axis readout and the draggable threshold/ROI/view-pan interactables are mouse/touch-only in this release (see Limitations below).\n\nWhen the figure has a legend, its entries come first: Tab and Home land on the first legend entry, and Page Down steps from the legend into the plot layers. This matches the pointer, where a legend drawn on top of a plot takes the clicks under it. Focusing an entry highlights the trace it labels, exactly as hovering it does, and the announcement names that entry's label even though a legend shows no tooltip unless you pass one — see Legend.\n\nMoving focus draws the same highlight ring a mouse hover would (there is never a separate \"focus\" look). A layer that has a tooltip shows that same card on the focused element. A legend entry does not, unless you passed a template.","title":"Keys"},{"category":"section","location":"accessibility/#What-gets-announced","page":"Keyboard and screen readers","text":"Screen readers announce each element you land on: an optional layer name, its position within that layer, and the same content the tooltip shows, as plain text. For example:\n\nPointInteractable(ax, pts; label = \"Scatter\", payloads = [(; x = 1.0, y = 4.0)])\n\nmight announce \"Scatter, element 1 of 3: x 1.0, y 4.0\". Without label, the announcement omits the prefix: \"element 1 of 3: x 1.0, y 4.0\".\n\nlabel is a single string per layer — set it once on the interactable, not per element. (Don't confuse it with a label payload field some examples elsewhere in these docs use for per-element tooltip data — that's unrelated, ordinary tooltip content.) It's accepted by PointInteractable, SegmentInteractable, RectInteractable (the list form), and PolygonInteractable — the same element-kind constructors tooltip is accepted by, minus the kinds keyboard nav doesn't visit.\n\nmasque(fig, [\n    PointInteractable(ax, pts; id = :cities, label = \"City\"),\n    RectInteractable(ax; rects = bars, id = :bars, label = \"Revenue by quarter\"),\n])\n\nlabel isn't yet threaded through the plot-object constructors (PointInteractable(ax, p::Makie.Scatter), RectInteractable(ax, p::Makie.BarPlot), and similar) or masque(fig)'s auto-extraction — the same gap tooltip has today. Build the interactable explicitly with the keyword-argument form above to set a label.","title":"What gets announced"},{"category":"section","location":"accessibility/#accessibility-limitations","page":"Keyboard and screen readers","text":"Grid layers (:grid — heatmap/image cells) are not keyboard-focusable, for the reasons above. They remain fully mouse/touch-interactive.\nThreshold, ROI, and view-pan drags have no keyboard equivalent yet — arrow-key nudging for these is on the roadmap, not in this release.\nScreen-reader support for aria-live inside a shadow DOM is uneven across browser/AT combinations. If announcements are silent in your setup, the visual focus ring and tooltip still work, and file an issue — this is the area most likely to need a fallback (e.g. moving the live region out of the shadow root) as real-world testing accumulates.","title":"Limitations"}]
}
