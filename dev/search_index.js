var documenterSearchIndex = {"docs":
[{"category":"section","location":"legend/#Legend","page":"Legend","text":"A legend already names every series in a figure, so it is a natural place to choose one. With Masque, hovering a legend entry highlights the lines or points it labels, and clicking it hands that series to your notebook. masque(fig) makes every Legend and axislegend interactive and links each entry to the plots it describes.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-legend-lines\" data-masque-embed=\"legend_lines\" title=\"two-line axislegend with listed @bind snapshots\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a legend entry. The last cell names that series, and the line stays highlighted.Draw two labeled lines and add axislegend, the way you already build a figure. masque links each entry to its line.begin\n    xs = range(0, 2π; length = 80)\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\")\n    lines!(ax, xs, sin.(xs); label = \"a\")\n    lines!(ax, xs, cos.(xs); label = \"b\")\n    axislegend(ax)\n    nothing\nend@bind pick stores a click on a legend entry in pick. Hover highlights that line and does not change pick.@bind pick masque(fig)(Image: The figure before any interaction)Before a click, pick is nothing. A legend click is a LegendEvent, and pick.label is that entry. A click on a line is an ElementEvent with no label. This cell reads pick, so it re-runs on the click.if pick isa LegendEvent\n    \"$(pick.label) selected\"\nelse\n    \"click a legend entry\"\nendclick a legend entry\n\n","title":"Legend"},{"category":"section","location":"legend/#What-a-click-returns","page":"Legend","text":"A click on an entry makes pick a LegendEvent. pick.label is the entry's text, pick.group is the group title in a grouped legend (nothing otherwise), and pick.targets lists the layers the entry highlights. Clicks on the plot itself still return the plot's own events, so a cell can check which kind it got:\n\npick isa LegendEvent ? \"series $(pick.label)\" : \"click a legend entry\"","title":"What a click returns"},{"category":"section","location":"legend/#Fade-the-other-series","page":"Legend","text":"The highlight is drawn over the figure; it cannot hide or dim the lines Makie drew. To fade the unselected series, draw a second figure in a cell that reads pick:\n\nbegin\n    focus = pick isa LegendEvent ? pick.label : nothing\n    fig2 = Figure(size = (560, 360))\n    ax2 = Axis(fig2[1, 1])\n    for (label, f) in ((\"a\", sin), (\"b\", cos))\n        faded = focus !== nothing && label != focus\n        lines!(ax2, xs, f.(xs); label, alpha = faded ? 0.2 : 1.0)\n    end\n    fig2\nend\n\nThe same pick.label can filter a table, choose which series to fit, or drive any other cell.","title":"Fade the other series"},{"category":"section","location":"legend/#Which-marks-an-entry-highlights","page":"Legend","text":"Each entry is linked automatically to the plots Makie drew it for: the line for a lines! entry, both the points and the line for scatterlines! and stem!, and one series of a series! plot. Pass targets= to LegendInteractable when you want something else — for example, to have an entry light up a scatter as well as its line:\n\nLegendInteractable(leg; targets = Dict(\"a\" => :lines, \"b\" => [:lines_2, :scatter]))\n\nA target is a layer id, which highlights every mark in that layer, or id:k, which highlights only element k of it. targets can also be a vector with one entry per legend row, top to bottom. A label or layer that does not exist raises an ArgumentError, so a typo shows up immediately.\n\nA legend built by hand from LineElements has nothing to link to unless you say so. Give the elements Makie's own plots= keyword, or pass targets=. An entry with no targets is still clickable; it just highlights nothing.","title":"Which marks an entry highlights"},{"category":"section","location":"legend/#A-tooltip-for-each-entry","page":"Legend","text":"Legend entries show no tooltip by default, since the label is already on screen and a card would cover the neighbouring entries. Pass a template to add one; its fields are label, group, and targets:\n\nLegendInteractable(leg; tooltip = masque\"$(label) — $(group)\")","title":"A tooltip for each entry"},{"category":"section","location":"legend/#Keep-a-series-highlighted-after-a-rebuild","page":"Legend","text":"selected= on the legend layer highlights the entry's swatch. To keep the series itself highlighted when the figure is rebuilt, select the series' own layer instead — for example selected = Dict(:lines => [1]). See Selection.\n\nA legend can also highlight series on other axes of the same figure; Linked views shows that across two panels. Keyboard focus visits the legend entries first, and highlights the series as hover does.","title":"Keep a series highlighted after a rebuild"},{"category":"section","location":"concepts/#Concepts","page":"Concepts","text":"Masque does three things to a Makie figure you have already drawn: it decides which marks respond to the pointer, it shows information about a mark without re-running Julia, and it hands a deliberate choice — a click, a brushed region, a dragged threshold — back to your notebook through @bind. This page explains how those pieces fit together, so the guides read as variations on one idea rather than a list of features.","title":"Concepts"},{"category":"section","location":"concepts/#One-figure,-one-overlay","page":"Concepts","text":"masque(fig, interactables) renders the figure once, works out where every interactive mark sits on the image, and returns a widget: the image with a thin browser overlay on top. The overlay does the hit-testing, draws highlights and tooltips, and moves drag handles. A figure with several axes still gets one masque call and one overlay; every axis shares it.\n\n@bind pick masque(fig)\n\nCalled with only the figure, masque walks every axis, legend, and colorbar and makes whatever it recognizes interactive (the Recipes masque(fig) extracts list says which plots). Pass interactables yourself when you want to choose the marks, attach your own data, or add something Makie never drew, such as a draggable threshold.","title":"One figure, one overlay"},{"category":"section","location":"concepts/#Interactables-and-payloads","page":"Concepts","text":"An interactable says what the pointer can hit and what each hit means. PointInteractable makes the points of a scatter hittable, RectInteractable bars or heatmap cells, ROIInteractable a box you drag, and so on; Constructors lists them all.\n\nEach hittable mark carries a payload: the data that belongs to it. Give payloads one entry per mark, in the order the marks were drawn — a vector of named tuples or a DataFrame with one row per mark:\n\ncities = PointInteractable(ax, s; payloads = rows)   # rows[i] belongs to point i\n\nThe payload is what makes an overlay about your data rather than about coordinates. The tooltip shows it, and a click hands it back to Julia (pick.city, pick.pop). If you omit payloads, each mark gets a 1-based index plus the coordinates its constructor knows (for a scatter, x and y).","title":"Interactables and payloads"},{"category":"section","location":"concepts/#Tooltips-are-templates,-not-functions","page":"Concepts","text":"A tooltip is built in the browser from the payload of the mark under the pointer. By default it is a small table of the payload's fields. A masque\"...\" template lays the same fields out as you like:\n\nPointInteractable(ax, s; payloads = rows, tooltip = masque\"$(city) — pop $(pop:,)\")\n\n$(city) is a payload field, not a Julia variable, and $(pop:,) formats a number with a d3-format spec. There is no tooltip = row -> ... callback: the tooltip has to work without a running Julia process (in a static export, for example), so anything computed goes into the payload first. tooltip = false turns the card off. See Tooltips.","title":"Tooltips are templates, not functions"},{"category":"section","location":"concepts/#What-each-gesture-does","page":"Concepts","text":"Holding the pointer over a mark and clicking it are different on purpose. Hover is for looking: it never leaves the browser, so it is instant and never re-runs a cell. A click is a decision: it becomes the value of the @bind variable, and every cell that reads that variable re-runs.\n\nGesture On the figure The @bind value Cells that read it\nHover a mark Tooltip and highlight Unchanged Do not re-run\nClick a mark (or Enter / Space on a focused mark) Mark stays highlighted The clicked mark's event Re-run\nClick empty space Nothing Unchanged Do not re-run\nDrag an ROI box or threshold line Box or line moves Unchanged while dragging Do not re-run\nRelease the ROI or threshold Enclosed marks highlight (with selects) The box, the enclosed marks, or the line's value Re-run\nPan or orbit (ViewInteractable) The view moves Never changes Do not re-run\n\nPan and orbit are about looking too: a camera position is not a result, so it never becomes the @bind value. With a live kernel Masque re-renders frames while you drag (see Pan and orbit).\n\nA click in empty space leaves the current selection alone. Clicking a different mark replaces it; to start with marks already selected, or to keep a selection when the figure is rebuilt, see Selection.","title":"What each gesture does"},{"category":"section","location":"concepts/#What-the-@bind-value-holds","page":"Concepts","text":"Before the first click or release, the bound variable is nothing (unless you passed selected=). After that it is an event: a small struct whose fields you read directly. For a clicked mark, the payload's fields are on the event (pick.city), next to pick.layer (which interactable was hit) and a 1-based pick.index. An event also indexes the data it came from: xs[pick] and df[pick, :] pick out that mark's row.\n\nInteraction @bind value Read it as\nClick a point, bar, polygon, line, or text label ElementEvent pick.index, payload fields; xs[pick]\nClick a legend entry LegendEvent pick.label\nRelease an ROI with selects over points Vector{ElementEvent} one event per enclosed point; [] when empty\nClick a heatmap or image cell GridCellEvent pick.i, pick.j, pick.value; A[pick]\nRelease an ROI with selects over a grid GridWindowEvent win.i1:win.i2, win.j1:win.j2; A[win]\nRelease an ROI without selects BoundsEvent box.xmin, box.xmax, box.ymin, box.ymax\nClick an axis (AxisInteractable) AxisEvent pick.x, pick.y\nClick a colorbar ColorbarEvent pick.value\nRelease a threshold line ThresholdEvent pick.value\n\nOne widget can hold several interactables, so the value is whichever event came last; check pick.layer or the event's type when a cell has to tell them apart. One exception: the layer an ROI's selects names belongs to the box. Its points or cells still show their tooltips, but they take no clicks: a click on one passes through to whatever clickable layer is underneath. Usually nothing is, and the value stays what the box holds (a Vector{ElementEvent} or a GridWindowEvent). An AxisInteractable catches clicks anywhere on its axis, so in a widget that has one, that click becomes an AxisEvent. Clicks on any other layer in the widget still arrive as a single event.","title":"What the @bind value holds"},{"category":"section","location":"concepts/#Live-notebook,-static-export,-and-this-site","page":"Concepts","text":"In a running notebook everything above works. In a static HTML export of the notebook there is no Julia process: tooltips, highlights, and drag handles still work, but nothing reaches @bind, so downstream cells keep the values they were exported with.\n\nThe interactive examples on this site are recordings of real notebooks. Hovering works as it does in Pluto; clicks swap in results that were computed ahead of time for a listed set of choices (the Simulating @bind badge marks these). Each example also has a Notebook as text section with the same cells, which you can copy into your own notebook.","title":"Live notebook, static export, and this site"},{"category":"section","location":"gallery/polar/#Polar-points","page":"Polar points","text":"Four scatter points on a PolarAxis. Hover for the default tooltip. Click for an ElementEvent: index is 1-based, x is θ, and y is r. masque(fig) names the layer :scatter.\n\nPolar is not a WebGL-only feature. See Click marks.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-polar\" data-masque-embed=\"gallery_polar\" title=\"Polar points\" style=\"width:100%;height:1320px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a point. The last cell reads its direction and radius.Scatter four points on a PolarAxis. masque makes each one clickable.begin\n    fig = Figure(; size = (480, 400))\n    ax = PolarAxis(fig[1, 1])\n    pts = Point2f[(0.0, 1.0), (π / 2, 2.0), (π, 1.5), (3π / 2, 2.5)]\n    scatter!(ax, pts; color = :dodgerblue, markersize = 22)\n    nothing\nend@bind pick stores the click.@bind pick masque(fig)(Image: The figure before any interaction)This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"click a point\"\nelse\n    dirs = [\"east\", \"north\", \"west\", \"south\"]\n    \"$(dirs[pick.index]) — r = $(pick.y)\"\nendclick a point\n\n","title":"Polar points"},{"category":"section","location":"gallery/polar/#Variations","page":"Polar points","text":"Continuous θ/r readout, the way an AxisInteractable reports (x, y), is not shipped. A click in empty space does not produce an event. ViewInteractable on a PolarAxis raises ArgumentError.\n\nnote: Note\nDiscrete point hits match on :cairo and :webgl. This player is the Cairo figure, including the listed clicks. heatmap! and barplot! on a PolarAxis are skipped with @warn on both backends. Load WGLMakie alone when the polar figure should be a live canvas; the hits do not change.","title":"Variations"},{"category":"section","location":"gallery/limits/#Limits","page":"Limits slider","text":"The axis is built with limits = (0, 6, 0, 40). Every marker inside that window is still a hit target. The point on the right edge stays clickable.\n\nThis page is that one window. Hover or click a marker. Dragging the axis is Drag to pan, which does not rebuild the cell.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-limits\" data-masque-embed=\"gallery_limits\" title=\"Limits slider\" style=\"width:100%;height:1100px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nThe rightmost point sits on the axis limit and stays clickable.Set the axis limits, scatter the points, and pass that scatter to PointInteractable.begin\n    data = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0), (4.0, 16.0), (5.0, 25.0), (6.0, 36.0)]\n    fig = Figure(size = (500, 320))\n    ax = Axis(\n        fig[1, 1];\n        limits = (0, 6, 0, 40),\n        title = \"the point at x = 6 stays clickable\",\n    )\n    s = scatter!(ax, first.(data), last.(data); color = :dodgerblue, markersize = 18)\n    pts = PointInteractable(ax, s)\n    nothing\nendHover and click stay on the figure. This page does not re-run a readout.@bind pick masque(fig, pts)(Image: The figure before any interaction)\n\n","title":"Limits"},{"category":"section","location":"gallery/limits/#Variations","page":"Limits slider","text":"Rebuilding the figure with new limits, or setting azimuth and elevation on an Axis3, is a different notebook. Selection survives either rebuild when you pass selected= into the new masque call. A Ref that does not read the slider holds the indices between rebuilds. Do not pass selected= to store a camera pose.\n\nnote: Note\nRe-projecting after a limits change is the same on :cairo and :webgl. This notebook does not load PlutoUI or bind a slider. For in-drag frames without a rebuild, see Drag to pan and Drag to orbit.","title":"Variations"},{"category":"section","location":"linked-views/#Linked-views","page":"Linked views","text":"Masque does not wire plots together itself; Pluto does. A click in a Masque widget becomes the value of a @bind variable, and every cell that reads that variable re-runs. So \"linking\" a scatter to a detail plot, a table, or a model fit is ordinary Pluto code: read pick, draw or compute something from it. This page shows that pattern first, then what one widget can do across several axes on its own.","title":"Linked views"},{"category":"section","location":"linked-views/#Drive-a-second-plot-from-a-click","page":"Linked views","text":"Put a key in each payload that identifies the row — an id, a name — and use it downstream. Here a map of cities drives a plot of the clicked city's series:\n\nbegin\n    cities = [\n        (id = \"tok\", name = \"Tokyo\", lon = 139.7, lat = 35.7),\n        (id = \"del\", name = \"Delhi\", lon = 77.2, lat = 28.6),\n        (id = \"sha\", name = \"Shanghai\", lon = 121.5, lat = 31.2),\n    ]\n    series = Dict(\n        \"tok\" => [3.1, 3.4, 3.2, 3.6],\n        \"del\" => [2.2, 2.5, 2.9, 3.0],\n        \"sha\" => [2.0, 2.4, 2.3, 2.7],\n    )\n    fig = Figure(size = (560, 320))\n    ax = Axis(fig[1, 1]; xlabel = \"longitude\", ylabel = \"latitude\")\n    s = scatter!(ax, [c.lon for c in cities], [c.lat for c in cities]; markersize = 16)\n    pts = PointInteractable(ax, s; payloads = cities)\n    nothing\nend\n\n@bind pick masque(fig, pts)\n\nbegin\n    detail = Figure(size = (560, 240))\n    dax = Axis(detail[1, 1]; title = pick === nothing ? \"click a city\" : pick.name)\n    pick === nothing || lines!(dax, series[pick.id])\n    detail\nend\n\nThe detail cell reads pick, so it re-runs on each click. The same pick.id could filter a DataFrame, pick the data for a fit, or be passed to another Masque widget — the second figure can be interactive too. Hovering never re-runs these cells; only clicks and releases do.\n\nTo show the clicked point as selected in a second widget as well, pass it as that widget's selected=; Selection round-trip shows it.","title":"Drive a second plot from a click"},{"category":"section","location":"linked-views/#Several-axes-in-one-figure","page":"Linked views","text":"One masque call covers every axis in a figure, and each axis keeps its own hits: hovering a point highlights that point on the panel you are on. Each scatter becomes its own layer (:scatter, :scatter_2, …), so pick.layer says which panel was clicked.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-lv-two-axis\" data-masque-embed=\"linked_two_axis\" title=\"Two Axis panels, four points, overlay-only hover\" style=\"width:100%;height:1100px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHold the pointer over a point. The highlight stays on the panel you are on.Draw both scatters in one figure, the way you already would. One masque call covers both axes.begin\n    xs = [1.0, 2.0, 3.0, 4.0]\n    ys = [2.0, 1.5, 3.0, 2.2]\n    zs = [0.8, 2.5, 1.2, 3.1]\n    fig = Figure(size = (640, 280))\n    ax_xy = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\", title = \"xy\")\n    ax_xz = Axis(fig[1, 2]; xlabel = \"x\", ylabel = \"z\", title = \"xz\")\n    scatter!(ax_xy, xs, ys; markersize = 18)\n    scatter!(ax_xz, xs, zs; markersize = 18)\n    nothing\nendHover stays on the figure. A click can still land in pick in your own notebook; this page does not re-run a readout.@bind pick masque(fig)(Image: The figure before any interaction)\n\n\n\nMasque does not assume that the same index in two plots is the same observation, so hovering point 3 in one panel does not highlight point 3 in the other. When they are the same row, make the connection explicitly: on a click, rebuild the figure with both layers selected,\n\nmasque(fig; selected = Dict(:scatter => [i], :scatter_2 => [i]))\n\nwhere i comes from a cell that does not read this widget's own value. With one index on each of two layers the widget highlights both but starts with its value at nothing, so a cell reading this widget's pick goes back to its \"nothing clicked\" state after the rebuild.","title":"Several axes in one figure"},{"category":"section","location":"linked-views/#Highlight-a-series-across-panels","page":"Linked views","text":"A legend entry is the one thing that highlights across axes by itself: hovering it lights up every mark of the series it names, on whatever axis the series is drawn. Clicking it returns the legend entry, which a downstream cell can use to filter (see Legend).\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-lv-legend-wash\" data-masque-embed=\"linked_legend_wash\" title=\"Legend whole-layer wash across two Axis panels\" style=\"width:100%;height:1100px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHold the pointer over a legend entry. Every point in that series highlights, on both axes.Label each scatter and put them in one Legend. masque links the entry to the series it names.begin\n    xs = [1.0, 2.0, 3.0, 4.0]\n    ys = [2.0, 1.5, 3.0, 2.2]\n    zs = [0.8, 2.5, 1.2, 3.1]\n    fig = Figure(size = (700, 280))\n    ax_xy = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\", title = \"xy\")\n    ax_xz = Axis(fig[1, 2]; xlabel = \"x\", ylabel = \"z\", title = \"xz\")\n    p_xy = scatter!(ax_xy, xs, ys; label = \"xy\", markersize = 18)\n    p_xz = scatter!(ax_xz, xs, zs; label = \"xz\", markersize = 18)\n    Legend(fig[1, 3], [p_xy, p_xz], [\"xy\", \"xz\"])\n    nothing\nendHover paints the series. This page does not re-run a readout.@bind pick masque(fig)(Image: The figure before any interaction)\n\n","title":"Highlight a series across panels"},{"category":"section","location":"linked-views/#Filter-a-table-from-a-region","page":"Linked views","text":"Brushing is the many-marks version of a click: an ROIInteractable with selects returns every point inside the box on release, and a cell slices your table with it — df[picks, :]. Brush a region walks through it.","title":"Filter a table from a region"},{"category":"section","location":"linked-views/#What-Masque-does-not-link-for-you","page":"Linked views","text":"Hover is local to the mark under the pointer; it does not echo into other plots.\nThere is no shared data source between widgets. Two masque calls are two independent widgets, connected only through the Pluto cells you write.\nEvery box with selects in one widget must name the same layer, and boxes are rectangles: there is no lasso and no cross-filtering between brushes on different plots.\n\nFor how hover, clicks, and drags reach Julia, see Concepts.","title":"What Masque does not link for you"},{"category":"section","location":"gallery/bars/#Bars-and-areas","page":"Bars and areas","text":"masque(fig) walks the figure and picks up histogram bins, waterfall bars, crossbar ranges, bar plots, and horizontal and vertical spans. No interactable is written by hand. Hover a bar, a band, or a range.\n\nThe four panels are one widget. For a single barplot!, see Click marks.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-bars\" data-masque-embed=\"gallery_bars\" title=\"Bars and areas\" style=\"width:100%;height:1480px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHover a bar, a band, or a span. One masque call covers every panel.Draw the panels the way you already draw them. You do not add an interactable for each recipe.begin\n    rng = Xoshiro(1)\n    bf = Figure(size = (760, 520))\n    hist!(Axis(bf[1, 1]; title = \"hist\"), randn(rng, 300); bins = 12, color = :steelblue)\n    waterfall!(Axis(bf[1, 2]; title = \"waterfall\"), 1:4, [3.0, -1.0, 2.0, -0.5])\n    crossbar!(\n        Axis(bf[2, 1]; title = \"crossbar\"),\n        [1, 2, 3],\n        [5.0, 6.0, 4.0],\n        [3.0, 4.0, 2.0],\n        [7.0, 8.0, 6.0],\n    )\n    a4 = Axis(bf[2, 2]; title = \"bars + spans\")\n    barplot!(a4, 1:3, [2.0, 3.0, 1.0])\n    hspan!(a4, [0.4], [0.8]; color = (:orange, 0.2))\n    vspan!(a4, [1.6], [2.0]; color = (:green, 0.2))\n    nothing\nendHover stays on the figure. This page does not re-run a readout.@bind pick masque(bf)(Image: The figure before any interaction)\n\n","title":"Bars and areas"},{"category":"section","location":"gallery/bars/#Variations","page":"Bars and areas","text":"The same call also covers the other bar-like recipes in Recipes masque(fig) extracts. A recipe that is not in that table is not a hit target until you add an interactable yourself.\n\nnote: Note\nAuto-extraction of these recipes is the same on :cairo and :webgl. This player is the Cairo figure, and hover stays in the overlay: the page does not re-run Julia on a click. Reach for :webgl when the figure is large or you are already on the live canvas. barplot! on a PolarAxis is not this figure; that call is skipped with @warn.","title":"Variations"},{"category":"section","location":"readouts/#Read-coordinates","page":"Read coordinates","text":"Not every question is about a mark. Sometimes you want a value off the plot itself: the data coordinates under the pointer, the value a colour stands for, or a cutoff you set by dragging a line. This page covers the three interactables that turn a pointer position into a number. None of them is added by masque(fig) on its own except the colorbar, so pass them yourself.","title":"Read coordinates"},{"category":"section","location":"readouts/#Read-(x,-y)-from-the-axis","page":"Read coordinates","text":"AxisInteractable turns the whole plot area into a readout. Moving the pointer shows the data coordinates in a card that follows the cursor; clicking makes pick an AxisEvent with pick.x and pick.y:\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-readouts-player\" data-masque-embed=\"readouts_axis\" title=\"Sine plot with axis coordinate readout overlay\" style=\"width:100%;height:1100px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nMove the pointer across the axis. The tooltip reads data (x, y) and follows the cursor.Draw the line the way you already draw it, and add AxisInteractable on that axis. masque(fig) does not add this on its own.begin\n    xs = range(0, 2π; length = 80)\n    fig = Figure(size = (560, 320))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"sin(x)\")\n    lines!(ax, xs, sin.(xs); color = :steelblue)\n    ints = AxisInteractable(ax)\n    nothing\nend@bind pick is there for a click in your own notebook. A click stores pick.x and pick.y. This page keeps the readout on the figure, so the cell below does not re-run.@bind pick masque(fig, ints)(Image: The figure before any interaction)\n\n\n\nUse it to mark a position — the start of a time window, a point to fit from — without needing a mark there. It works on linear and log axes, and on categorical ones. On a categorical axis the card shows the category under the pointer, and a click returns the category's position (Makie places categories at 1, 2, …, n) with its label in pick.xcat or pick.ycat; on a numeric dimension that field is nothing.","title":"Read (x, y) from the axis"},{"category":"section","location":"readouts/#Read-a-colorbar-value","page":"Read coordinates","text":"ColorbarInteractable makes a colorbar answer \"what value is this colour?\". Hovering shows the value under the pointer; a click makes pick a ColorbarEvent with pick.value. masque(fig) adds one for every Colorbar in the figure; build it yourself when you want the colorbar without the heatmap's cells in the same widget:\n\nbegin\n    z = [Float64(i + 3j) for i in 1:4, j in 1:3]\n    fig = Figure(size = (560, 280))\n    ax = Axis(fig[1, 1])\n    hm = heatmap!(ax, 1:4, 1:3, z)\n    cb = Colorbar(fig[1, 2], hm)\n    cbint = ColorbarInteractable(cb)\n    nothing\nend\n\n@bind pick masque(fig, cbint)\n\nA click on the colorbar could, for example, set a contour level or a threshold for the heatmap drawn in a later cell.","title":"Read a colorbar value"},{"category":"section","location":"readouts/#Drag-a-threshold","page":"Read coordinates","text":"ThresholdInteractable draws a line across the axis that you drag. It suits any cut-off you would otherwise set with a slider, with the advantage that you set it against the data itself. value is where the line starts; :horizontal gives a line at constant y that you drag up and down, :vertical one at constant x:\n\nbegin\n    ys = [0.2, 0.8, 0.4, 0.9, 0.3, 0.6, 0.1, 0.7]\n    fig = Figure(size = (560, 320))\n    ax = Axis(fig[1, 1])\n    scatter!(ax, 1:8, ys; markersize = 16)\n    cutoff = ThresholdInteractable(ax; orientation = :horizontal, value = 0.5)\n    nothing\nend\n\n@bind level masque(fig, cutoff)\n\nThe line moves on the figure while you drag, and on release level becomes a ThresholdEvent; level.value is the new position in data coordinates. Before the first drag level is nothing, so fall back to the starting value:\n\nbegin\n    t = level === nothing ? 0.5 : level.value\n    \"$(count(>(t), ys)) of $(length(ys)) points above $(round(t; digits = 2))\"\nend\n\nA rebuilt figure starts the line at value again. Feeding level back into the same figure would make Pluto report a cyclic reference, for the same reason as selected = pick (see Selection), so keep the starting value in its own cell if other inputs rebuild the figure.","title":"Drag a threshold"},{"category":"section","location":"readouts/#Where-these-work","page":"Read coordinates","text":"All three need to turn a pixel back into data, so they need a 2D Axis (or colorbar) with an identity, log10, or log scale; on an Axis3 or a PolarAxis they raise an ArgumentError when masque runs. A threshold dragged along a categorical dimension shows the category while you drag; on release the line snaps onto that category, level.value is its position, and level.category its label. See Supported plots and axes. If the axis also has a ViewInteractable, a plain drag moves the threshold and Shift+drag pans.","title":"Where these work"},{"category":"section","location":"api/#API","page":"API","text":"Full docstrings for every exported name, grouped by area. Usage guidance and worked examples live on the other pages; this page is the reference.","title":"API"},{"category":"section","location":"api/#Entry-point","page":"API","text":"","title":"Entry point"},{"category":"section","location":"api/#Interactable-constructors","page":"API","text":"","title":"Interactable constructors"},{"category":"section","location":"api/#Tooltip-macro-and-type","page":"API","text":"For usage, see Tooltips.","title":"Tooltip macro and type"},{"category":"section","location":"api/#Tooltip-chrome","page":"API","text":"Set any of these on masque to lock that property (and opt it out of dark-mode inversion):\n\nmasque(fig, interactables...;\n    tooltip_bg        = nothing,   # background  — CSS string or Makie color\n    tooltip_color     = nothing,   # text color  — CSS string or Makie color\n    tooltip_accent    = nothing,   # accent (emphasis / links)\n    tooltip_font      = nothing,   # font-family — String\n    tooltip_font_size = nothing,   # Real → appended with \"px\"\n    tooltip_radius    = nothing,   # Real → appended with \"px\"\n    tooltip_caret     = true,      # Bool — draw the caret toward the mark\n)\n\nnothing (the default for every keyword except tooltip_caret) means use the built-in default. Only the keywords you set change anything.\n\nThe --masque-tip-* custom properties inherit like any CSS custom property. Setting one on an ancestor of the cell overrides it with no Julia API:\n\n<style>\nmain { --masque-tip-bg: #1a1a2e; --masque-tip-color: #e0e0e0; }\n</style>\n\n--masque-tip-bg / --masque-tip-color / --masque-tip-border are derived from the figure's background unless you set them here or with a tooltip_* keyword. The \"Legacy fallback\" column is what a browser without CSS relative-color syntax uses instead:\n\nCustom property Legacy fallback (light / dark) Julia keyword\n--masque-tip-bg #ffffff / #1e1e1e tooltip_bg\n--masque-tip-color #1a1a1a / #e8e8e8 tooltip_color\n--masque-tip-border rgba(0,0,0,0.1) / rgba(255,255,255,0.15) — (CSS only)\n--masque-tip-accent #6b7280 tooltip_accent\n--masque-tip-font system-ui, -apple-system, sans-serif tooltip_font\n--masque-tip-font-size 11px tooltip_font_size\n--masque-tip-radius 4px tooltip_radius\n--masque-tip-caret block (the caret's display) tooltip_caret (false → none)\n--masque-tip-padding 8px 12px — (CSS only)\n--masque-tip-shadow 0 2px 4px rgba(0,0,0,0.12), 0 8px 16px rgba(0,0,0,0.08) / 0 2px 4px rgba(0,0,0,0.4), 0 8px 16px rgba(0,0,0,0.3) — (CSS only)\n--masque-tip-maxwidth 320px — (CSS only)\n--masque-mark-border (unset — plain 1px border) — (from the mark's colors; see Accent color)","title":"Tooltip chrome"},{"category":"section","location":"api/#Custom-hit-interface","page":"API","text":"For usage, see Custom hits.","title":"Custom-hit interface"},{"category":"section","location":"api/#Backend-abstraction","page":"API","text":"CairoBackend and WebGLBackend are the two concrete backends, but they are defined inside Masque's package extensions (ext/MasqueCairoMakieExt.jl, ext/MasqueWGLMakieExt.jl) rather than in Masque itself — they only exist once CairoMakie / WGLMakie is loaded, so Documenter cannot resolve @docs for them without loading both weak dependencies into the docs build to document two structs. They are documented in prose instead: see Backends for what each does, and masque's docstring on this page for the backend= keyword both accept.","title":"Backend abstraction"},{"category":"module","location":"api/#Masque","page":"API","text":"Masque\n\nOverlay JS interactivity — hover tooltips, click-to-select, drag-to-pan/rotate — on a static or live Makie Figure for use in a Pluto notebook.\n\nDeclare AbstractInteractables (or call masque(fig) for zero-config auto-extraction via auto_interactables) and bind the result with @bind; the bond reports the current selection — nothing when nothing is selected, otherwise an InteractionEvent. Needs a rendering backend loaded: using CairoMakie for a static image with a JS hit-test overlay, or using WGLMakie for a live browser-GPU canvas (animation, large/live data, 3D) — both expose the same masque/@bind contract.\n\nExamples\n\nusing Masque, CairoMakie\nfig = Figure(); ax = Axis(fig[1, 1])\nscatter!(ax, [1, 2, 3], [1, 4, 9])\n@bind sel masque(fig)   # zero-config: auto-extracts the scatter\n\n\n\n\n\n","title":"Masque"},{"category":"function","location":"api/#Masque.masque","page":"API","text":"masque(fig, interactables; kwargs...) -> MasqueWidget\nmasque(fig, interactable; kwargs...)    # single-interactable convenience\n\nOverlay fig with JS hit-testing and return a Pluto @bind source. fig is not mutated.\n\nThe bond is nothing until the first commit, unless selected= restored one. A click is one InteractionEvent. A selects ROIInteractable aimed at points commits a Vector{ElementEvent} (an empty box is ElementEvent[]); aimed at a grid, one GridWindowEvent. The box owns that bond: its target layer shows tooltips but commits no clicks. Clicks on other layers stay single events.\n\nKeywords\n\nselected — the selection's starting value, 1-based. One index on a point layer mounts as that ElementEvent; 1 and [1] are the same. Several indices highlight those marks and leave the bond nothing (a point layer holds one event). On a selects point brush, 1 and [1] mount as a one-element vector, and [] mounts as ElementEvent[]. Also accepts the event itself, or a NamedTuple / Dict keyed by layer id when the figure has more than one seedable layer. A bare index is an ArgumentError in that case. Works on :circles/:rects/:polygons/:segments/:polyline/:lines; any other kind, or an out-of-range index (0 included), raises ArgumentError naming 1:n. Clicking replaces the selection, so this is only needed to carry one through a rebuild — and it must come from a cell that doesn't read this widget's own bond, which Pluto rejects as a cyclic reference.\nbackend — CairoBackend() (static image) or WebGLBackend() (live canvas), each with its own keywords. Defaults to whichever of CairoMakie / WGLMakie is loaded, Cairo if both, ArgumentError if neither.\nmax_width — target display width in px (Pluto's column). Default 700.\ntooltip_bg, tooltip_color, tooltip_accent, tooltip_font, tooltip_font_size, tooltip_radius, tooltip_caret — tooltip card styling; each defaults to the built-in style. See the Tooltips page for the full system, including the --masque-tip-* CSS escape hatch.\n\nExamples\n\nusing Masque, CairoMakie\nfig = Figure(); ax = Axis(fig[1, 1])\npts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\nscatter!(ax, first.(pts), last.(pts))\n@bind sel masque(fig, [PointInteractable(ax, pts; payloads = [\"a\", \"b\", \"c\"])])\n\n\n\n\n\nmasque(fig; selected=nothing)\n\nAuto-extract interactables from fig (see auto_interactables) and overlay them — the zero-config path. Equivalent to masque(fig, auto_interactables(fig)); unsupported plot types are skipped with a warning. For control over ids/payloads, build the vector yourself.\n\n\n\n\n\n","title":"Masque.masque"},{"category":"function","location":"api/#Masque.auto_interactables","page":"API","text":"auto_interactables(fig) -> Vector{AbstractInteractable}\n\nIntrospect a Makie Figure: for every supported plot in every Axis, Axis3, or PolarAxis, build the interactable its explicit constructor would. On Axis3, only Scatter/Lines/ LineSegments/MeshScatter/Wireframe/Arrows3D are supported; on PolarAxis, only Scatter/Lines/LineSegments/ScatterLines/Series. Other kinds are skipped with a warning. A recipe with no branch of its own still contributes each child that has one (arc! is the lines! it draws), under that child's layer id. A child with visible[] == false is not a layer (triplot!'s ghost edges). A construct with no vertices does not take a layer id (qqplot! with qqline = :none). A data-space Scatter child is left alone (hexbin!), and so is a child whose space is not :data (bracket!). Layer ids are the plot kind (:scatter, :lines, …), suffixed _2, _3, … when a kind repeats. Returns the same concrete vector you could pass to masque yourself — edit or extend it freely.\n\nEach interactable inherits its constructor's default per-element payloads, so the zero-config path on a very large plot allocates one payload per element; construct with a lean payloads= yourself for huge data.\n\n\n\n\n\n","title":"Masque.auto_interactables"},{"category":"type","location":"api/#Masque.InteractionEvent","page":"API","text":"InteractionEvent\n\nAbstract type of every masque @bind value that is not nothing. Concrete subtypes carry the fields of that commit: an element pick, a legend entry, a grid cell or window, an axis or colorbar click, a threshold, or ROI bounds. A selects ROI over points yields a Vector{ElementEvent} instead of one event. Field names on the struct win over a payload key of the same name; on an ElementEvent or LegendEvent, other names forward to the payload. See bondtype and transform_bond.\n\n\n\n\n\n","title":"Masque.InteractionEvent"},{"category":"type","location":"api/#Masque.ElementEvent","page":"API","text":"ElementEvent(layer, index, payload)\n\nOne element of a point, bar, polygon, segment, polyline, or list-of-rects layer. index is 1-based. Other fields (e.g. city) are read from payload. Use as a row index: df[pick, :], xs[pick].\n\n\n\n\n\n","title":"Masque.ElementEvent"},{"category":"type","location":"api/#Masque.LegendEvent","page":"API","text":"LegendEvent(layer, index, payload)\n\nOne legend entry. index is which entry (1-based), not a row of the author's table. entry.label and entry.targets come from the payload. df[entry, :] does not treat the entry number as a row.\n\n\n\n\n\n","title":"Masque.LegendEvent"},{"category":"type","location":"api/#Masque.GridCellEvent","page":"API","text":"GridCellEvent(layer, i, j, value)\n\nOne heatmap or image cell. i and j are 1-based column and row of the author's (ncols, nrows) matrix. A[cell] is A[cell.i, cell.j]. value is the shipped cell value, or nothing when values were not sent.\n\n\n\n\n\n","title":"Masque.GridCellEvent"},{"category":"type","location":"api/#Masque.GridWindowEvent","page":"API","text":"GridWindowEvent(layer, i1, i2, j1, j2, xmin, xmax, ymin, ymax)\n\nA brushed window on a grid. Ranges are 1-based inclusive: A[win] is A[win.i1:win.i2, win.j1:win.j2]. A miss has an empty i1:i2 (and j1:j2).\n\n\n\n\n\n","title":"Masque.GridWindowEvent"},{"category":"type","location":"api/#Masque.AxisEvent","page":"API","text":"AxisEvent(layer, x, y)\nAxisEvent(layer, x, y, xcat, ycat)\n\nAn axis click at data coordinates (x, y). Not an array index. On a categorical dimension the coordinate is the category's position (Makie places categories at 1:n), and xcat / ycat holds its label; on a numeric dimension that field is nothing.\n\n\n\n\n\n","title":"Masque.AxisEvent"},{"category":"type","location":"api/#Masque.ThresholdEvent","page":"API","text":"ThresholdEvent(layer, value[, category])\n\nA threshold drag release at data coordinate value. Pass back as value= to restore the line. Along a categorical dimension, value is the category's position (Makie places categories at 1:n) and category holds its label; otherwise category is nothing.\n\n\n\n\n\n","title":"Masque.ThresholdEvent"},{"category":"type","location":"api/#Masque.ColorbarEvent","page":"API","text":"ColorbarEvent(layer, value)\n\nA colorbar click at data coordinate value.\n\n\n\n\n\n","title":"Masque.ColorbarEvent"},{"category":"type","location":"api/#Masque.BoundsEvent","page":"API","text":"BoundsEvent(layer, xmin, xmax, ymin, ymax)\n\nA bounds-only ROI release. Pass back as bounds= to restore the box. A selects ROI is not a BoundsEvent.\n\n\n\n\n\n","title":"Masque.BoundsEvent"},{"category":"function","location":"api/#Masque.bondtype","page":"API","text":"bondtype(interactable) -> Type\n\nThe type of one commit from this interactable. Default ElementEvent. When the same widget carries a selects ROI aimed at this interactable's layer, that layer commits no clicks at all: the box owns the bond. That is a property of the call, not of the layer's bondtype.\n\n\n\n\n\n","title":"Masque.bondtype"},{"category":"function","location":"api/#Masque.transform_bond","page":"API","text":"transform_bond(interactable, layer, index, js_payload) -> InteractionEvent\n\nBuild one event from a wire hit. index is already 1-based for an element kind, or nothing for axis / colorbar / threshold / ROI / grid (those read the browser payload). Custom interactables implement this (and bondtype) when the default ElementEvent is not enough; a type that only wants ElementEvent writes hitlayers and inherits the default.\n\n\n\n\n\n","title":"Masque.transform_bond"},{"category":"type","location":"api/#Masque.PointInteractable","page":"API","text":"PointInteractable(ax, points; id=:points, payloads=<auto>, radius=nothing, radius3d=nothing, tooltip=nothing)\nPointInteractable(ax, p::Makie.Scatter; id=:scatter, payloads=nothing, radius=nothing)\nPointInteractable(ax, p::Makie.MeshScatter; id=:meshscatter, payloads=nothing, radius=nothing, radius3d=nothing)\n\nScatter-style points, hit-tested as circles. Produces one :circles HitLayer.\n\nArguments\n\npoints — data-space points, each a 2- or 3-element point/tuple (Axis3 scatters use 3).\nid — the layer id; becomes InteractionEvent.layer on a hit.\npayloads — one entry per point (ArgumentError if the length doesn't match points), or a DataFrame with one row per point once DataFrames is loaded. Default: (; index, x, y), or (; index, x, y, z) for 3-coordinate points — index is 1-based.\nradius — highlight and click-target radius in px (scaled to the rendered image's DPI), the same for every point. Default nothing: use the drawn radius of the one Scatter on ax with these positions (see below). If none matches, or more than one does, assume Makie's default :circle at the theme markersize (radius ≈0.3525×markersize; the theme default markersize is 9). Pass a number to override. A matched marker with no readable bbox (a Char, an image, a per-element vector of markers) keeps the markersize / 2 bound.\nradius3d — per-point data-space half-extents (Vector{Makie.Vec3f}), for markers whose on-screen size is camera/depth-dependent (e.g. meshscatter). When set, overrides radius with an axis-aligned pixel-radius approximation projected per point — it can underestimate the true silhouette (worst case ~29%, at adversarial azimuth/elevation). Must have one entry per point (ArgumentError otherwise).\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError; not meaningful).\nlabel — an optional screen-reader announcement prefix for this layer (e.g. \"Scatter\"), used by the overlay's keyboard navigation (\"Scatter, element 3 of 10: …\"). Default nothing (no prefix). Not the same thing as a label payload key (see the PointInteractable examples elsewhere in this file) — that's per-element tooltip data.\ncolors — an optional tooltip accent colour; see HitLayer's colors field. Default nothing (no accent). Not derived automatically here — only PointInteractable(ax, p::Makie.Scatter) resolves it, from p's own colour.\n\nFrom a plot object\n\nPointInteractable(ax, p::Makie.Scatter) is the usual call. It reads points from p's converted data and derives radius from the marker's drawn extent, not the full markersize square — Makie's default :circle marker draws a disc of diameter ≈0.705·markersize (radius ≈0.3525·markersize), so that's what ships; other default_marker_map() symbols/BezierPaths use their own bbox in the same way, a GeometryBasics Circle/Rect marker draws at the full markersize (radius = markersize / 2), and anything else (a Char glyph, an image, a per-element vector of markers) falls back to markersize / 2 as a conservative bound. This requires markerspace = :pixel (the default); pass radius= explicitly for any other markerspace, or it errors. The points constructor above takes the same radius when exactly one Scatter on ax has the same positions (including a scatterlines!/stem! child scatter); it does not resolve colors. The overlay adds its own hit-test slack on top, so the smaller radius doesn't make small markers harder to click. The scatter constructor also resolves colors from p.color[]: a single colour (including a bare numeric value mapped through colormap) ships as one CSS string; a numeric (colormap-driven) or explicit per-point colour vector ships as a shared palette + one index per point; anything else (e.g. no colour, or unresolvable) omits colors — no accent, not an error. A value outside colorrange is clamped to the nearest palette end rather than resolved through lowclip/highclip, so such a point's accent can differ slightly from its marker's actual (clipped) colour. PointInteractable(ax, p::Makie.MeshScatter) derives radius3d from p's data-space markersize (a Vec3f, a Real, or a per-element vector of either); pass radius=/radius3d= explicitly if it can't be derived.\n\nExamples\n\npts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\nscatter!(ax, first.(pts), last.(pts); markersize = 14)\nPointInteractable(ax, pts)   # radius ≈ 0.3525 * 14, from that scatter\n\np = scatter!(ax, xs, ys; markersize = 14)\nPointInteractable(ax, p)   # the usual call; same radius, and colors from p\n\n\n\n\n\n","title":"Masque.PointInteractable"},{"category":"type","location":"api/#Masque.SegmentInteractable","page":"API","text":"SegmentInteractable(ax, vertices; mode=:polyline, unit=:segment, id=:segments, payloads=nothing, tol=6, tooltip=nothing)\nSegmentInteractable(ax, p; id=<kind-specific>, payloads=nothing, tol=6)   # from a plot object\n\nLines / polylines or disjoint segment pairs. Produces one :polyline, :lines, or :segments HitLayer.\n\nArguments\n\nvertices — data-space points, each a 2- or 3-element point/tuple.\nmode — :polyline (default): vertices is one connected path. :pairs: vertices is disjoint pairs (v1,v2), (v3,v4), …, length(vertices) ÷ 2 segments. Any other value raises ArgumentError.\nunit — what one element is. :segment (default): a :polyline path is hit per edge (length(vertices) - 1 elements) and :pairs is hit per pair. :line: the whole path is one element (kind :lines) — the pointer still has to land within tol of some edge, and a NaN gap stays a gap in that one line. :line requires mode = :polyline (ArgumentError otherwise). This is how lines! / stairs! / a scatterlines! line are built; the raw vertex constructor stays per-segment unless you pass unit = :line.\nid — the layer id; becomes InteractionEvent.layer on a hit.\npayloads — one entry per element (per segment when unit = :segment, one entry when unit = :line); ArgumentError if the length doesn't match. Default: (; segment_index) (1-based) for :segment, (; index) (1-based) for :line.\ntol — hit-test slack around a segment, in logical px (scaled to the rendered image's DPI like PointInteractable's radius). Must be finite and positive (ArgumentError otherwise). Shipped in the manifest as a per-layer \"tol\" field; the overlay's client-side default (SEG_TOL in frontend/src/geometry.ts, 8 image px) applies only when this field is absent.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\nlabel — an optional screen-reader announcement prefix for this layer (see PointInteractable). Default nothing.\n\nFrom a plot object\n\nSegmentInteractable(ax, p) reads vertices from p (no mode/unit/tooltip keyword — those are fixed by the plot type):\n\np default id hit vertices from\nMakie.Lines :lines one :lines element, the whole path converted data\nMakie.Stairs :stairs one :lines element, the whole staircase the child Lines' pre-expanded step polyline\nMakie.Series :series one :lines layer, one element per series each child line (or a ScatterLines child's line)\nMakie.LineSegments :segments :pairs, per segment converted data\nMakie.Wireframe :wireframe :pairs, per edge the child LineSegments' edges (incl. mesh-triangulation diagonals)\nMakie.Arrows3D :arrows3d :pairs, per shaft processed startpoints/endpoints (post-align/lengthscale); default payload (; index, x, y, z, u, v, w) from points/directions\nMakie.Errorbars :errorbars :pairs, per bar each bar's low→high endpoints\nMakie.Rangebars :rangebars :pairs, per bar each bar's low→high endpoints\nMakie.HLines :hlines :pairs, per line each line's rendered span (xmin/xmax fractions of the axis; default 0–1 is the full limits; re-resolved on limit changes)\nMakie.VLines :vlines :pairs, per line each line's rendered span (ymin/ymax fractions of the axis; default 0–1 is the full limits; re-resolved on limit changes)\n\nA series! element's default payload is (; index, label) when the child plot's label is a non-empty string (Makie's own default is \"series k\"), otherwise (; index).\n\nExamples\n\np = lines!(ax, xs, ys)\nSegmentInteractable(ax, p)                       # one element: the whole line\n\nSegmentInteractable(ax, [(0,0), (1,1), (2,0)]; mode = :polyline)          # per segment\nSegmentInteractable(ax, [(0,0), (1,1), (2,0)]; mode = :polyline, unit = :line)  # one element\n\n\n\n\n\n","title":"Masque.SegmentInteractable"},{"category":"type","location":"api/#Masque.RectInteractable","page":"API","text":"RectInteractable(ax; rects, id=:rects, payloads=nothing, tooltip=nothing, clamp_to_viewport=false)\nRectInteractable(ax; grid, id=:rects, payloads=nothing, tooltip=nothing)\nRectInteractable(ax, p; id=<kind-specific>, payloads=nothing)   # from a plot object\n\nAxis-aligned rectangles: an explicit list (bars, boxes) or a compact heatmap/image grid. Pass exactly one of rects/grid — passing both, or neither, raises ArgumentError at construction. Produces one :rects or :grid HitLayer.\n\nArguments (list form: rects=)\n\nrects — data-space boxes [(xc, yc, w, h), …] (center + width/height).\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :rects.\npayloads — one entry per rect; ArgumentError if the length doesn't match. Default: (; index), 1-based.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\nclamp_to_viewport — clamp each rect's pixel bounds to the axis viewport (inward rounding, so integer quantization never expands past the edge) before shipping geometry. Used internally by the HSpan/VSpan introspection methods below; rarely needed directly. Default false.\nlabel — an optional screen-reader announcement prefix for this layer (see PointInteractable). Default nothing. Accepted for both forms, but inert on a :grid layer — it's still stored and shipped in the manifest, but has no effect, since a :grid layer is not keyboard-navigable (element count is unbounded).\n\nArguments (grid form: grid=)\n\ngrid — (xedges, yedges, values): xedges/yedges are cell-edge vectors (length ncols+1/nrows+1), values an (ncols, nrows) Matrix of per-cell values. Shape mismatch raises ArgumentError. id/tooltip as above; payloads is unused (cell (i, j, value) is resolved client-side). When a cell is at least one screen pixel, the manifest carries values (row-major). Below that it carries sample: one source value per screen pixel of the axis viewport, the cell under that pixel's center. A pixel whose center misses the grid is NaN and is not a hit. A source cell that is itself NaN, Inf, or missing is still that cell (missing is stored as NaN). A matrix that is not real-valued (a color image!) ships edges only on this branch: the cell index, no numeric value. Clicks still carry that center cell, so A[cell] indexes it.\n\nFrom a plot object\n\nRectInteractable(ax, p) builds rects/grid and default payloads from p:\n\np default id form notes\nMakie.Heatmap / Makie.Image :cells grid edges from converted coordinate/coordinate-free ranges\nMakie.BarPlot :bars list reads the laid-out child Poly (dodge/stack/auto-width honored); payload (; low, high, value)\nMakie.Spy :spy list cell size from the child Scatter's data-space markersize (length-2 vector or scalar; other shapes error)\nMakie.Hist :hist list payload (; value, low, high) (value is a count only for normalization = :none)\nMakie.Waterfall :waterfall list payload (; low, high, value)\nMakie.CrossBar :crossbar list payload (; midpoint, low, high)\nMakie.HSpan :hspan list spans the full x-range of ax's current limits; clamp_to_viewport = true; payload (; low, high) (y-bounds); re-resolved on limit changes\nMakie.VSpan :vspan list spans the full y-range of ax's current limits; clamp_to_viewport = true; payload (; low, high) (x-bounds); re-resolved on limit changes\n\nExamples\n\nRectInteractable(ax; rects = [(0.0, 0.0, 1.0, 1.0)], payloads = [(; label = \"a\")])\n\nxedges = 0:0.5:2; yedges = 0:1:3; vals = rand(4, 3)\nRectInteractable(ax; grid = (xedges, yedges, vals))\n\np = heatmap!(ax, X, Y, Z)\nRectInteractable(ax, p)\n\n\n\n\n\n","title":"Masque.RectInteractable"},{"category":"type","location":"api/#Masque.PolygonInteractable","page":"API","text":"PolygonInteractable(ax, rings; id=:polygons, payloads=nothing, tooltip=nothing, holes=nothing)\nPolygonInteractable(ax, p; id=<kind-specific>, payloads=nothing)   # from a plot object\n\nArbitrary filled polygons, hit-tested even-odd. Produces one :polygons HitLayer.\n\nArguments\n\nrings — Vector{Vector{point}}, one or more rings, each a Vector of 2- or 3-element data-space points/tuples (one polygon per ring; a ring need not be closed — the hit-test closes it implicitly).\nholes — one group of hole rings per element, same point type as rings. nothing (default) means every element is solid. A point inside a hole is not a hit of that element, and the highlight leaves the hole unfilled. The group count must match rings.\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :polygons.\npayloads — one entry per ring; ArgumentError if the length doesn't match. Default: (; index), 1-based.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\nlabel — an optional screen-reader announcement prefix for this layer (see PointInteractable). Default nothing.\n\nFrom a plot object\n\nPolygonInteractable(ax, p) builds rings and default payloads from p:\n\np default id rings from notes\nMakie.Poly :poly converted geometry one ring, or many for a multi-ring Poly\nMakie.Band :band lower curve + reversed upper curve always exactly one open ring\nMakie.Density :density its descendant Band's KDE fill same shape as Band\nMakie.Contourf :contourf each filled polygon's exterior, plus its holes as further rings of the same element payload (; low, high), the band edges nearest each polygon's fill color\nMakie.Violin :violin each violin's outline payload (; x), the nearest category to the ring's geometric center\nMakie.Voronoiplot :voronoiplot each cell's exterior ring cells come back in tessellation order (no cheap cell→generator map), so default payload is (; index) only\n\nExamples\n\nPolygonInteractable(ax, [[(0,0), (1,0), (1,1), (0,1)]])\n\np = poly!(ax, points)\nPolygonInteractable(ax, p)\n\n\n\n\n\n","title":"Masque.PolygonInteractable"},{"category":"type","location":"api/#Masque.AxisInteractable","page":"API","text":"AxisInteractable(ax; id=:axis)\n\nThe whole axis as one hit region: a click or hover anywhere returns the data coordinate under the cursor. No per-element geometry — the browser inverts pixels→data live via the shipped AxisTransform (no Julia round-trip on hover). Produces one :axis HitLayer with geometry = nothing.\n\nArguments\n\nax — a Makie.Axis (linear, log, or categorical). id — the layer id; becomes InteractionEvent.layer on a hit. Default :axis.\n\nPayload on hit (client-side): (; x, y).\n\nmasque raises ArgumentError at build time if ax is an Axis3 (a screen pixel is a ray, not a data point — continuous readout is undefined), a PolarAxis (continuous θ/r inversion isn't shipped to JS yet), or either scale isn't client-invertible (supported: identity, log10, log; categorical axes are fine).\n\nExamples\n\nAxisInteractable(ax)\n\n\n\n\n\n","title":"Masque.AxisInteractable"},{"category":"type","location":"api/#Masque.ColorbarInteractable","page":"API","text":"ColorbarInteractable(cb; id=:colorbar)\n\nA Makie.Colorbar block as one hit region, bounded to its pixel bounding box: a click or hover anywhere on the bar inverts the cursor position to the bar's data value, client-side — like AxisInteractable but scoped to the colorbar and 1-D. Produces one :axis HitLayer with geometry set to the colorbar's pixel bbox.\n\nArguments\n\ncb — a Makie.Colorbar (not an Axis). id — the layer id; becomes InteractionEvent.layer on a hit. Default :colorbar.\n\nPayload on hit (client-side): (; value).\n\nmasque raises ArgumentError at build time if the colorbar's value-axis scale isn't client-invertible (supported: identity, log10, log).\n\nExamples\n\ncb = Colorbar(fig[1, 2], plotobj)\nColorbarInteractable(cb)\n\n\n\n\n\n","title":"Masque.ColorbarInteractable"},{"category":"type","location":"api/#Masque.LegendInteractable","page":"API","text":"LegendInteractable(leg; id=:legend, targets=nothing, events=(:click,:hover), tooltip=nothing)\n\nA Makie.Legend block whose entries are hit regions linking to the plot layer(s) they stand for: hover/click an entry to highlight the layer(s) named in targets. Produces one :rects HitLayer, one rect per legend entry.\n\nArguments\n\nleg — a Makie.Legend.\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :legend.\ntargets — how each entry links to other layers, resolved once at construction:\nnothing (default) — masque(fig) links each entry to the layers of the plots its elements were built with (Makie.get_plots). That lookup runs during auto extraction. Calling LegendInteractable yourself, or a hand-built entry whose elements carry no plots, leaves the link list empty. The entry stays a hit target: hover leaves the other layers as they are, and a click reports a LegendEvent.\na Dict{<:AbstractString} keyed by entry label — Symbol or Vector{Symbol} of layer ids for that entry. A key matching no entry label raises ArgumentError.\na Vector with one entry per legend entry (nothing/Symbol/Vector{Symbol}), in entry order (top-to-bottom, matching leg.entrygroups[] flattened). A wrong-length Vector raises ArgumentError.\nEvery id named here must belong to another layer in the same masque() call whose kind supports pre-highlight (ArgumentError from build_manifest otherwise — see HitLayer's links field). A spec may be a layer id (every element of that layer) or id:k pinning element k (1-based); auto-extracted series! entries use the pin.\ntooltip — nothing (default) and false show no card. The entry's label is already drawn in the row, and a card there covers the entries around it. masque\"...\" shows that template (payload fields: label, group, targets). tooltip = true is rejected (ArgumentError). Focusing an entry still announces its label when no template is set.\nevents — the pointer events this layer responds to. Default (:click, :hover).\n\nA custom legend built from LineElement/MarkerElement/PolyElement without plots= has nothing to auto-link — pass plots= on the element (Makie's own kwarg) or use targets= here.\n\nmasque raises ArgumentError at build time if leg is handed to AxisInteractable, ViewInteractable, ThresholdInteractable, or ROIInteractable instead — those need a Makie.Axis, not a Legend.\n\nExamples\n\nl1 = lines!(ax, xs, ys1; label = \"a\")\nl2 = lines!(ax, xs, ys2; label = \"b\")\nleg = axislegend(ax)\nLegendInteractable(leg)   # hit targets only; `masque(fig)` is what fills in the links\n\nLegendInteractable(leg; targets = Dict(\"a\" => :lines, \"b\" => [:lines_2, :scatter]))\n\n\n\n\n\n","title":"Masque.LegendInteractable"},{"category":"type","location":"api/#Masque.TextInteractable","page":"API","text":"TextInteractable(ax, p::Makie.Text; id=:text, payloads=nothing, tooltip=nothing)\n\nClick-to-pick text labels (from text!/annotation!), hit-tested as bounding-box rects. Has no explicit-geometry constructor — this from-a-plot-object form is the only way to build one. Produces one :rects HitLayer, one box per string.\n\nArguments\n\np — a Makie.Text plot (for annotation!, pass its descendant Text, e.g. via auto_interactables).\nid — the layer id; becomes InteractionEvent.layer on a hit. Default :text.\npayloads — one entry per string; ArgumentError if the length doesn't match. Default: (; text, index, x, y) — text is the string, index 1-based, (x, y) its data-space anchor.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress. tooltip = true is rejected (ArgumentError).\n\nGeometry is each string's axis-aligned bounding box (Makie.string_boundingboxes), not projected data coordinates — a rotated label gets its expanded axis-aligned box. Boxes are read lazily in hitlayers, not at construction, so a TextInteractable can be built before the figure is finalized.\n\nExamples\n\np = text!(ax, \"hello\"; position = (1.0, 2.0))\nTextInteractable(ax, p)\n\n\n\n\n\n","title":"Masque.TextInteractable"},{"category":"type","location":"api/#Masque.ThresholdInteractable","page":"API","text":"ThresholdInteractable(ax; orientation=:horizontal, value, id=:threshold)\n\nA draggable horizontal or vertical line: drag for a live client-side readout, and the pixel position inverts to a data-space scalar via AxisTransform on mouse-up. Produces one :threshold HitLayer.\n\nArguments\n\nax — a Makie.Axis.\norientation — :horizontal (constant-y line, dragged vertically) or :vertical (constant-x line, dragged horizontally). Any other value raises ArgumentError. Default :horizontal.\nvalue — the line's initial data-space position (a y-value for :horizontal, x-value for :vertical). Required, no default.\nid — the layer id; becomes InteractionEvent.layer on commit. Default :threshold.\n\nPayload on commit (client-side): the scalar data coordinate.\n\nmasque raises ArgumentError at build time if ax is an Axis3 (a screen pixel is a ray, not a data value — inversion is undefined), a PolarAxis (continuous inversion isn't shipped), or the dragged axis's scale isn't client-invertible (:horizontal needs the y-scale, :vertical the x-scale; supported: identity, log10, log).\n\nExamples\n\nThresholdInteractable(ax; orientation = :horizontal, value = 5.0)\n\n\n\n\n\n","title":"Masque.ThresholdInteractable"},{"category":"type","location":"api/#Masque.ROIInteractable","page":"API","text":"ROIInteractable(ax; bounds, id=:roi, selects=nothing)\n\nA draggable and resizable rectangle: drag the interior to move it, a corner to resize both edges, or the middle of a side (no drawn grip there) to resize just that one edge; on mouse-up its two opposite pixel corners invert to data-space bounds via AxisTransform. An AbstractSelector — with selects set, it also brushes a compatible layer, reporting the contained elements. Produces one :roi HitLayer.\n\nArguments\n\nax — a Makie.Axis.\nbounds — initial (xmin, xmax, ymin, ymax) in data space. Requires xmin < xmax and ymin < ymax (ArgumentError otherwise); length must be 4 (ArgumentError otherwise).\nid — the layer id; becomes InteractionEvent.layer on commit. Default :roi.\nselects — the id of a :circles or :grid layer to brush: on mouse-up, elements whose geometry falls inside the ROI are reported. masque raises ArgumentError at build time if selects names a layer absent from the same call, or one of an unsupported kind.\n\nPayload on commit (no selects): a BoundsEvent. With selects set, the bond is a Vector{ElementEvent} for a :circles target (one per contained element), or one GridWindowEvent for a :grid target — see InteractionEvent. The box owns that bond: the target layer keeps its hover tooltip but takes no clicks. A click on it passes through to any clickable layer underneath (an AxisInteractable catches clicks anywhere on its axis); with none there, the value stays the brush. bounds= accepts a BoundsEvent or a (xmin, xmax, ymin, ymax) tuple.\n\nmasque raises ArgumentError at build time if ax is an Axis3 (a screen pixel is a ray, not a data point), a PolarAxis (continuous inversion isn't shipped), a categorical axis (bounds need numeric limits), or either scale isn't client-invertible (supported: identity, log10, log).\n\nExamples\n\nROIInteractable(ax; bounds = (0.0, 1.0, 0.0, 1.0))\n\n# brush a scatter layer named :scatter\nROIInteractable(ax; bounds = (0.0, 1.0, 0.0, 1.0), selects = :scatter)\n\n\n\n\n\n","title":"Masque.ROIInteractable"},{"category":"type","location":"api/#Masque.ViewInteractable","page":"API","text":"ViewInteractable(ax; id=:view)\n\nDrag-to-pan (2D Axis) or drag-to-orbit (Axis3). Produces one :view HitLayer covering ax's whole viewport; it sorts after Tier-0 :threshold/:roi layers so an ordinary drag on those wins without a modifier — Shift+drag forces the view gesture even over a ThresholdInteractable/ROIInteractable hit.\n\nArguments\n\nax — a Makie.Axis (pan) or Makie.Axis3 (orbit). id — the layer id, used to route gesture-channel frame requests back to this axis. Default :view.\n\nCommits nothing. A camera is operational state, not an analysis value a notebook reads (docs/dev/architecture/12-gesture-channel.md §12.3) — the bond masque returns never carries a :view InteractionEvent. Drag frames stream over a with_js_link gesture channel instead, on both backends: Julia mutates ax.limits[] (pan) or ax.azimuth[]/ax.elevation[] (orbit) and ships a hit manifest for every frame the camera moves. :cairo ships that frame as a PNG; :webgl ships a freshly serialized scene applied to the canvas already on the page. In-drag frames render at px_per_unit = 1; the release frame renders at the widget's own resolution. Because nothing commits, ax's camera stays wherever the gesture left it until the cell re-runs (a fresh Figure/Axis resets it); to persist a view across re-renders, bind it explicitly with the Ref + @bind pattern (§12.8).\n\nmasque raises ArgumentError at build time if ax is a PolarAxis (continuous θ/r view gestures aren't shipped), a Colorbar's value axis (no pan/orbit view applies), a categorical 2D axis (pan needs numeric limits to shift), or (2D only) either scale isn't client-invertible (supported: identity, log10, log). Axis3 has no scale/categorical restriction — camera angles are read live from ax at render time.\n\nExamples\n\nViewInteractable(ax)\n\n\n\n\n\n","title":"Masque.ViewInteractable"},{"category":"type","location":"api/#Masque.SliceInteractable","page":"API","text":"SliceInteractable(ax; series, orientation=:vertical, crosshair=true, id=:slice, covers=(), tooltip=nothing)\nSliceInteractable(ax, plot; orientation=nothing, crosshair=true, id=:slice, covers=nothing, tooltip=nothing)\nSliceInteractable(ax, plots; orientation=nothing, crosshair=true, id=:slice, covers=nothing, tooltip=nothing)\n\nSample one or more 1-D series at the cursor and show that sample in the tooltip. masque(fig) does not add a slice, and a plot without one draws no hairline. This interactable draws one hair — vertical or horizontal, matching orientation — and a filled dot per series in support. crosshair=false keeps the dots and the tooltip and draws no hair. Hover only — nothing is committed. Produces one :slice HitLayer, which is not a hit target.\n\nArguments\n\nax — a Makie.Axis.\nseries — a vector of (; x, y), each x and y an equal-length vector of reals. Optional id (default :s1, :s2, …), label, and color. For :vertical, x is strictly increasing; for :horizontal, y is. A non-finite probe coordinate starts a new run, and a run of one point cannot be interpolated. A decreasing or repeated probe coordinate raises ArgumentError. A Stairs plot is the exception: its steppoints repeat the probe on each riser, and that repeat is kept.\norientation — :vertical (sample y at the cursor's data x, and draw the vertical hair; the default) or :horizontal (sample x at the cursor's data y, and draw the horizontal hair). On the plot constructor, nothing (the default) follows the plot: a Density or Band with direction == :y is :horizontal, and everything else is :vertical.\ncrosshair — draw that one hair. false leaves the sample tooltip and the dots, with no hair. Default true. A figure with no slice draws no hair either way.\nid — the layer id. Default :slice.\ncovers — layer ids in the same masque call whose hover this slice replaces. Each must be a :polygons or :lines layer (ArgumentError otherwise). While the pointer is over one of them the cursor stays crosshair, the polygon or line highlight is skipped, and the tooltip is the sample. Default () on the series constructor. On the plot constructor, nothing (the default) names that plot's auto-extract layer id (:density, :lines, :series, :stairs, :band, with _2, _3, … when a vector repeats a kind).\ntooltip — what the card says while this slice is the thing under the pointer (a covered layer, or empty axis interior inside at least one series). nothing is the auto table of the live sample (the probe coordinate plus one field per series id), masque\"…\" is a template over those same fields, and false suppresses the card. A marker that is not covered, and a colorbar, keep their own tooltip. tooltip = true is rejected (ArgumentError).\nplot / plots — a Lines, Stairs, Series, Band, or Density, or a vector of those. Vertices are the points those plots already draw. Stairs uses the child line's steppoints, so between risers the sample is constant; at a riser the sample is the y where that riser starts. Density and Band contribute the band's upper curve as drawn. A Band with direction = :y flips its converted edge (Makie swaps only the mesh). A Density with direction = :y already stores Point2(offset + density, x) and is :horizontal; it is not flipped again. A vector becomes one slice; mixed orientations raise ArgumentError unless orientation is passed.\n\nmasque raises ArgumentError at build time if ax is an Axis3 or a PolarAxis, if either scale is not client-invertible (identity, log10, log), if either axis is categorical, or if the figure already has a slice on this axis.\n\nExamples\n\nSliceInteractable(ax; series = [(; id = :wide, x = xs, y = ys), (; id = :narrow, x = xs, y = zs)])\n\nd1 = density!(ax, randn(200))\nd2 = density!(ax, randn(200) .+ 2)\nSliceInteractable(ax, [d1, d2])\n\n\n\n\n\n","title":"Masque.SliceInteractable"},{"category":"type","location":"api/#Masque.RegionInteractable","page":"API","text":"RegionInteractable(ax; regions, payloads, id=:region, tooltip=nothing, events=(:click, :hover))\n\nDeclarative mixed hit regions in data space — circles, rects, and polygons in one call, no JavaScript required. Grouped into up to three HitLayers (one per geometry kind present), so a single call can mix shapes freely.\n\nArguments\n\nregions — a Vector, each element one of:\n(:circle, (cx, cy), r) — r in logical pixels, multiplied by ctx.scaling (not data units)\n(:rect, (cx, cy), w, h) — w, h in data units\n(:polygon, [(x, y), …]) — a ring of points\nAny other first element raises ArgumentError.\npayloads — one entry per region, matched 1:1 by position (ArgumentError on a length mismatch); no auto-generated default (unlike the other built-ins, this keyword is required).\nid — the base layer id. The generated layers are Symbol(id, :_c) (circles), Symbol(id, :_r) (rects), Symbol(id, :_p) (polygons) — only the kinds actually present are emitted. InteractionEvent.layer and selected= keys use these suffixed ids, not id itself. Default :region.\ntooltip — nothing for the auto name/value table (default), masque\"...\" for a template, or false to suppress; applies to every generated layer. tooltip = true is rejected (ArgumentError).\nevents — the pointer events all generated layers respond to. Default (:click, :hover).\n\nExamples\n\nRegionInteractable(\n    ax;\n    regions = [(:circle, (0.0, 0.0), 1.0), (:rect, (3.0, 0.0), 2.0, 1.0)],\n    payloads = [(; label = \"circle\"), (; label = \"rect\")],\n)\n\n\n\n\n\n","title":"Masque.RegionInteractable"},{"category":"type","location":"api/#Masque.FunctionInteractable","page":"API","text":"FunctionInteractable(f; events=(:click, :hover))\n\nFull-control escape hatch for a geometry kind the other built-ins don't express: f(ctx) -> Vector{HitLayer} is called at manifest-build time and its result is used verbatim.\n\nArguments\n\nf — a function (ctx::InteractionContext,) -> Vector{HitLayer}. Project data-space points with data_to_image_px(ctx, ax, point), and look up an axis's transform id with Masque.axis_id(ctx, ax) (not exported) when constructing a HitLayer.\nevents — the pointer events reported by Masque.events(::FunctionInteractable); f is free to give its HitLayers different events per layer if it wants. Default (:click, :hover).\n\nExamples\n\nFunctionInteractable() do ctx\n    q = data_to_image_px(ctx, ax, (1.0, 2.0))\n    [HitLayer(:custom, :circles, [q[1], q[2], 10], [(; label = \"manual\")], Masque.axis_id(ctx, ax), (:click, :hover))]\nend\n\n\n\n\n\n","title":"Masque.FunctionInteractable"},{"category":"type","location":"api/#Masque.Markup","page":"API","text":"Markup\n\nA parsed masque\"...\" tooltip template: ordered segments (each a literal String, emitted verbatim as HTML, or a Field placeholder) plus the unique fields referenced. Built by @masque_str — see that macro for template syntax; construct a Markup yourself only if you need parse_template/check_fields directly.\n\nPass a Markup as an interactable's tooltip= keyword to render it client-side, from the hovered element's payloads[] entry, with no round-trip to Julia. At masque()/build_manifest time every field the template references is checked against the layer's actual payload keys (when payloads are NamedTuples); a missing field raises ArgumentError with a \"did you mean?\" suggestion, before any figure is rendered.\n\n\n\n\n\n","title":"Masque.Markup"},{"category":"macro","location":"api/#Masque.@masque_str","page":"API","text":"masque\"...\"\n\nBuild a Markup tooltip template for an interactable's tooltip= keyword. Placeholders are resolved in the browser, from the hovered element's payloads[] entry — $(field) is not Julia string interpolation, and there is no runtime masque(string) form.\n\nSyntax\n\nEverything outside $(...) is literal HTML, inserted as-is (author-trusted, not sanitized) — escape your own </& as you would in @htl.\n$(field) — the hovered element's payload field field (an identifier), HTML-escaped at hover time.\n$(field:spec) — same, formatted by a d3-format spec before escaping (e.g. $(population:,), $(pct:.1%)). spec is checked structurally at macro-expansion (valid d3-format grammar); its runtime meaning (precision, trim, sign) is the browser's format() to interpret.\n\\$ — a literal dollar sign.\n\nA template with no $(...) at all is a fixed label, e.g. masque\"<em>static</em>\".\n\nErrors\n\nMalformed syntax (a bare $, unbalanced/empty $(), a non-identifier field name, or a structurally invalid d3-format spec) raises TemplateValidationError at macro-expansion — before masque() is ever called, with a caret pointing at the offending span. A field that parses fine but is absent from the payload at masque()/build_manifest time raises ArgumentError instead (see Markup).\n\nSee the Tooltips page of the documentation for the full template/tooltip system (styling, security model, wire format).\n\nExamples\n\ntooltip = masque\"<b>$(name)</b> — $(population:,) people\"\nPointInteractable(ax, pts; payloads = [(; name = \"a\", population = 1200)], tooltip)\n\n\n\n\n\n","title":"Masque.@masque_str"},{"category":"type","location":"api/#Masque.AbstractInteractable","page":"API","text":"AbstractInteractable\n\nSupertype for everything masque can turn into hit-testable JS layers. The built-in kinds (PointInteractable, SegmentInteractable, RectInteractable, PolygonInteractable, AxisInteractable, ColorbarInteractable, LegendInteractable, ThresholdInteractable, ROIInteractable, TextInteractable, ViewInteractable, SliceInteractable, RegionInteractable, FunctionInteractable) cover most needs; implement this interface for anything else.\n\nInterface\n\nOnly hitlayers is exported — every other method below is a non-exported function of the Masque module. Extend them as Masque.validate(::MyType, ctx) = …, etc.; a bare validate(::MyType, ctx) = … at top level defines an unrelated function that masque never calls, and masque(fig, MyType()) will build without error while silently ignoring it.\n\nRequired:\n\nhitlayers(i, ctx::InteractionContext) -> Vector{HitLayer} — see hitlayers (exported).\n\nOptional (default shown; all non-exported — extend as Masque.<name>):\n\nMasque.validate(i, ctx::InteractionContext) -> Union{Nothing,String} — return an error message if i can't be built against ctx (masque raises it as ArgumentError), else nothing. Default: always valid.\nMasque.events(i) -> Tuple — the pointer events this interactable's layer(s) respond to (:click, :hover, :drag). Default: (:click, :hover).\nMasque.tooltip_spec(i) — nothing for the auto name/value table, a Markup (built with masque\"...\") template, or false to suppress. Default: nothing.\nMasque.hoverstyle(i) -> NamedTuple — one (; stroke, width) hover outline style per layer (the manifest ships one style per layer, not per element). Default: (; stroke = nothing, width = 2) — stroke = nothing means the overlay draws its own split highlight: a brightening color-dodge fill plus a flat chrome edge stroke (#7a7a7a on a light figure, #c8c8c8 on a dark one; the stroke is not blended into the mark), instead of a stroke colour, so every layer brightens without Masque resolving the element's colour; a CSS colour string overrides it verbatim for that layer (no blend, single unblended element, the outline is exactly that colour). colors (see HitLayer) no longer affects the hover/selection outline at all — it only drives the tooltip's accent border.\nMasque.hit_tol(i) -> Union{Nothing,Real} — logical-px hit-test slack for :segments/ :polyline/:lines layers, shipped in the manifest as image px (round(Int, hit_tol(i) * ctx.scaling)). nothing (default) omits the field; the overlay then falls back to its own fixed slack. Only SegmentInteractable sets this.\n\nAbstractSelector subtypes additionally implement Masque.selects/Masque.compatible_kinds.\n\n\n\n\n\n","title":"Masque.AbstractInteractable"},{"category":"type","location":"api/#Masque.AbstractSelector","page":"API","text":"AbstractSelector\n\nSupertype for interactables that highlight elements on another layer — today just ROIInteractable's selects mode, brushing a :circles/:grid layer. Subtypes additionally implement these non-exported functions (extend as Masque.selects(::MyType) = …):\n\nMasque.selects(i) -> Union{Nothing,Symbol} — the target layer id, or nothing.\nMasque.compatible_kinds(i) -> Tuple — the target HitLayer.kinds this selector accepts; masque raises ArgumentError at build time if selects names a layer of an unlisted kind.\n\n\n\n\n\n","title":"Masque.AbstractSelector"},{"category":"type","location":"api/#Masque.HitLayer","page":"API","text":"HitLayer\n\nThe unit masque serializes to the browser: one geometry kind for one interactable, plus the data needed to resolve a pointer hit to an element index and its payload. Built by hitlayers; user-facing mainly when writing a FunctionInteractable.\n\nFields\n\nid::Symbol — the layer id; becomes InteractionEvent.layer on a hit.\nkind::Symbol — one of :circles, :polyline, :lines, :segments, :rects, :grid, :polygons, :axis, :threshold, :roi, :view, :slice. geometry's layout depends on it:\n:circles — flat Real[], (cx, cy, r) per element (image px)\n:rects — flat Real[], (cx, cy, w, h) per element (image px)\n:polyline / :segments — flat Real[], (x, y) per vertex — one connected path hit per segment, or disjoint pairs, respectively (image px)\n:lines — Vector{Real}[], one flat (x, y)-per-vertex polyline per element (image px; NaN is a gap inside that line, not another element). A lines! / stairs! / scatterlines! line is one entry; a series! is one entry per series\n:polygons — one element per filled polygon. A solid element is a flat (x, y)-per-vertex ring (Vector{Real}, image px). An element with holes is a vector of those rings instead: the exterior first, then each hole. The overlay even-odd-tests the rings of one element together, so a point in a hole is not a hit of that element.\n:grid — a Dict with \"xedges\", \"yedges\", \"ncols\", \"nrows\", and either \"values\" (the source matrix, when a cell is at least one screen pixel) or \"sample\" (one source value per screen pixel of the axis viewport, when cells are smaller). A sub-pixel matrix that is not real-valued ships neither.\n:axis — nothing (whole-axis readout, AxisInteractable) or flat Real[x, y, w, h] (the colorbar's pixel bbox, ColorbarInteractable); not element-indexed\n:threshold / :roi / :view — a small Dict (orientation/position, drag bbox + hit half-size handle — the overlay paints the grip at a fixed 7 CSS px — or viewport + camera, respectively); not element-indexed\n:slice — a Dict of data-space series to sample at the cursor (SliceInteractable); not a hit target. The overlay hair is drawn only when this layer's crosshair is true, and only the arm named by orientation\npayloads::Vector{Any} — one JSON-serializable entry per element, positional (payloads[k] binds element k); empty for the element-count-free kinds above.\naxis::Symbol — the id of this layer's AxisTransform in InteractionContext.transforms (see axis_id).\nevents::Tuple — the pointer events this layer responds to (:click, :hover, :drag).\nlabel::Union{Nothing,String} — an optional announcement prefix for screen readers (e.g. \"Scatter\"), used by the keyboard-navigation overlay (\"Scatter, element 3 of 10: …\"). Not to be confused with a label payload key (e.g. (; label = \"a\") in the examples below) — that's per-element tooltip data; this is one string per layer. nothing (default) omits it from the manifest.\ncolors — an optional tooltip accent colour for this layer's elements: a single CSS colour string (uniform across the layer), or (; palette, index) with palette::Vector{String} (CSS colours) and one 0-based index per element into it (colormapped/categorical data). nothing (default) omits it from the manifest — no accent border on the tooltip. Built by PointInteractable's plot-object constructor when the source plot's colour is resolvable; not derived automatically for a bare-points/vertices interactable.\nlinks — an optional Vector{Vector{Symbol}}, one entry per element: other layers this element highlights on hover/click (e.g. a legend entry linking to the trace(s) it labels). Each id is a layer id (every element of that layer) or id:k pinning element k (1-based) of that layer — auto-extracted series! legend entries use the pin so each swatch lights one trace. nothing (default) omits it from the manifest. Built by LegendInteractable; every id it names must belong to another layer in the same masque() call whose kind supports pre-highlight (build_manifest raises ArgumentError otherwise, for explicitly-given targets — the auto-extracted path drops an unsupported target with a @warn instead of failing the whole build). An id:k pin whose k is out of range is the same kind of error as an unknown layer.\n\n\n\n\n\n","title":"Masque.HitLayer"},{"category":"type","location":"api/#Masque.InteractionContext","page":"API","text":"InteractionContext\n\nBackend-produced bridge handed to every interactable. project is the backend's data→image-px closure (so projection is not hard-wired to Makie). transforms are serialized to JS; ids maps an axis object to its transform id.\n\n\n\n\n\n","title":"Masque.InteractionContext"},{"category":"type","location":"api/#Masque.AxisTransform","page":"API","text":"AxisTransform\n\nOne axis expressed declaratively, in the artifact's pixel space, so JS can invert pixels↔data for AxisInteractable and live hover-coordinate readout.\n\n\n\n\n\n","title":"Masque.AxisTransform"},{"category":"function","location":"api/#Masque.data_to_image_px","page":"API","text":"data_to_image_px(ctx::InteractionContext, ax, p) -> Point2f\n\nProject a data-space point p (a 2- or 3-element point/tuple) on axis ax to image-pixel coordinates (top-left origin, y-down), applying ax's transform and camera the same way Makie renders it. The one coordinate primitive every AbstractInteractable's hitlayers method calls — never re-derive projection by hand. ax must be a Makie.Axis, Makie.Axis3, or Makie.PolarAxis that is part of the figure masque rendered (this function does not itself validate that — a mismatched ax silently projects against the wrong scene; use axis_id(ctx, ax) if you need the fail-loud registration check).\n\nExamples\n\nq = data_to_image_px(ctx, ax, (1.0, 2.0))   # -> Point2f in image px\n\n\n\n\n\n","title":"Masque.data_to_image_px"},{"category":"function","location":"api/#Masque.hitlayers","page":"API","text":"hitlayers(interactable, ctx::InteractionContext) -> Vector{HitLayer}\n\nBuild the HitLayer(s) an interactable contributes to the manifest — the only method every AbstractInteractable subtype must implement. Project data-space geometry with data_to_image_px(ctx, ax, point); never re-derive projection. Most built-ins return a single HitLayer; RegionInteractable can return several (one per region kind), and FunctionInteractable delegates entirely to a user function of ctx.\n\n\n\n\n\n","title":"Masque.hitlayers"},{"category":"type","location":"api/#Masque.AbstractBackend","page":"API","text":"AbstractBackend\n\nSupertype for a Masque rendering backend. Concrete backends live in package extensions — CairoBackend (static image, from the CairoMakie extension) and WebGLBackend (live browser-GPU canvas, from the WGLMakie extension) — and implement render, context, _ppu, and make_widget. masque resolves one automatically from whichever extension is loaded, or takes one explicitly via its backend= keyword.\n\n\n\n\n\n","title":"Masque.AbstractBackend"},{"category":"section","location":"performance/#Performance","page":"Performance","text":"Masque adds very little to a figure you were going to show anyway, and hovering costs nothing on the Julia side. What does cost something is worth knowing before you point it at a large dataset. This page explains what grows with what; the measured sizes and timings live in the maintainers' performance findings, which are kept up to date as the format changes.","title":"Performance"},{"category":"section","location":"performance/#What-a-widget-sends-to-the-browser","page":"Performance","text":"A Masque widget is two things:\n\nThe picture. With CairoMakie, the figure rendered as an image. Its size depends on the figure's width on the page and on how busy the plot is, not on how many marks are interactive. WGLMakie sends the scene to draw instead of a picture; see Backends.\nThe hit data. Where every interactive mark is, plus its payload. This grows with the number of interactive marks and with the size of each payload.\n\nFor a typical interactive plot both are small and the notebook stays responsive. The picture stays roughly the same size however many marks there are, while the hit data keeps growing with them, so on a plot with very many interactive marks the hit data becomes the larger term and is what makes the notebook feel slow. The findings page has the measured crossover.\n\nHeatmaps are the exception to \"grows with the data\". When a grid's cells are smaller than one screen pixel on its axis, Masque sends one value per screen pixel instead of the whole matrix (see Inspect a grid), so a very large heatmap costs about the same as one that just fills the plot.","title":"What a widget sends to the browser"},{"category":"section","location":"performance/#What-happens-on-each-interaction","page":"Performance","text":"Hover is handled entirely in the browser: no Julia runs, whatever the size of the data.\nA click on a mark, or releasing a box or a threshold, sets the @bind value, and Pluto re-runs every cell that reads it. The time that takes is the time your cells take — including drawing a new figure if one depends on the click. A click on empty space, and the end of a pan or orbit, set nothing and re-run nothing.\nPan and orbit re-render the figure in Julia for each frame while you drag, so they cost one render per frame.","title":"What happens on each interaction"},{"category":"section","location":"performance/#Keeping-large-figures-fast","page":"Performance","text":"Keep payloads to the fields you show. Every field travels with every mark. Put the data you need later in Julia, and look it up with pick.index or an id field.\nUse templates, not per-mark strings. A masque\"...\" template is sent once per layer; a formatted string in every payload is sent once per mark.\nMake fewer marks interactive. Draw all the points, but pass only the interesting ones — outliers, a sample, the current selection — to a PointInteractable.\nRebuild less often. Every upstream change re-renders the figure and re-sends the widget. If a figure is redrawn many times a second, or animates, WGLMakie's live canvas is the better fit; see Backends.\nMind the figure's width. A wider figure is a bigger picture, though it does not change the hit data. masque's max_width keyword (700 pixels by default) caps the display width it renders for; a narrower figure is rendered at its own width.","title":"Keeping large figures fast"},{"category":"section","location":"troubleshooting/#Troubleshooting","page":"Troubleshooting","text":"Each heading is the failed try or the error prefix you searched for. For constructor signatures, see Constructors.","title":"Troubleshooting"},{"category":"section","location":"troubleshooting/#Most-common","page":"Troubleshooting","text":"No Makie backend loaded: see Tried masque with no Makie backend.\nCyclic @bind / selected=pick: see Tried feeding this widget's bond into the same call's selected=.\nHover does not update pick: see Tried reading pick on hover.","title":"Most common"},{"category":"section","location":"troubleshooting/#Errors-from-masque()-/-interactable-constructors","page":"Troubleshooting","text":"","title":"Errors from masque() / interactable constructors"},{"category":"section","location":"troubleshooting/#Tried-masque-with-no-Makie-backend","page":"Troubleshooting","text":"Error prefix: masque(fig) needs a rendering backend loaded\n\nCause: neither CairoMakie nor WGLMakie is loaded in the session.\n\nFix: add using CairoMakie (static PNG, the default) or using WGLMakie (live GPU canvas; experimental) before calling masque. Loading both is fine; unqualified masque then stays on CairoMakie. For more information, see Backends.","title":"Tried masque with no Makie backend"},{"category":"section","location":"troubleshooting/#Tried-tooltip-true","page":"Troubleshooting","text":"Error prefix: tooltip = true is not meaningful\n\nCause: an interactable was built with tooltip = true.\n\nFix: omit tooltip for the auto name/value table (the default), pass masque\"...\" for a template, or tooltip = false to suppress it. true is not one of the three valid forms. For more information, see Tooltips.","title":"Tried tooltip = true"},{"category":"section","location":"troubleshooting/#Tried-payloads-of-the-wrong-length","page":"Troubleshooting","text":"Error prefix: payloads has N entries, expected M\n\nCause: payloads does not have one entry per geometry element. expand_payloads throws that ArgumentError for every length check, including PointInteractable and RegionInteractable. A DataFrame says rows instead of entries.\n\nFix: match lengths 1:1, or omit payloads on constructors that have an auto default.","title":"Tried payloads of the wrong length"},{"category":"section","location":"troubleshooting/#Tried-mode-or-orientation-other-than-the-two-valid-symbols","page":"Troubleshooting","text":"Error prefix: mode must be :polyline or :pairs / orientation must be :horizontal or :vertical\n\nCause: SegmentInteractable(...; mode = ...) or ThresholdInteractable(...; orientation = ...) got an unknown symbol.\n\nFix: use :polyline (connected path) or :pairs (disjoint segments); :horizontal (constant-y, dragged vertically) or :vertical.","title":"Tried mode or orientation other than the two valid symbols"},{"category":"section","location":"troubleshooting/#Tried-unit-:line-with-mode-:pairs","page":"Troubleshooting","text":"Error prefix: unit=:line applies only to mode=:polyline\n\nCause: SegmentInteractable(...; unit = :line) was combined with mode = :pairs (or any mode other than :polyline).\n\nFix: drop unit (the default :segment is one element per edge or pair) or keep mode = :polyline. lines! / stairs! / series! already pass unit = :line.","title":"Tried unit = :line with mode = :pairs"},{"category":"section","location":"troubleshooting/#Tried-ROI-bounds-that-are-not-a-4-tuple-in-order","page":"Troubleshooting","text":"Error prefix: ROIInteractable: bounds must be (xmin, xmax, ymin, ymax) / need xmin < xmax and ymin < ymax\n\nCause: bounds is not a 4-tuple, or the min/max order is backwards.\n\nFix: pass bounds = (xmin, xmax, ymin, ymax) with xmin < xmax and ymin < ymax.","title":"Tried ROI bounds that are not a 4-tuple in order"},{"category":"section","location":"troubleshooting/#Tried-an-unknown-Region-kind,-or-selected-on-the-base-Region-id","page":"Troubleshooting","text":"Error prefix: RegionInteractable: unknown region kind / RegionInteractable: payloads has N entries, expected M\n\nCause: a region tuple's first element is not :circle, :rect, or :polygon, or regions and payloads do not line up 1:1. selected = Dict(:cells => [1]) when id = :cells also fails: Region layers are :cells_c / :cells_r / :cells_p.\n\nFix: check the region tuple shapes against Custom hits. Key selected= on the suffixed ids.","title":"Tried an unknown Region kind, or selected= on the base Region id"},{"category":"section","location":"troubleshooting/#Tried-a-scale-Masque-cannot-invert-in-the-browser","page":"Troubleshooting","text":"Error prefix: is not invertible client-side / needs client-side invertible\n\nCause: AxisInteractable, ColorbarInteractable, ThresholdInteractable, ROIInteractable, SliceInteractable, and 2D ViewInteractable invert a pixel back to a data value in the browser. That works for identity, log10, or log. Any other Makie scale (Makie.pseudolog10, Makie.Symlog10, a custom ReversibleScale) fails loud at masque() time instead of reporting the wrong coordinate. SliceInteractable says \"needs client-side invertible x and y scales\".\n\nFix: switch the axis to a supported scale, or use an element interactable (PointInteractable, SegmentInteractable, …) instead of a continuous-readout one.\n\nThis is not the Axis3 orbit path. ViewInteractable on Axis3 does not invert a pixel to data. Orbit is allowed; a pixel→data error comes from Axis, Threshold, ROI, or Slice, not from View.","title":"Tried a scale Masque cannot invert in the browser"},{"category":"section","location":"troubleshooting/#Tried-AxisInteractable,-ThresholdInteractable,-ROIInteractable,-or-SliceInteractable-on-Axis3","page":"Troubleshooting","text":"Error prefix: continuous pixel→data readout is undefined on an Axis3 / undefined on an Axis3\n\nCause: those four kinds need a 2D pixel→data inverse. A screen pixel on Axis3 is a ray, not a data point.\n\nFix: pick marks with element interactables (points, segments, polygons) on 3D axes. Do not attach Axis, Threshold, ROI, or Slice to Axis3.","title":"Tried AxisInteractable, ThresholdInteractable, ROIInteractable, or SliceInteractable on Axis3"},{"category":"section","location":"troubleshooting/#Tried-ViewInteractable-orbit-on-Axis3","page":"Troubleshooting","text":"Cause: orbit is allowed. ViewInteractable on Axis3 validates and sorts as a :view layer. If you saw a continuous pixel→data error, that error came from Axis, Threshold, ROI, or Slice, not from View.\n\nFix: pass ViewInteractable(ax) for orbit. In-drag frames on both backends need a live kernel. The bond never carries :view. For more information, see Backends.","title":"Tried ViewInteractable orbit on Axis3"},{"category":"section","location":"troubleshooting/#Tried-continuous-θ/r-readout-on-PolarAxis","page":"Troubleshooting","text":"Error prefix: continuous θ/r readout on PolarAxis / PolarAxis view gestures need continuous θ/r / PolarAxis continuous θ/r inversion is not yet shipped\n\nCause: continuous polar inversion is not shipped. AxisInteractable, ThresholdInteractable, ROIInteractable, SliceInteractable, and ViewInteractable on PolarAxis raise ArgumentError.\n\nFix: use element interactables (Scatter, Lines, LineSegments, ScatterLines) for discrete hits. Polar is not WebGL-only.","title":"Tried continuous θ/r readout on PolarAxis"},{"category":"section","location":"troubleshooting/#Tried-heatmap!-or-barplot!-on-PolarAxis","page":"Troubleshooting","text":"Cause: masque(fig) skips those kinds on PolarAxis with @warn (an axis-aligned hit layer would misalign). Explicit AABB constructors can still build and sit in the wrong place.\n\nFix: stick to the polar rows of Recipes masque(fig) extracts, or use a Cartesian Axis.","title":"Tried heatmap! or barplot! on PolarAxis"},{"category":"section","location":"troubleshooting/#Tried-ROI-or-View-pan-on-a-categorical-2D-axis","page":"Troubleshooting","text":"Error prefix: bounds need continuous axes / pan needs continuous numeric axes\n\nCause: ROIInteractable and 2D ViewInteractable pan need numeric axis limits; a categorical axis has none. SliceInteractable also rejects a categorical axis: interpolation needs a numeric coordinate, unlike AxisInteractable, which reads the category. (ViewInteractable orbit on Axis3 is a different gate — that path is allowed.)\n\nFix: use AxisInteractable (reads the category) instead, or pass numeric limits. A slice needs a continuous axis.","title":"Tried ROI or View pan on a categorical 2D axis"},{"category":"section","location":"troubleshooting/#Tried-an-LScene-figure-on-CairoMakie","page":"Troubleshooting","text":"Error prefix: Masque's CairoMakie backend supports Makie.Axis, Makie.Axis3, and Makie.PolarAxis\n\nCause: the figure contains an LScene block. The :cairo backend refuses to render it rather than silently drop it.\n\nFix: restart the session with using WGLMakie instead of CairoMakie — :webgl renders LScene live, with no hit-testing overlay. Masque builds no overlay for LScene on either backend.","title":"Tried an LScene figure on CairoMakie"},{"category":"section","location":"troubleshooting/#Tried-selected-on-a-kind-that-cannot-hydrate","page":"Troubleshooting","text":"Error prefix: does not support pre-highlight / out of range\n\nCause: the layer id does not support highlight in the overlay from selected= (:grid, :axis, :threshold, :roi, :view cannot; only :circles / :rects / :polygons / :segments / :polyline / :lines can), or an index is out of range. Region keys are the suffixed ids, not the base id.\n\nFix: check the layer's kind against Selection. Indices are 1-based and must be in 1:n.","title":"Tried selected= on a kind that cannot hydrate"},{"category":"section","location":"troubleshooting/#An-error-mentioning-\"Makie-internals-changed?\"","page":"Troubleshooting","text":"Cause: an internal-invariant guard tripped. Masque introspects a live Makie plot object's fields, and a Makie / CairoMakie / WGLMakie version bump can move or rename one of them. This is not user misuse.\n\nFix: file an issue with your Makie / CairoMakie / WGLMakie versions; pin to a known-good version in the meantime.","title":"An error mentioning \"Makie internals changed?\""},{"category":"section","location":"troubleshooting/#Not-errors,-but-surprising","page":"Troubleshooting","text":"","title":"Not errors, but surprising"},{"category":"section","location":"troubleshooting/#Tried-loading-neither-backend,-or-both","page":"Troubleshooting","text":"Loading neither raises the ArgumentError earlier on this page. Loading both is fine; unqualified masque defaults to CairoMakie. For more information, see Backends.","title":"Tried loading neither backend, or both"},{"category":"section","location":"troubleshooting/#Tried-CairoMakie.activate!(type-\"svg\")-and-the-widget-is-a-PNG","page":"Troubleshooting","text":"Cause: type = \"svg\" chooses the MIME for a bare Figure. masque draws a PNG on CairoMakie, or a canvas on WGLMakie, and lays the overlay on that picture. save(\"figure.svg\", fig) writes SVG from the file extension.\n\nFix: leave CairoMakie.activate!(type = \"svg\") in place for cells that return a Figure. Write the file with save(\"figure.svg\", fig). Return masque(fig) from the cell that should show the overlay. For more information, see SVG display and files.","title":"Tried CairoMakie.activate!(type = \"svg\") and the widget is a PNG"},{"category":"section","location":"troubleshooting/#Tried-feeding-this-widget's-bond-into-the-same-call's-selected","page":"Troubleshooting","text":"Pluto says: cyclic references.\n\nCause: you passed this @bind value into the same masque(...) call's selected= in one cell. Pluto detects the self-reference and refuses to run the cell.\n\nFix: a click already updates the overlay selection on its own. selected= is only for a starting value your Julia code computes, and that has to come from a cell that does not read this widget's bond. For more information, see Keep a selection when the figure rebuilds in Selection.","title":"Tried feeding this widget's bond into the same call's selected="},{"category":"section","location":"troubleshooting/#Tried-reading-pick-on-hover","page":"Troubleshooting","text":"Cause: hover is overlay-only. The tooltip and highlight run in the browser. They do not assign @bind.\n\nFix: read pick after a click (or Enter on a focused mark). For channel timing, see Overlay, bind, and the host.","title":"Tried reading pick on hover"},{"category":"section","location":"troubleshooting/#Right-click-opens-the-context-menu","page":"Troubleshooting","text":"Right-click the figure to open the context menu on the Cairo image or the WebGL canvas. Control-click does the same on macOS. That press does not start a drag, and @bind stays unchanged. For channel timing, see Overlay, bind, and the host.","title":"Right-click opens the context menu"},{"category":"section","location":"troubleshooting/#Tried-a-click-and-nothing-happened","page":"Troubleshooting","text":"Check, in order:\n\nIs the bond actually read somewhere? @bind pick masque(...) with no cell reading pick looks like nothing happened.\nDid you click empty space? Clicks that do not land on a hit region are a no-op, by design. A miss does not clear the selection.\nIs the element you clicked actually interactive? An unsupported plot type in masque(fig) is skipped with a @warn in the notebook log, not an error, so it silently is not clickable. Polar heatmap / barplot is this skip. The allowlist is Recipes masque(fig) extracts.","title":"Tried a click and nothing happened"},{"category":"section","location":"troubleshooting/#Tried-a-tooltip-and-saw-[object-Object]","page":"Troubleshooting","text":"Cause: the auto-table (or a $(field) template) is rendering a payload value that is itself a nested Dict or array. That stringifies to [object Object] in JavaScript.\n\nFix: flatten the field to a scalar (string or number) in Julia before it goes into payloads, or reference a pre-formatted string field with masque\"$(that_field)\".","title":"Tried a tooltip and saw [object Object]"},{"category":"section","location":"troubleshooting/#Tried-a-window-resize-and-the-overlay-looked-misaligned","page":"Troubleshooting","text":"Cause: almost always a px_per_unit / max_width mismatch between what was rendered and what the display shows. Hit-testing re-reads the element's actual on-screen size at runtime, so page zoom or window resizing after the widget mounted is not the cause.\n\nFix: re-run the cell that calls masque(...). If the misalignment persists, check that max_width on masque or the explicit backend struct matches the column width you expect.","title":"Tried a window resize and the overlay looked misaligned"},{"category":"section","location":"troubleshooting/#Tried-reading-pick-after-a-pan-or-orbit","page":"Troubleshooting","text":"Cause: ViewInteractable commits nothing. The bond does not change when you drag the view.\n\nFix: do not read @bind for camera state. Live frames on both backends use with_js_link.","title":"Tried reading pick after a pan or orbit"},{"category":"section","location":"troubleshooting/#Browser-console-errors","page":"Troubleshooting","text":"Open the browser developer-tools console. A serialization error there on :webgl usually means the installed WGLMakie version is outside Masque's pinned compat range — see Backends. Any other console error alongside a widget that otherwise renders is worth reporting as a bug rather than assuming it is expected.","title":"Browser console errors"},{"category":"section","location":"troubleshooting/#Tried-WGLMakie-and-the-spinner-never-stops","page":"Troubleshooting","text":"Cause: the cell displayed a WGLMakie Figure. Its MIME show waits for a Bonito session Pluto never starts, so the cell hangs on .wglmakie-spinner.\n\nFix: return masque(f) from the construction cell. That cell is already the overlay. @bind is optional. A trailing ; is only so Pluto does not show the widget twice. For the cell layout, see The widget is the figure.","title":"Tried WGLMakie and the spinner never stops"},{"category":"section","location":"troubleshooting/#Tried-WGLMakie-and-the-canvas-is-blank","page":"Troubleshooting","text":"Cause: typically a version mismatch between the installed WGLMakie and the one Masque's :webgl extension was verified against, or the figure has no supported plot for masque(fig) to introspect (zero-config on an empty Axis3 renders the canvas but nothing is interactive — that is expected).\n\nFix: check the WGLMakie compat bound in Project.toml; confirm the figure actually has a plot call in it before masque(fig). Drag-orbit on :webgl streams live scene frames; see Backends.","title":"Tried WGLMakie and the canvas is blank"},{"category":"section","location":"troubleshooting/#A-WebGL-plot-says-its-GPU-context-was-released","page":"Troubleshooting","text":"Cause: the browser caps how many WebGL contexts a page can keep (16 on desktop Chrome and Safari, 8 on Android Chrome). Masque keeps at most 8. A plot that scrolls out of view releases its context before the next plot takes one. More than 8 plots on screen at once cannot all be live; the extras show this note. Hover and @bind still work.\n\nFix: scroll so fewer :webgl plots are on screen at once, or use :cairo for a plot that is a static picture.","title":"A WebGL plot says its GPU context was released"},{"category":"section","location":"gallery/polygons/#Polygons","page":"Polygons","text":"masque(fig) also picks up filled areas: band!, density!, contourf!, violin!, voronoiplot!, and boxplot!. Hover a region to read its payload. Nothing here is wired with PolygonInteractable.\n\nFor a poly! you authored as a ring, see Click marks.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-polygons\" data-masque-embed=\"gallery_polygons\" title=\"Polygons\" style=\"width:100%;height:1480px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHover a band, a density, a filled contour, a violin, a voronoi cell, or a box.Draw the panels the way you already draw them. masque makes each filled shape clickable.begin\n    rng = Xoshiro(1)\n    pf = Figure(size = (900, 520))\n    band!(Axis(pf[1, 1]; title = \"band\"), 1:10, (1:10) .- 0.5, (1:10) .+ 1.5)\n    density!(Axis(pf[1, 2]; title = \"density\"), randn(rng, 400))\n    contourf!(\n        Axis(pf[1, 3]; title = \"contourf\"),\n        1:20,\n        1:20,\n        [sin(i / 3) * cos(j / 3) for i in 1:20, j in 1:20];\n        levels = 6,\n    )\n    violin!(Axis(pf[2, 1]; title = \"violin\"), repeat([1, 2, 3], inner = 100), randn(rng, 300))\n    voronoiplot!(Axis(pf[2, 2]; title = \"voronoi\"), rand(rng, 15), rand(rng, 15))\n    boxplot!(Axis(pf[2, 3]; title = \"boxplot\"), repeat([1, 2], inner = 120), randn(rng, 240))\n    nothing\nendHover stays on the figure. This page does not re-run a readout.@bind pick masque(pf)(Image: The figure before any interaction)\n\n","title":"Polygons"},{"category":"section","location":"gallery/polygons/#Variations","page":"Polygons","text":"Pass the rings yourself when the geometry did not come from one of those recipes:\n\nPolygonInteractable(\n    ax, rings;\n    id = :regions,\n    payloads = [(; index = k, shape = \"ring$k\") for k in eachindex(rings)],\n)\n\nThe bond is an ElementEvent with index and the payload fields.\n\nnote: Note\nThese filled-area recipes extract the same way on :cairo and :webgl. This player is the Cairo figure. Hover is overlay-only on the static page. A manual PolygonInteractable is the same hit on both backends. heatmap! on a PolarAxis is skipped with @warn; it is not a polygon layer.","title":"Variations"},{"category":"section","location":"gallery/image/#Image-ROI","page":"Image ROI","text":"The figure is an RGB image. The box selects a window of cells, not a list of pixels. On release the bond is one GridWindowEvent: 1-based inclusive i1:i2 and j1:j2, plus the data bounds. R[region] is R[region.i1:region.i2, region.j1:region.j2]. The image array stays in Julia.\n\nThe grid layer exists so the brush has cells to land on. The box is the outline; the enclosed block is fill-only. See Inspect a grid and Brush a region.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-image\" data-masque-embed=\"gallery_image\" title=\"Image ROI\" style=\"width:100%;height:1560px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nDrag the box over the image. In your notebook, the last cell slices the arrays inside the box when you release.Draw the image, and brush it with a box that selects the pixel grid.begin\n    nx, ny = 96, 64\n    clamp01(v) = clamp(v, 0.0, 1.0)\n    R = clamp01.([0.5 + 0.45 * sin(i / 9) * cos(j / 7) for i in 1:nx, j in 1:ny])\n    G = clamp01.([0.5 + 0.4 * cos(i / 11) for i in 1:nx, j in 1:ny])\n    B = clamp01.([0.5 + 0.45 * (j / ny) for i in 1:nx, j in 1:ny])\n    rgb = [RGBf(R[i, j], G[i, j], B[i, j]) for i in 1:nx, j in 1:ny]\n    fig = Figure(size = (560, 400))\n    ax = Axis(fig[1, 1]; title = \"drag the box to select a region\")\n    image!(ax, 0 .. Float64(nx), 0 .. Float64(ny), rgb)\n    xe = collect(0.0:1.0:nx)\n    ye = collect(0.0:1.0:ny)\n    lum = [0.299 * R[i, j] + 0.587 * G[i, j] + 0.114 * B[i, j] for i in 1:nx, j in 1:ny]\n    ints = [\n        RectInteractable(ax; grid = (xe, ye, lum), id = :img),\n        ROIInteractable(ax; bounds = (10.0, 40.0, 10.0, 40.0), selects = :img),\n    ]\n    nothing\nend@bind region stores the window when you release. Dragging does not re-run the cell below until then.@bind region masque(fig, ints)(Image: The figure before any interaction)This cell reads region, so it re-runs when you release the box.if region === nothing || isempty(region.i1:region.i2)\n    \"Adjust the box. A live notebook slices the array on release.\"\nelse\n    Rs = vec(R[region.i1:region.i2, region.j1:region.j2])\n    Gs = vec(G[region.i1:region.i2, region.j1:region.j2])\n    Bs = vec(B[region.i1:region.i2, region.j1:region.j2])\n    q(v) = round(quantile(v, 0.5); digits = 3)\n    \"n=$(length(Rs))  median R $(q(Rs))  G $(q(Gs))  B $(q(Bs))\"\nendAdjust the box. A live notebook slices the array on release.\n\n","title":"Image ROI"},{"category":"section","location":"gallery/image/#Variations","page":"Image ROI","text":"The readout counts the pixels in the box and prints the median of each channel. That cell runs once per committed box, not per pointer frame. A miss is a GridWindowEvent whose i1:i2 is empty, not [].\n\nnote: Note\nAn image brush is the same event on :cairo and :webgl, on a 2D axis. Axis3 and PolarAxis raise ArgumentError. This player shows the resting box on the Cairo figure. It does not scrub windows: every grid brush shares one snapshot key, so a static page cannot list two different rectangles.","title":"Variations"},{"category":"section","location":"constructors/#Constructors","page":"Constructors","text":"Read Getting started before this page. Lookup for built-in constructors: the signature, the @bind type, the HitLayer kind, and the Guide that teaches the job. Signatures come from src/interactables.jl and src/introspect.jl. This page has no player.\n\nEvery constructor takes an Axis (or a Makie.Colorbar / Makie.Legend) and geometry in data space, plus id — the Symbol the event reports as layer. Element constructors also take payloads (a vector or a DataFrame, except heatmap/image) and tooltip (nothing / masque\"...\" / false) unless the plot-object method omits them. Omit payloads and the default is a 1-based index plus the coordinates that constructor ships. LegendInteractable takes tooltip and a fixed payload; it does not take payloads=. Whole-axis and drag constructors take id plus their own keywords; payloads= / tooltip= is a MethodError. FunctionInteractable takes neither an axis nor id. RegionInteractable requires payloads.","title":"Constructors"},{"category":"section","location":"constructors/#Bond","page":"Constructors","text":"The @bind value is nothing until the first commit of that interaction, unless selected= restored one. bondtype is a property of the interactable (and of selects on an ROI). Indices are 1-based. Named fields on the event are how you read the pick. The wire stays 0-based.\n\nInteraction Value Reads as Guide\nClick a point, bar, polygon, segment, or text ElementEvent pick.city, pick.index; xs[pick], df[pick, :] Getting started, Click marks\nLegend entry LegendEvent entry.label; not a table row Legend\nselects over points Vector{ElementEvent} e.city; empty box [] Brush a region\nHeatmap / image cell GridCellEvent pick.i, pick.j; A[pick] Inspect a grid\nHeatmap / image brush GridWindowEvent i1:i2, j1:j2; A[win] Inspect a grid\nAxis click AxisEvent pick.x, pick.y Read coordinates\nColorbar click ColorbarEvent pick.value Read coordinates\nThreshold release ThresholdEvent pick.value Read coordinates\nBounds-only ROI BoundsEvent pick.xmin … pick.ymax Brush a region\nView pan / orbit none the bond does not change Pan and orbit\n\nA widget that mixes two of these holds whichever event came last. The exception is a selects ROI: the box owns the value, so the layer it selects shows tooltips but commits no clicks. Clicks on other layers stay single events. See Concepts. ViewInteractable is not in that list: a camera is operational state. For selected=, see Selection.","title":"Bond"},{"category":"section","location":"constructors/#Zero-config:-masque(fig)","page":"Constructors","text":"masque(fig) is masque(fig, auto_interactables(fig)) after layout. auto_interactables walks every Axis, Axis3, and PolarAxis plot it knows, plus every Colorbar and Legend. It does not install AxisInteractable, ThresholdInteractable, ROIInteractable, ViewInteractable, or SliceInteractable.\n\n@bind pick masque(fig)\n\nints = auto_interactables(fig)\n\nGrab that vector, tweak ids or payloads, append a custom interactable, and pass it back:\n\nints = let\n    ints = auto_interactables(fig)\n    push!(ints, RegionInteractable(ax; regions = ..., payloads = ...))\n    ints\nend\n\n@bind pick masque(fig, ints)\n\nUnsupported plots are skipped with @warn, not an error. An empty figure warns \"overlaying nothing\". Layer ids are the plot kind (:scatter, :bars, :cells, …), suffixed _2, _3 when a kind repeats. Heatmap and image share :cells. BarPlot is :bars, not :barplot. LineSegments is :segments. pick.layer is that id.\n\nOn Axis3 and PolarAxis, auto skips kinds that would misalign. The allowlist is Recipes masque(fig) extracts. Explicit AABB constructors can still build on those axes and sit in the wrong place; auto is the safe path.\n\nStem and ScatterLines become two layers from _construct only (suffixes :stem_stems and :scatterlines_line). There is no public plot-object constructor for Stem, ScatterLines, BoxPlot, or Annotation, and no id_stems / id_line keyword.\n\nZero-config uses the Scatter plot-object constructor, so the highlight in the overlay hugs the drawn marker. PointInteractable(ax, points) with radius omitted uses that same radius when exactly one Scatter on ax has those positions in the same order. Otherwise it uses Makie's default :circle at the theme markersize. Pass radius= when that lookup would be ambiguous, or when there is no scatter to hug. A marker with no readable bbox (a character, an image) keeps markersize / 2. Default :circle → r ≈ 0.3525 × markersize. A Circle or Rect sprite → r = markersize / 2. The quick start on Getting started passes the scatter, so the radius comes from the marker.\n\nOn huge data, auto allocates one default payload per element. Pass a lean payloads= (or skip the layer) yourself. Auto-extracted layers do not take tooltip= or label=; build an explicit interactable for those. max_width defaults to 700 (Pluto's column). Do not deepcopy(fig) (Makie Figures cannot). Do not add a second @bind pick cell.\n\nPointInteractable(ax, p::Makie.Scatter; tooltip = masque\"…\") is a MethodError. Plot-object constructors that take payloads still do not take tooltip= except TextInteractable. tooltip = true raises ArgumentError. For more information, see Tooltips.\n\nLegendInteractable from auto receives plotmap so entries link to traces. LegendInteractable(leg) with no plotmap / targets= has empty links, stays hittable, and does not resolve plots on its own.","title":"Zero-config: masque(fig)"},{"category":"section","location":"constructors/#From-a-plot-object","page":"Constructors","text":"Pass the plot object a plot! call returns. The geometry is pulled from it. The result is the same interactable the explicit constructor would build, so payloads, selected=, and tooltips still apply. ax is required: a plot has no back-reference to its axis.\n\nbegin\n    p = scatter!(ax, xs, ys; markersize = 14)\n    pt = PointInteractable(ax, p)   # radius from the marker's drawn extent\nend\n\n@bind pick masque(fig, pt)\n\nid and payloads take the same keywords as the explicit constructors. Defaults are the plot's name in lowercase (:scatter, :lines, :hist, …), except Heatmap/Image (:cells), BarPlot (:bars), and LineSegments (:segments). masque(fig) uses those same ids. pick.layer === :heatmap never matches. The Heatmap/Image method takes no payloads keyword: cells resolve {i, j, value} client-side, same as grid = (...). That layer is :grid, so selected= cannot hydrate it.","title":"From a plot object"},{"category":"section","location":"constructors/#Element-constructors","page":"Constructors","text":"Constructor Signature Bond Kind Guide\nPointInteractable (ax, points; radius=nothing, radius3d=nothing, id=:points) or (ax, p::Scatter; id=:scatter) ElementEvent: 1-based index, x, y[, z] :circles Getting started, Click marks\nPointInteractable (ax, p::MeshScatter; id=:meshscatter) ElementEvent: 1-based index, x, y, z :circles Backends\nSegmentInteractable (ax, vertices; mode=:polyline, unit=:segment, tol=6, id=:segments) ElementEvent: 1-based segment_index (:segment) or index (:line) :polyline, :lines, or :segments Click marks\nRectInteractable (ax; rects, clamp_to_viewport=false, id=:rects) or (ax, p::BarPlot; id=:bars) ElementEvent: explicit index; BarPlot low, high, value :rects Click marks\nRectInteractable (ax; grid, id=:rects) or (ax, p::Union{Heatmap,Image}; id=:cells) GridCellEvent: 1-based i, j; A[cell]; value when shipped :grid Inspect a grid\nPolygonInteractable (ax, rings; holes=nothing, id=:polygons) or (ax, p::Poly; id=:poly) ElementEvent: 1-based index :polygons Click marks\nTextInteractable (ax, p::Makie.Text; id=:text) only ElementEvent: text, 1-based index, x, y :rects Click marks\n\nmode is :polyline (connected path) or :pairs (disjoint pairs). unit is :segment (one element per edge or pair; the default) or :line (the whole path is one element; requires mode = :polyline). lines! / stairs! / a scatterlines! line already pass unit = :line. tol is hit-test slack in logical px, scaled to DPI like radius (default 6). radius3d is per-point data-space half-extents on a 3D axis and overrides radius. clamp_to_viewport clamps a list rect that spans past the axis edge. holes is one group of hole rings per element, the same point type as rings. Omit it and every element is solid. A point in a hole is not a hit of that element.\n\nPlot-object SegmentInteractable does not take mode, unit, or tooltip; the plot type fixes all three. PointInteractable(ax, p::Scatter) does not take tooltip= (MethodError). Heatmap/image take id only.\n\nselected= hydrates :circles, :rects, :polygons, :segments, :polyline, and :lines. It cannot hydrate :grid. For more information, see Selection.","title":"Element constructors"},{"category":"section","location":"constructors/#Plot-object-defaults","page":"Constructors","text":"Each row is *(ax, p) unless noted. id is the auto layer id. Bond is ElementEvent except Heatmap/Image (GridCellEvent).\n\nPlot Constructor Default fields Kind\nScatter PointInteractable 1-based index, x, y[, z] :circles\nMeshScatter PointInteractable 1-based index, x, y, z; radius3d from data-space markersize :circles\nLines / Stairs SegmentInteractable 1-based index :lines\nSeries SegmentInteractable 1-based index; label when Makie set one :lines\nLineSegments / Errorbars / Rangebars / HLines / VLines / Wireframe SegmentInteractable 1-based segment_index :segments\nArrows3D SegmentInteractable 1-based index, x, y, z, u, v, w :segments\nBarPlot RectInteractable low, high, value (dodge/stack/auto-width honored) :rects\nHist RectInteractable value, low, high :rects\nWaterfall RectInteractable low, high, value :rects\nCrossBar RectInteractable midpoint, low, high :rects\nHSpan / VSpan RectInteractable low, high; clamp_to_viewport = true :rects\nSpy RectInteractable 1-based index :rects\nHeatmap / Image RectInteractable GridCellEvent: 1-based i, j :grid\nPoly / Band / Density / Voronoiplot PolygonInteractable 1-based index :polygons\nContourf PolygonInteractable low, high :polygons\nViolin PolygonInteractable x :polygons\nText TextInteractable text, 1-based index, x, y :rects\nStem auto only points + stems :circles + :segments\nScatterLines auto only points + line :circles + :lines\nBoxPlot auto only q1, median, q3; whiskers and outliers are not hit-tested :rects or :polygons\nAnnotation auto only (inner Text) text fields :rects\n\nannotation! labels are reachable only through that inner Text or masque(fig), never a hand-written TextInteractable. For Axis3 / PolarAxis yes-or-no, and for types that are skipped, see Recipes masque(fig) extracts.","title":"Plot-object defaults"},{"category":"section","location":"constructors/#Axis,-legend,-and-drag","page":"Constructors","text":"Constructor Signature Bond Kind Guide\nAxisInteractable (ax; id=:axis) AxisEvent: x, y :axis Read coordinates\nColorbarInteractable (cb; id=:colorbar) ColorbarEvent: value :axis (bbox) Read coordinates\nLegendInteractable (leg; targets=nothing, id=:legend) LegendEvent: label, group, targets :rects Legend\nThresholdInteractable (ax; orientation=:horizontal, value, id=:threshold) ThresholdEvent: value on release :threshold Read coordinates\nROIInteractable (ax; bounds, selects=nothing, id=:roi) BoundsEvent; with selects, Vector{ElementEvent} or GridWindowEvent :roi Brush a region\nViewInteractable (ax; id=:view) none — commits nothing :view Pan and orbit\nSliceInteractable (ax, plot) or (ax; series, orientation=:vertical, crosshair=true, id=:slice, covers=(), tooltip=nothing) none — hover only :slice Sample a series\n\nvalue= on a threshold accepts a number or a ThresholdEvent. A colorbar click is a ColorbarEvent with pick.value. ColorbarInteractable takes the colorbar and id only. bounds= accepts a 4-tuple or a BoundsEvent.\n\nAxisInteractable, ThresholdInteractable, ROIInteractable, and SliceInteractable are 2D-only: they raise ArgumentError on Axis3 or PolarAxis. They need a linear or log scale. Categorical is fine for axis and threshold, not for ROI or a slice. ViewInteractable raises ArgumentError on polar, a Colorbar, or a categorical 2D axis; Axis3 orbit is allowed. Shift+drag wins over ROI or threshold on the same axis. selects accepts a :circles or :grid layer id only. Colorbar kind is :axis, not :colorbar. Legend kind is :rects.\n\nChanging limits (2D) or azimuth/elevation (Axis3) and rebuilding the widget re-projects the overlay. Dragging with ViewInteractable is different: it commits nothing. The wheel zooms a 2D view about the cursor. The axis frame stays put while the data inside it slides. On both backends, in-drag frames stream over with_js_link. :cairo ships a PNG; :webgl ships a serialized scene onto the canvas already on the page. See Pan and orbit, Limits, and Drag to pan.","title":"Axis, legend, and drag"},{"category":"section","location":"constructors/#Sample-a-series","page":"Constructors","text":"You already drew the line. Pass that plot:\n\ns = lines!(ax, xs, ys)\nprobe = SliceInteractable(ax, s)\n\nHover reads the line at the cursor. @bind does not change: a slice commits nothing. masque(fig) does not add one, so a heatmap, a scatter, an axis readout, and empty space draw no hair.\n\nThe default is one vertical hair, sampling y at the cursor's data x, plus a filled dot on each series in that series' colour. The hair is a quieter grey than the selection edge (#b0b0b0 on a light figure, #929292 on a dark one), at 80% opacity, with a 1.5px fringe in the figure's background. orientation = :horizontal samples x at data y and draws the horizontal hair. crosshair = false keeps the dots and the tooltip and draws no hair. One slice per axis.\n\nWhile the pointer is over a covered layer, or over empty axis interior inside at least one series, the tooltip is that sample. A marker that is not covered, and a colorbar, keep their own tooltip, and a marker turns the hair off.\n\nSliceInteractable(ax, plot) and SliceInteractable(ax, plots) accept Lines, Stairs, Series, Band, and Density. Vertices are the points those plots already draw. Stairs keeps the expanded steppoints, so the sample is constant between risers and, on the riser itself, the y where that riser starts. Density and Band contribute the band's upper curve as drawn: a Band with direction = :y flips the converted edge, and a Density with direction = :y is already stored in drawn coordinates and is sampled horizontally. orientation = nothing on the plot constructor follows that: :y is horizontal, and everything else is vertical.\n\ncovers = nothing on the plot constructor names each plot's auto-extract layer id, numbered inside that call's vector (:lines, then :lines_2 when the vector repeats a kind; also :stairs, :series, :band, :density). It does not count other plots already on the axis, so slicing only the second lines! covers :lines. Pass covers to name :polygons or :lines layers in the same masque call whose highlight this slice replaces.\n\nWithout a plot object, pass the vertices. Each series is (; x, y) plus optional id, label, and color. For a vertical slice, x is strictly increasing; for a horizontal slice, y is.\n\nprobe = SliceInteractable(ax; series = [(; id = :wide, x = xs, y = ys)])\n\ntooltip = nothing is the auto table of the live sample. masque\"…\" is a template over those fields. false suppresses the card.\n\nAxis3, PolarAxis, a categorical axis, and a scale other than identity, log10, or log raise ArgumentError. See Troubleshooting.","title":"Sample a series"},{"category":"section","location":"constructors/#Custom","page":"Constructors","text":"Constructor Signature Bond Kind Guide\nRegionInteractable (ax; regions, payloads, id=:region) ElementEvent per split layer :circles / :rects / :polygons as :id_c / :id_r / :id_p Custom hits\nFunctionInteractable (f; events=(:click, :hover)) the bond type of each layer's kind; default ElementEvent whatever f emits Custom hits\n\nA type of your own implements hitlayers, plus bondtype and transform_bond when the commit is not ElementEvent. See A custom interactable.","title":"Custom"},{"category":"section","location":"constructors/#3D-axes-and-PolarAxis","page":"Constructors","text":"Axis3 gets the point and segment kinds with 3D fields: Scatter carries index, x, y, z. A lines! is one whole-line element whose default payload is {index} (the path is still projected from the 3D vertices). MeshScatter gets depth-correct per-marker hit radii from its data-space markersize (radius3d). Wireframe edges and Arrows3D shafts hit as segments. Geometry is projected once, in Julia, at build time, so it is the same on :cairo and :webgl. Auto-extract allowlists are Recipes masque(fig) extracts. See Backends.\n\nPolarAxis gets the same discrete point and segment overlays on both backends. Continuous θ/r readout is not shipped. AxisInteractable, ThresholdInteractable, ROIInteractable, SliceInteractable, and orbit-mode ViewInteractable do not work on polar. On Axis3, those 2D-only constructors raise ArgumentError; orbit-mode ViewInteractable is allowed. LScene is not supported on either backend. See Troubleshooting.\n\nFor the exported API dump, see API.","title":"3D axes and PolarAxis"},{"category":"section","location":"gallery/boxselect/#Box-select-scatter","page":"Box-select scatter","text":"Drag the box, or a corner grip, and release. The bond is a Vector{ElementEvent}, one event per enclosed point, with that point's payload (group, x, y). An empty box is [], not nothing.\n\nThe readout counts the two groups and the mean of the enclosed coordinates. xs[picks] uses the events as indices. See Brush a region.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-boxselect\" data-masque-embed=\"gallery_boxselect\" title=\"Box-select scatter\" style=\"width:100%;height:1400px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nDrag the box. The last cell counts the points inside, by group.Scatter the points, keep each point's group on the payload, and brush them with a box.begin\n    npts = 80\n    xs = [2 + 6 * (i / npts) + 0.6 * sin(i) for i in 1:npts]\n    ys = [3 + 5 * cos(i / 7) + 0.5 * (i % 5) for i in 1:npts]\n    grp = [iseven(i) ? \"A\" : \"B\" for i in 1:npts]\n    fig = Figure(size = (560, 380))\n    ax = Axis(fig[1, 1]; title = \"drag the box\")\n    s = scatter!(ax, xs, ys; color = map(g -> g == \"A\" ? :steelblue : :darkorange, grp), markersize = 9)\n    pts = PointInteractable(\n        ax, s;\n        id = :pts,\n        payloads = [(; group = grp[i], x = xs[i], y = ys[i]) for i in eachindex(xs)],\n    )\n    roi = ROIInteractable(ax; bounds = (3.0, 6.0, 2.0, 7.0), selects = :pts)\n    nothing\nend@bind picks stores the points inside the box when you release.@bind picks masque(fig, [pts, roi])(Image: The figure before any interaction)This cell reads picks, so it re-runs when you release the box.if picks === nothing || isempty(picks)\n    \"Adjust the box to select points.\"\nelse\n    n = length(picks)\n    na = count(e -> e.group == \"A\", picks)\n    mx = round(sum(xs[picks]) / n; digits = 2)\n    my = round(sum(ys[picks]) / n; digits = 2)\n    \"$(n) points — group A $(na), group B $(n - na); mean ($(mx), $(my))\"\nendAdjust the box to select points.\n\n","title":"Box-select scatter"},{"category":"section","location":"gallery/boxselect/#Variations","page":"Box-select scatter","text":"selects names a :circles or :grid layer in the same masque call. selects = :bars raises ArgumentError. A corner grip resizes both axes. The middle of a side resizes that one axis, with no grip drawn there. Shift+drag pans instead, when a ViewInteractable is on the same axis.\n\nnote: Note\nThe brush is the same on :cairo and :webgl on a 2D axis with scale identity, log10, or log. It raises ArgumentError on Axis3, PolarAxis, and a categorical axis. This player is the Cairo figure. The listed brushes are snapshots, so the counts update without a kernel. A drag you perform yourself on this page moves the box in the overlay and does not recompute the mean.","title":"Variations"},{"category":"section","location":"gallery/pan/#Drag-to-pan","page":"Drag to pan","text":"ViewInteractable on a 2D axis pans while you drag. The wheel zooms about the cursor. The axis frame stays put while the data inside it slides. The bond does not update when the camera moves. A click on a marker still reports that point. The camera is not an analysis value.\n\nIn-drag frames need a live kernel. They travel on with_js_link, not on @bind. See Pan and orbit. The clip is that drag.\n\n<video id=\"masque-gal-pan\" title=\"Drag to pan a 2D scatter\"\n       controls muted loop playsinline autoplay\n       style=\"width:100%;max-width:640px;height:auto;border:0;background:transparent;\"></video>\n<script>\n(function () {\n  var pretty = /\\/$/.test(location.pathname) || /\\/index\\.html$/.test(location.pathname);\n  var el = document.getElementById(\"masque-gal-pan\");\n  if (!el) return;\n  el.src = (pretty ? \"../../assets/\" : \"../assets/\") + \"gallery-pan.mp4\";\n})();\n</script>\n\nbegin\n    data = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0), (4.0, 16.0), (5.0, 25.0), (6.0, 36.0)]\n    fig = Figure(size = (500, 320))\n    ax = Axis(fig[1, 1]; limits = (0, 8, 0, 40), title = \"drag to pan\")\n    s = scatter!(ax, first.(data), last.(data); color = :dodgerblue, markersize = 18)\n    pts = PointInteractable(ax, s)\n    view = ViewInteractable(ax)\n    nothing\nend\n\n@bind pick masque(fig, [pts, view])","title":"Drag to pan"},{"category":"section","location":"gallery/pan/#Variations","page":"Drag to pan","text":"Shift+drag pans when an ROI or a threshold is on the same axis. PolarAxis, a Colorbar, a categorical axis, and a scale other than identity, log10, or log raise ArgumentError at masque time. A second figure whose limits are computed from this bond does not follow the drag: the bond never carries :view.\n\nnote: Note\nThe gesture commits nothing on both backends. On a live kernel, :cairo ships a PNG each frame and :webgl ships a serialized scene onto the canvas already on the page. The clip above is that Cairo drag. Reach for :webgl when those frames have to stay on the GPU.","title":"Variations"},{"category":"section","location":"tooltips/#Tooltips","page":"Tooltips","text":"Hold the pointer over a mark and a card appears above it with that mark's data. The card is built in the browser from the mark's payload, so it appears instantly and never re-runs Julia. You choose one of three things for each interactable: the default table, a template of your own, or no card at all.\n\nIn this notebook each city's card is a template that prints the name in bold and the population with thousands separators, and the card's border takes the marker's colour:\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-tt-template\" data-masque-embed=\"tooltips_template\" title=\"Four-city scatter with templated tooltips\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHold the pointer over a city to read its name and population. Click one and the last cell names it.Scatter the cities the way you already would. The tooltip template reads city and pop off each point, and the border uses the same palette as the markers.begin\n    cities = [\n        (city = \"Tokyo\", pop = 37_400_000, x = 1.0, y = 1.0),\n        (city = \"Delhi\", pop = 32_900_000, x = 2.0, y = 4.0),\n        (city = \"Shanghai\", pop = 28_500_000, x = 3.0, y = 9.0),\n        (city = \"São Paulo\", pop = 22_400_000, x = 4.0, y = 16.0),\n    ]\n    palette = [\"#e6194b\", \"#3cb44b\", \"#4363d8\", \"#f58231\"]\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\")\n    scatter!(ax, [c.x for c in cities], [c.y for c in cities]; color = palette, markersize = 20)\n    tips = PointInteractable(\n        ax, [(c.x, c.y) for c in cities];\n        payloads = cities,\n        colors = (; palette, index = collect(0:(length(cities) - 1))),\n        tooltip = masque\"<b>$(city)</b><br>pop $(pop:,)\",\n    )\n    nothing\nend@bind pick stores a click in pick. Hover updates the card and does not change pick.@bind pick masque(fig, tips)(Image: The figure before any interaction)Before a click, pick is nothing. This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"hover a city, or click one\"\nelse\n    \"$(pick.city) selected\"\nendhover a city, or click one\n\n","title":"Tooltips"},{"category":"section","location":"tooltips/#The-default-card","page":"Tooltips","text":"Leave tooltip out and the card is a small table of the payload's fields. For a scatter whose payloads you did not set, that is the point's index, x, and y; with your own payloads, it is your fields. The table is a good first draft and often all you need.\n\nTwo interactables are exceptions. A legend entry shows no card by default, because its label is already drawn next to the swatch; pass a template if you want one (see Legend). A SliceInteractable shows the series values at the cursor rather than a mark's payload.","title":"The default card"},{"category":"section","location":"tooltips/#Write-a-template","page":"Tooltips","text":"A template is a masque\"...\" string. Each $(field) is replaced by that field of the payload under the pointer:\n\nPointInteractable(ax, s;\n    payloads = cities,\n    tooltip = masque\"<b>$(city)</b><br>pop $(pop:,)\",\n)\n\nIn the template Becomes\n$(field) the payload's field, HTML-escaped\n$(field:spec) the same, formatted with a d3-format spec first — :, adds thousands separators, :.2f two decimals, :.1% a percentage\n\\$ a literal dollar sign\nanything else copied into the card as HTML, so <b> and <br> work\n\n$(field) names a payload field, not a Julia variable, and it cannot be an expression. The template has to work in the browser without Julia — in a static export, for instance — so anything you want to compute goes into the payload first:\n\nrows = [(; city = c.city, density = round(c.pop / c.area; digits = 1)) for c in raw]\n\nBecause the template's own text is inserted as HTML, keep it to markup you wrote. Payload values are always escaped, so data cannot inject markup, but a template like <a href=\"$(url)\"> still trusts whatever URL the data holds.\n\ntooltip = false turns the card off while keeping the hover highlight and the click.","title":"Write a template"},{"category":"section","location":"tooltips/#Mistakes-Masque-catches","page":"Tooltips","text":"A template that cannot be parsed — an expression such as $(pop + 1), an unclosed $(, or a format spec d3 does not understand — is a TemplateValidationError as soon as the cell containing masque\"...\" runs.\n\nA field the payload does not have is caught when masque builds the widget, with a \"did you mean\" suggestion for a close misspelling. That check reads the field names of named-tuple payloads. With a DataFrame or Dict payloads it is skipped, and a misspelled field simply leaves a blank in the card — worth a quick hover after you write the template.","title":"Mistakes Masque catches"},{"category":"section","location":"tooltips/#Where-the-card-appears","page":"Tooltips","text":"The card sits above the mark under the pointer, with a small caret pointing at it, and flips below the mark if it would run off the top of the figure or shifts sideways at the edges. On a line it follows the nearest point of the line. Axis readouts, thresholds, boxes, the view, and a slice have no single mark, so their card follows the pointer instead. When a mark has keyboard focus, the card anchors to it the same way.","title":"Where the card appears"},{"category":"section","location":"tooltips/#Match-a-dark-figure","page":"Tooltips","text":"The card's light or dark theme comes from the figure's own background, not from the browser or operating-system setting, so a dark figure gets a dark card even on a light page:\n\nfig = Figure(size = (560, 360); backgroundcolor = :gray12)\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-tt-dark\" data-masque-embed=\"tooltips_dark\" title=\"Dark-figure scatter with figure-derived tooltip theme\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHold the pointer over a point. The card is dark because the figure is dark. Click a point and the last cell names it.Draw the scatter on a dark figure, and pass that scatter to PointInteractable. The tooltip picks up the figure background and the marker color.begin\n    points = [\n        (label = \"alpha\", x = 1.0, y = 1.0),\n        (label = \"beta\", x = 2.0, y = 2.0),\n        (label = \"gamma\", x = 3.0, y = 1.2),\n    ]\n    fig = Figure(size = (560, 360); backgroundcolor = :gray12)\n    ax = Axis(\n        fig[1, 1];\n        backgroundcolor = :gray20,\n        xtickcolor = :gray80,\n        ytickcolor = :gray80,\n        xticklabelcolor = :gray80,\n        yticklabelcolor = :gray80,\n        xlabelcolor = :gray80,\n        ylabelcolor = :gray80,\n        bottomspinecolor = :gray70,\n        leftspinecolor = :gray70,\n        rightspinecolor = :gray70,\n        topspinecolor = :gray70,\n    )\n    s = scatter!(\n        ax, [p.x for p in points], [p.y for p in points];\n        color = [\"#e6194b\", \"#3cb44b\", \"#4363d8\"],\n        markersize = 22,\n    )\n    pts = PointInteractable(ax, s; payloads = points)\n    nothing\nend@bind pick stores a click in pick. Hover updates the card and does not change pick.@bind pick masque(fig, pts)(Image: The figure before any interaction)Before a click, pick is nothing. pick.label is the point's name. This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"hover a point, or click one\"\nelse\n    \"$(pick.label) selected\"\nendhover a point, or click one\n\n\n\nBrowsers too old for CSS relative colours fall back to the system light/dark setting.","title":"Match a dark figure"},{"category":"section","location":"tooltips/#Accent-color","page":"Tooltips","text":"When Masque knows the colour of the mark under the pointer, the card gets a 3px border in that colour; the text stays neutral. The colour comes from scatter!'s color= when you pass the Scatter itself to PointInteractable (as the dark example does), from the colors= keyword (as the city example does), or from a legend entry's swatch. Without a known colour the border is a plain hairline.\n\nTo change the card's background, text colour, font, or corner radius for a whole widget, pass the tooltip_* keywords to masque, or set the --masque-tip-* CSS properties on the page; see Tooltip chrome.","title":"Accent color"},{"category":"section","location":"gallery/tooltips/#Tooltip-templates","page":"Tooltip templates","text":"Hover a marker. The tooltip is a masque\"...\" template: $(city) is a field on that point's payload, and $(pop:,) is a d3-format spec, so 37000000 reads as 37,000,000. A value that contains HTML is escaped. Markup you write in the template is not.\n\nClick writes @bind. The readout below the figure names the city. Hover does not change the bond.\n\nFor the template rules, see Tooltips.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-tooltips\" data-masque-embed=\"gallery_tooltips\" title=\"Tooltip templates\" style=\"width:100%;height:1320px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHold the pointer over a city, then click it. The last cell names the city.Scatter the cities and give each point a tooltip that reads its name and population.begin\n    cities = [\n        (city = \"Tokyo\", pop = 37_000_000, x = 1.0, y = 1.0),\n        (city = \"Delhi\", pop = 32_000_000, x = 2.0, y = 4.0),\n        (city = \"Shanghai\", pop = 29_000_000, x = 3.0, y = 9.0),\n        (city = \"São Paulo\", pop = 22_000_000, x = 4.0, y = 16.0),\n    ]\n    fig = Figure(size = (500, 350))\n    ax = Axis(fig[1, 1]; title = \"hover a city\")\n    scatter!(ax, [c.x for c in cities], [c.y for c in cities]; color = :crimson, markersize = 20)\n    tips = PointInteractable(\n        ax, [(c.x, c.y) for c in cities];\n        id = :cities,\n        payloads = cities,\n        tooltip = masque\"<b>$(city)</b><br>pop $(pop:,)\",\n    )\n    nothing\nend@bind pick stores the click. Hover updates the card and does not change pick.@bind pick masque(fig, tips)(Image: The figure before any interaction)This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"hover a city, or click one\"\nelse\n    \"$(pick.city) — pop $(pick.pop)\"\nendhover a city, or click one\n\n","title":"Tooltip templates"},{"category":"section","location":"gallery/tooltips/#Variations","page":"Tooltip templates","text":"Leave tooltip unset and the overlay draws a name/value table from the payload. tooltip_bg, tooltip_color, tooltip_caret, and tooltip_radius on masque theme that card for the whole figure:\n\nmasque(\n    fig, ints;\n    tooltip_bg = :midnightblue,\n    tooltip_color = :white,\n    tooltip_caret = false,\n    tooltip_radius = 8,\n)\n\nnote: Note\nThe overlay is the same code on :cairo and :webgl. This player is the Cairo figure: hover is drawn on the page, and each click swaps in a snapshot of the readout. Load WGLMakie alone when you want the figure on a live canvas. Hover, highlight, and the click readout match on both backends.","title":"Variations"},{"category":"section","location":"gallery/selection/#Selection-round-trip","page":"Selection round-trip","text":"Click a point on the left. That index is passed as selected= on a second masque call, which highlights the same mark. The two widgets do not share an overlay. The highlight on the right is in the manifest for that figure.\n\nselected= takes 1-based indices. The readout prints pick.index that way.\n\nDo not pass this widget's own bond into the same masque call. Pluto reports a cycle and does not run the cell. For selected= on one figure, see Selection.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-selection\" data-masque-embed=\"gallery_selection\" title=\"Selection round-trip\" style=\"width:100%;height:780px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a point on the left. The plot on the right marks the same point.Draw the scatter and pass it to PointInteractable. @bind pick stores the click.begin\n    data = [(1.0, 1.0), (2.0, 2.0), (3.0, 1.5), (4.0, 2.5), (5.0, 1.2)]\n    fig_l = Figure(size = (420, 300))\n    ax_l = Axis(fig_l[1, 1]; title = \"click to select\")\n    s_left = scatter!(ax_l, first.(data), last.(data); color = :teal, markersize = 20)\n    left = PointInteractable(ax_l, s_left)\n    nothing\nend@bind pick masque(fig_l, left)(Image: The figure before any interaction)This cell reads pick. The next figure uses that same variable, so it updates with the click.if pick === nothing\n    \"click a point\"\nelse\n    \"point $(pick.index) selected\"\nendclick a pointBuild the second scatter from pick. selected paints the point the click named.begin\n    fig_r = Figure(size = (420, 300))\n    ax_r = Axis(fig_r[1, 1]; title = \"the same point, on the other plot\")\n    s_right = scatter!(ax_r, first.(data), last.(data); color = :teal, markersize = 20)\n    right = PointInteractable(ax_r, s_right)\n    held = pick === nothing ? Int[] : Int[pick.index]\n    nothing\nend@bind _echo masque(fig_r, right; selected = held)\n\n","title":"Selection round-trip"},{"category":"section","location":"gallery/selection/#Variations","page":"Selection round-trip","text":"The player keeps the last click. To accumulate indices across clicks, store them in a Ref in a cell that does not read this @bind, then pass the growing set as selected= on the second widget. A slider that rebuilds the figure drops the highlight unless you pass that set again. See Limits.\n\nnote: Note\nselected= highlights the same way on :cairo and :webgl. This player is the Cairo figure. A live notebook re-runs the second widget when the bond changes; the snapshots here are those re-runs, baked in. There is no same-index link between two scatters beyond the indices you pass yourself.","title":"Variations"},{"category":"section","location":"roi/#Brush-a-region","page":"Brush a region","text":"Drag a box across a plot and release it; the notebook gets what the box covers. That can be the points inside it, a block of heatmap cells, or just the box's coordinates. While you drag, only the box moves — Julia runs once, when you let go.\n\nDrag the box over the stations below. The table lists the ones inside:\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-roi-table\" data-masque-embed=\"roi_table\" title=\"Stations scatter with a region box and listed @bind table snapshots\" style=\"width:100%;height:1400px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nDrag the box over some stations. The last cell lists the ones inside.Draw the scatter the way you already draw it. Pass that scatter to PointInteractable, with the station rows as payloads, so a hover reads the name and the group. ROIInteractable brushes those points.begin\n    samples = [\n        (name = \"North-1\", x = 1.5, y = 8.0, group = \"North\"),\n        (name = \"North-2\", x = 2.5, y = 7.2, group = \"North\"),\n        (name = \"North-3\", x = 2.0, y = 9.0, group = \"North\"),\n        (name = \"Mid-1\", x = 5.0, y = 5.0, group = \"Mid\"),\n        (name = \"Mid-2\", x = 5.8, y = 4.2, group = \"Mid\"),\n        (name = \"Mid-3\", x = 4.5, y = 5.8, group = \"Mid\"),\n        (name = \"South-1\", x = 8.0, y = 1.5, group = \"South\"),\n        (name = \"South-2\", x = 8.8, y = 2.4, group = \"South\"),\n        (name = \"South-3\", x = 7.2, y = 2.0, group = \"South\"),\n        (name = \"East\", x = 9.0, y = 6.5, group = \"East\"),\n    ]\n    group_color = Dict(\n        \"North\" => \"#4363d8\",\n        \"Mid\" => \"#f58231\",\n        \"South\" => \"#3cb44b\",\n        \"East\" => \"#911eb4\",\n    )\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\", limits = (0.5, 10.0, 0.5, 10.0))\n    s = scatter!(\n        ax, [p.x for p in samples], [p.y for p in samples];\n        color = [group_color[p.group] for p in samples],\n        markersize = 18,\n    )\n    pts = PointInteractable(ax, s; id = :pts, payloads = samples)\n    roi = ROIInteractable(ax; bounds = (4.0, 6.5, 3.8, 6.5), selects = :pts)\n    nothing\nend@bind picks stores the stations inside the box when you release. Dragging moves the box and does not change picks.@bind picks masque(fig, [pts, roi])(Image: The figure before any interaction)Before a drag, picks is nothing. An empty box is an empty list. Otherwise picks is the stations inside, and this cell builds the table.if picks === nothing\n    md\"*Drag the box over some stations, then release.*\"\nelseif isempty(picks)\n    md\"*No stations in the box.*\"\nelse\n    rows = samples[picks]\n    md_rows = [\"| Station | x | y | Group |\", \"|---|---:|---:|---|\"]\n    for r in rows\n        push!(md_rows, \"| $(r.name) | $(r.x) | $(r.y) | $(r.group) |\")\n    end\n    Markdown.parse(\"**$(length(rows)) stations**\\n\\n\" * join(md_rows, \"\\n\"))\nend\n\n","title":"Brush a region"},{"category":"section","location":"roi/#Pick-the-points-inside-a-box","page":"Brush a region","text":"An ROIInteractable draws the box; selects names the layer whose marks it collects. Give the points an id so the box can refer to them, and pass both to the same masque call:\n\npts = PointInteractable(ax, s; id = :pts, payloads = samples)\nroi = ROIInteractable(ax; bounds = (4.0, 6.5, 3.8, 6.5), selects = :pts)\n\n@bind picks masque(fig, [pts, roi])\n\nbounds is where the box starts, as (xmin, xmax, ymin, ymax) in data coordinates. After a release, picks is a Vector{ElementEvent} with one event per enclosed point, and the points inside stay highlighted. An empty box gives an empty vector, not nothing, so one isempty check covers it. Each event carries its point's payload (e.name, e.group), and the vector indexes your data directly: samples[picks] is the rows inside the box.\n\nIf your rows are a DataFrame, pass it as payloads — one row per point, in the order you plotted them — and slice it with the events:\n\npts = PointInteractable(ax, s; id = :pts, payloads = df)\n\npicks === nothing || isempty(picks) ? df[1:0, :] : df[picks, :]\n\nThe box owns the value: the points still show their tooltips, but clicking one commits nothing from that layer, so picks stays what the box holds. The click passes through, so a clickable layer underneath (such as an AxisInteractable) still takes it.","title":"Pick the points inside a box"},{"category":"section","location":"roi/#Brush-heatmap-cells","page":"Brush a region","text":"Point selects at a heatmap or image layer instead, and the box returns one GridWindowEvent describing the block of cells it covers: win.i1:win.i2 columns and win.j1:win.j2 rows, so A[win] is that sub-matrix. The box owns the value: clicking a cell outside it shows the tooltip but leaves the value alone, so the value is always a GridWindowEvent. See Inspect a grid.","title":"Brush heatmap cells"},{"category":"section","location":"roi/#Read-the-box-itself","page":"Brush a region","text":"Leave out selects and the value is the box: a BoundsEvent with xmin, xmax, ymin, and ymax. Use this when the region is the result — a time window, a crop, a range to fit over. bounds accepts a BoundsEvent too, so one widget's box can seed another's; passing a widget its own box back would be a cyclic reference in Pluto.","title":"Read the box itself"},{"category":"section","location":"roi/#Moving-and-resizing","page":"Brush a region","text":"Drag inside the box to move it, a corner to resize it in both directions, or the middle of an edge to move just that edge; the pointer changes to show which. If the axis also has a ViewInteractable, a plain drag moves the box and Shift+drag pans the plot. The box cannot be moved with the keyboard.","title":"Moving and resizing"},{"category":"section","location":"roi/#Where-it-works","page":"Brush a region","text":"The box needs a 2D Axis with numeric limits and a scale the browser can invert (identity, log10, or log); on an Axis3, a PolarAxis, or a categorical axis it raises an ArgumentError when masque runs. selects can name a layer of points or a heatmap/image, and that layer must be in the same masque call; bars and lines cannot be brushed. See Supported plots and axes.\n\nFor a larger example, Box-select scatter summarizes two groups of points inside the box, and Image ROI brushes an image.","title":"Where it works"},{"category":"section","location":"backends/#Backends","page":"Backends","text":"Masque has two backends. The interaction contract — masque, @bind, InteractionEvent, every interactable — is the same on both. Cost and view preview are not.\n\nFor your first overlay, see Getting started.","title":"Backends"},{"category":"section","location":"backends/#Choose-a-backend","page":"Backends","text":"Load a Makie backend before you call masque.\n\nLoad neither CairoMakie nor WGLMakie: the first masque call raises ArgumentError.\nLoad only CairoMakie: Masque uses the static PNG backend.\nLoad only WGLMakie: Masque uses the WebGL backend.\nLoad both: an unqualified masque(fig) stays on CairoMakie, so a Cairo-baked session is not blocked by a stray using WGLMakie.\n\nbackend= takes an extension instance, not a :cairo or :webgl symbol. CairoBackend and WebGLBackend are not in Masque's exports. Reach them with Base.get_extension when you need a WebGL-only knob such as px_per_unit:\n\nmasque(\n    fig, interactables;\n    backend = Base.get_extension(Masque, :MasqueWGLMakieExt).WebGLBackend(;\n        px_per_unit = 3.0,\n    ),\n)\n\nmax_width on masque applies when the backend is implicit (Pluto's column, in CSS px; default 700). An explicit backend struct uses that struct's own max_width.","title":"Choose a backend"},{"category":"section","location":"backends/#CairoMakie","page":"Backends","text":"using CairoMakie selects it. CairoMakie renders the figure to a PNG once. Masque ships a hit-region manifest with that image. A TypeScript overlay (assets/overlay.js) hit-tests pointer events, draws highlight in the overlay and the tooltip locally, and sends a deliberate click back through @bind.\n\nOne render per masque() call, independent of how many elements are interactive. Cheap for a figure you build once and inspect. Expensive if you re-render every animation frame (each frame re-rasterizes the whole scene).\n\nA static Axis3 figure on CairoMakie is a valid 3D plot. 3D does not require WGLMakie. Scatter on Axis3 commits an ElementEvent with x, y, z. A lines! is one whole-line element whose default payload is {index}. MeshScatter derives radius3d from data-space markersize. Orbit is ViewInteractable on that axis. For which recipes masque(fig) extracts, see Recipes masque(fig) extracts. For in-drag preview, see Pan and orbit preview.","title":"CairoMakie"},{"category":"section","location":"backends/#SVG-display-and-files","page":"Backends","text":"CairoMakie.activate!(type = \"svg\") chooses the picture a bare Figure shows in Pluto, VS Code, and other rich displays. Return the Figure from a cell when you want that display. Return masque(fig) when you want the overlay. The two can share a notebook.\n\nsave(\"figure.svg\", fig) writes an SVG file. The extension sets the format. Call it on the figure when you want that file, including from a cell whose return value is masque(fig).\n\nmasque(fig) draws the picture the overlay sits on. CairoMakie draws a PNG. WGLMakie draws a GPU canvas. Hover highlight, selection, and the tooltip are drawn in the overlay, so they stay sharp when you zoom the page. The picture under them is the PNG or the canvas.\n\nWGLMakie.activate! takes no type. Calling CairoMakie.activate! means CairoMakie is loaded. With both backends loaded, an unqualified masque(fig) uses the PNG. For more information, see Choose a backend.","title":"SVG display and files"},{"category":"section","location":"backends/#WGLMakie","page":"Backends","text":"Status: experimental. Verified end to end in a real Pluto notebook (render, overlay, and the @bind round-trip).\n\nusing WGLMakie (with CairoMakie not loaded) switches masque to a browser-GPU canvas. The overlay sits on top. The masque / @bind API does not change.","title":"WGLMakie"},{"category":"section","location":"backends/#The-widget-is-the-figure","page":"Backends","text":"Return masque(f) from the construction cell. That cell is already a masqued figure. @bind is optional. Displaying a WGLMakie Figure hangs on .wglmakie-spinner: its MIME show waits for a Bonito session Pluto never starts.\n\nfig = let\n    f = Figure()\n    ax = Axis3(f[1, 1])\n    scatter!(ax, randn(200), randn(200), randn(200))\n    masque(f)\nend\n\nA trailing ; is only for @bind, so Pluto does not show the widget twice:\n\nfig = let\n    f = Figure()\n    ax = Axis3(f[1, 1])\n    scatter!(ax, randn(200), randn(200), randn(200))\n    masque(f)\nend;\n\n@bind pick fig\n\nCairoMakie can still @bind pick masque(fig) after displaying a Figure: a PNG show is fine. This layout is the WGLMakie recommendation, and a reasonable Pluto habit on either backend.\n\nLoad WGLMakie for a live GPU canvas: animation, frequent re-renders, or large updating data, where per-frame PNG cost dominates. For a figure you build once and inspect, CairoMakie's static PNG is lighter.\n\nThe WGLMakie JS bundle ships once per notebook. Each extra masque(fig) cell pays for its own scene, not another copy of the bundle. The extension is version-coupled to WGLMakie internals (serialize_scene). Treat a WGLMakie version bump as a re-check, not an automatic upgrade.\n\nAt most 8 :webgl plots hold a live WebGL context. Desktop Chrome and Safari allow 16, Android Chrome allows 8, and Firefox allows several hundred. 8 fits all of them and leaves room for another tab. A plot outside the viewport is not given a context until it scrolls into view, and a plot that leaves the viewport releases its context before the next plot takes one. If more than 8 are on screen together, the extras show a note instead of the browser blanking an arbitrary canvas. :cairo does not use a WebGL context.\n\nusing WGLMakie in a session that already loaded CairoMakie does not switch the PNG. Pick WebGL with backend= as shown earlier, or start a session that loads only WGLMakie.\n\nFor runnable examples, see Examples.","title":"The widget is the figure"},{"category":"section","location":"backends/#Pan-and-orbit-preview","page":"Backends","text":"ViewInteractable commits nothing. The bond never carries :view. Drag is operational camera state, not analysis data.\n\nOn both backends, in-drag frames stream over with_js_link. Julia mutates limits (2D pan or wheel zoom) or azimuth / elevation (Axis3 orbit), recomputes hit regions, and ships a fresh frame. The wheel zooms a 2D view about the cursor. The axis frame stays put while the data inside it slides. :cairo ships a PNG. :webgl ships a serialized scene onto the canvas already on the page. That channel needs a live kernel. It is dead on static export.\n\nViewInteractable on Axis3 is allowed: that is orbit. Polar, Colorbar, categorical 2D, and non-invertible 2D scales raise ArgumentError. LScene: CairoMakie refuses the figure; WGLMakie renders with no overlay.\n\nPersist a camera across remount with an explicit Ref plus rebuild, not with selected=. The Limits shows that pattern. For the cairo in-drag clip and the cells, see Pan and orbit.","title":"Pan and orbit preview"},{"category":"section","location":"backends/#Export-static-HTML","page":"Backends","text":"On either backend, a Pluto notebook exported to static HTML keeps pointer inspection (tooltip and highlight in the overlay): the hit-test manifest is baked in, and so is the base (PNG on :cairo; serialized scene on :webgl, redrawn on the reader's GPU with no Julia server). The page still loads Pluto's frontend from a CDN.\n\nLost on either backend: anything that needs Julia to recompute. A click that updates pick and re-runs downstream cells does nothing without a live kernel. with_js_link view frames die. Inspection-without-kernel is true on both backends.","title":"Export static HTML"},{"category":"section","location":"selection/#Selection","page":"Selection","text":"A click selects a mark: the figure keeps it highlighted, and the @bind variable holds it until something replaces it. This page is about what happens around that moment — what replaces a selection, how to start with one, and how to keep one when the figure is rebuilt.\n\nThe examples assume a scatter of cities drawn into fig, made interactive with cities = PointInteractable(ax, s; id = :cities, payloads = rows), and bound as\n\n@bind pick masque(fig, cities)","title":"Selection"},{"category":"section","location":"selection/#What-replaces-a-selection","page":"Selection","text":"Before the first click pick is nothing. A click on a mark makes pick that mark's ElementEvent, and a click on another mark replaces it: one widget holds one selection, so the last click wins. A click on empty space changes nothing — the highlight stays and no cell re-runs — so a stray click never throws away a choice. With the keyboard, Enter or Space on a focused mark selects it exactly as a click does.\n\nTo pick several marks at once, drag a box instead: an ROIInteractable with selects returns every mark inside it (see Brush a region).","title":"What replaces a selection"},{"category":"section","location":"selection/#Start-with-a-mark-selected","page":"Selection","text":"selected= sets the selection the widget starts with. Indices are 1-based, like pick.index:\n\n@bind pick masque(fig, cities; selected = 1)\n\nThe widget mounts with the first city highlighted, and pick starts as its event rather than nothing, so downstream cells have something to show before anyone clicks.\n\nA few rules follow from \"one widget holds one selection\":\n\nselected = [1, 8] highlights both marks, but pick stays nothing, since a single click value cannot hold two marks. The next click replaces the highlight. (With a selects box in the widget, the value is a vector, and selected = [1, 8] does start as those two events.)\nWhen the widget has more than one layer you could select, name the layer: selected = (; cities = 1) or selected = Dict(:cities => [1, 8]). A bare number is ambiguous there and raises an ArgumentError, as does an index outside 1:n.\nPoints, bars, polygons, lines, and segments can start selected. Heatmap cells, axis readouts, boxes, thresholds, and the view cannot; passing selected= for them raises an ArgumentError.\nFor a RegionInteractable, use the layer ids it creates per shape (:cells_c, :cells_r, :cells_p for a base id :cells); see Custom hits.","title":"Start with a mark selected"},{"category":"section","location":"selection/#Keep-a-selection-when-the-figure-rebuilds","page":"Selection","text":"When a cell upstream of the figure re-runs — a slider changes the data, say — masque builds a new widget, and the selection starts over. That is deliberate. A selection is an index into the data that drew the figure, and after the data changes the same index can point at a different mark; silently carrying it over would highlight the wrong city.\n\nWhen you do want a selection to survive, store the indices you still consider valid in a cell that does not read pick, and pass them back in:\n\nheld = [1, 8]   # computed from your data, not from `pick`\n\n@bind pick masque(fig, cities; selected = held)\n\nYou might try to feed the widget its own value, selected = pick. Pluto refuses that cell with a Cyclic references error, because the cell would both define pick and depend on it. It is also unnecessary: the widget already keeps its highlight between clicks for as long as it exists.\n\nA different widget can take the value, though. Passing one widget's pick as another widget's selected= mirrors a click from one figure into another; Selection round-trip shows it, and Linked views covers driving other plots and tables from a click.","title":"Keep a selection when the figure rebuilds"},{"category":"section","location":"selection/#Highlight-and-value","page":"Selection","text":"The highlight is drawn by the overlay in the browser; the PNG of the figure never changes, and no Julia runs to draw it. It follows the selection, but it is not the same thing in every case: a legend click highlights every mark of the series it labels while pick holds only the legend entry, and a heatmap click highlights one cell. Legend and Inspect a grid describe those cases.","title":"Highlight and value"},{"category":"section","location":"support/#Supported-plots-and-axes","page":"Supported plots and axes","text":"This page is the one place that lists what Masque can overlay and where each interaction works. The guides describe what an interaction does and link here for the limits.","title":"Supported plots and axes"},{"category":"section","location":"support/#Interactions-by-axis","page":"Supported plots and axes","text":"Hovering and clicking marks works on all three axis types, but Axis3 and PolarAxis each build only some plot types (the recipe table below says exactly which). The interactions that turn a pointer position back into a data value — reading coordinates, dragging a threshold or a box, panning — need a flat 2D axis whose scale the browser can invert.\n\nInteraction Axis Axis3 PolarAxis\nHover and click marks every recipe some recipes ³ some recipes ³\nRead (x, y) (AxisInteractable) yes ¹ no no\nDrag a threshold (ThresholdInteractable) yes ¹ no no\nBrush a box (ROIInteractable) yes ² no no\nSample a series (SliceInteractable) yes ² no no\nPan or orbit (ViewInteractable) pan ² orbit no\n\n¹ Scale identity, log10, or log. A categorical axis works too: the hover card shows the category, and the event holds its position with the label alongside (see Read coordinates).\n\n² Scale identity, log10, or log, and numeric (not categorical) limits.\n\n³ Axis3 builds scatter!, meshscatter!, lines!, linesegments!, wireframe!, and arrows3d!. PolarAxis builds scatter!, lines!, linesegments!, scatterlines!, and series!. Every other plot on those axes is skipped.\n\n\"Skipped\" means masque(fig) leaves that plot out with a warning naming it; \"no\" means passing the interactable raises an ArgumentError when masque runs, rather than reporting a wrong coordinate. A ColorbarInteractable reads values on identity, log10, and log colorbars. Other Makie scales, such as Makie.pseudolog10 and Makie.Symlog10, raise ArgumentError for every interaction marked ¹ or ². Legend entries and colorbars are figure-level blocks rather than axis plots, so they work whatever axes the figure holds. LScene is not supported: CairoMakie refuses the figure, and WGLMakie draws it with no overlay. Troubleshooting has each error message and its fix.\n\nKeyboard focus reaches points, bars, polygons, lines, segments, text, and legend entries, but not heatmap cells, axis or colorbar readouts, boxes, thresholds, or the view; see Keyboard and screen readers.","title":"Interactions by axis"},{"category":"section","location":"support/#Recipes-masque(fig)-extracts","page":"Supported plots and axes","text":"auto_interactables walks every axis and applies this table. A recipe missing from the table still contributes each visible child the table already knows, under that child's layer id. The walk stops at the child, so a parent that later gets its own row is not registered twice. A recipe with nothing to contribute is skipped with @warn, and the warning names the recipe. Constructor signatures and default fields stay in Element constructors and Plot-object defaults.\n\nAxis is a 2D Makie.Axis. Colorbar and Legend are figure-content blocks, not scene plots.\n\nRecipe Layer id Kind Axis Axis3 PolarAxis\nscatter! :scatter :circles yes yes yes\nmeshscatter! :meshscatter :circles yes yes —\nlines! :lines :lines yes yes yes\nlinesegments! :segments :segments yes yes yes\nwireframe! :wireframe :segments yes yes —\narrows3d! (Arrows3D) :arrows3d :segments yes yes —\nheatmap! / image! :cells :grid yes — —\nbarplot! :bars :rects yes — —\npoly! :poly :polygons yes — —\nstairs! :stairs :lines yes — —\nseries! :series :lines yes — yes\nerrorbars! :errorbars :segments yes — —\nrangebars! :rangebars :segments yes — —\nhlines! / vlines! :hlines / :vlines :segments yes — —\nspy! :spy :rects yes — —\nhist! :hist :rects yes — —\nwaterfall! :waterfall :rects yes — —\ncrossbar! :crossbar :rects yes — —\nhspan! / vspan! :hspan / :vspan :rects yes — —\nband! :band :polygons yes — —\ndensity! :density :polygons yes — —\ncontourf! :contourf :polygons yes — —\nviolin! :violin :polygons yes — —\nvoronoiplot! :voronoiplot :polygons yes — —\nstem! :stem + :stem_stems :circles + :segments yes — —\nscatterlines! :scatterlines + :scatterlines_line :circles + :lines yes — yes\nboxplot! :boxplot :rects or :polygons (body only) yes — —\ntext! :text :rects yes — —\nannotation! :annotation :rects yes — —\nColorbar (block) :colorbar :axis figure content  \nLegend (block) :legend :rects figure content  \n\nstem! and scatterlines! become two layers. boxplot! hits the box body; whiskers and outliers are not hit-tested. annotation! is the inner Text. text! whose space is not :data is skipped with a specific warning. lines! and stairs! are one whole-line element. series! is one :lines layer with one element per series.\n\ncontourf! is filled levels. A pointer in a hole misses that polygon. It hits another polygon only when one is actually drawn in the hole. An empty hole, including a peak above the top level, hits nothing. hexbin! stays unconstructed: its scatter is data-space. bracket!'s label warns masque: skipping non-data-space text. A non-Text child whose space is not :data is skipped with no warning of its own. Hidden children are not layers. LScene has no overlay; see Troubleshooting. For a type you implement yourself, see Custom hits.","title":"Recipes masque(fig) extracts"},{"category":"section","location":"contributing/#Contributing","page":"Contributing","text":"The overlay and the :webgl shim are one TypeScript project in frontend/. npm run build writes both committed bundles: ../assets/overlay.js (IIFE) and ../assets/masque-webgl.js (ESM).\n\ncd frontend\nnpm ci\nnpm run lint && npm run typecheck && npm test && npm run build\n\nCI is the sole author of those two files (it rebuilds and commits on main). Committing a local build is optional.\n\nJulia tests are split into three GROUPs so each process has a known loaded-backend set (Core = Cairo only; WebGL = WGL first, then both at the end; NoBackend = neither). GROUP defaults to Core:\n\njulia --project=. -e 'using Pkg; Pkg.test()'                 # GROUP=Core\nGROUP=NoBackend julia --project=. -e 'using Pkg; Pkg.test()' # neither backend\nGROUP=WebGL julia --project=. -e 'using Pkg; Pkg.test()'     # WGL then both\n\nJulia code is formatted with Runic (CI enforces it, and checks the whole repo, not only src / test):\n\njulia -e 'using Runic; exit(Runic.main([\"--inplace\", \"src\", \"test\", \"bench\", \"docs\"]))'\n\nThis site is Documenter, built from docs/src/. Maintainer notes live in docs/dev/ and are not part of the Documenter sidebar.\n\njulia --project=docs -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'\njulia --project=docs docs/make.jl\n\nLocal make.jl builds HTML under docs/build/ and skips deploydocs (that runs on CI, which also owns the deploy to GitHub Pages on main and tags).","title":"Contributing"},{"category":"section","location":"contributing/#Live-verification","page":"Contributing","text":"User-facing overlay changes need a live Pluto check on each backend. The playbook is docs/dev/live-interaction-checklist.md.","title":"Live verification"},{"category":"section","location":"gestures/#Overlay,-bind,-and-the-host","page":"Overlay, bind, and the host","text":"Concepts explains hover, clicks, and @bind for everyday use. This page is the detailed version: the overlay, @bind, the gesture channel that streams pan frames, and the docs-site player — for when a hover does not re-run Julia, or a docs embed is not a live notebook.\n\nMasque splits pointer work across four channels: overlay chrome, the @bind bond, the gesture channel, and a downstream Julia cell. Holding the pointer over a mark is not a click. A click is not a pan.\n\nnote: Note\nHover stays in the overlay. A click writes @bind. Pan uses the gesture channel and does not write @bind. This docs site swaps recorded snapshots; it does not run a Julia kernel. A static generate_html export keeps overlay inspection only.\n\nThe three-point scatter on Getting started already shows two of those channels on one plot. Holding the pointer over a point reads its name in the overlay. A click writes sel. This page names every channel so a docs-site embed or a static export is not a Masque bug.","title":"Overlay, bind, and the host"},{"category":"section","location":"gestures/#Pluto-cells-in-these-docs","page":"Overlay, bind, and the host","text":"Paste each snippet into its own Pluto cell. Pluto runs one top-level expression per cell. Wrap multiple statements in begin ... end. Showing fig alone does not mount the overlay; masque returns the HTML that does. For Pkg.add(url=…) and Pluto.activate_notebook_environment, see Install.","title":"Pluto cells in these docs"},{"category":"section","location":"gestures/#One-figure,-one-overlay","page":"Overlay, bind, and the host","text":"At masque() time, one Makie Figure plus its interactables produces a backend image (PNG or GPU canvas) and hit geometry for every axis, then one manifest, then HTML: one image and one overlay. Several axes still share that overlay. They are not several masque calls.\n\nThe overlay is a stateless view: tooltips, highlight in the overlay, and drag chrome. Authoritative analysis state lives in Julia as the @bind bond when a cell reads it.","title":"One figure, one overlay"},{"category":"section","location":"gestures/#Four-channels","page":"Overlay, bind, and the host","text":"These four are separate channels, not speeds of one channel.\n\nOverlay chrome. Tooltips, highlight in the overlay, an ROI box, a threshold line, and a view readout. No Julia round trip.\nBond (@bind). The analysis value a cell reads: a click, an Enter commit, an ROI release, or a threshold release.\nGesture channel (with_js_link). In-drag pan and orbit frames on both backends. Not a bond. Not a faster @bind.\nDownstream Julia cell. Re-runs only when it reads a bond that changed.\n\nA Dash hover callback is a click in Masque. Altair .interactive() pan is overlay motion, not @bind. Holding the pointer over a mark never writes @bind.","title":"Four channels"},{"category":"section","location":"gestures/#Gesture-timing","page":"Overlay, bind, and the host","text":"Holding the pointer over a mark never assigns the bond. The highlight after a click also runs in the overlay. The PNG does not change. An in-drag ROI box or threshold line stays in the overlay until release. A view drag commits nothing: camera state is not analysis data.\n\n<div class=\"masque-diagram\">\n  <img class=\"masque-diagram-light\" src=\"assets/diagrams/channels-timing.svg\"\n       alt=\"Swimlanes for overlay chrome, the bind bond, the gesture channel, and a downstream Julia cell for each gesture: hover tooltip and highlight, click-echo wash, click or Enter commit, ROI or threshold in-drag, ROI or threshold release, view pan or orbit in-drag, view release, and empty-space click.\">\n  <img class=\"masque-diagram-dark\" src=\"assets/diagrams/channels-timing-dark.svg\"\n       alt=\"Swimlanes for overlay chrome, the bind bond, the gesture channel, and a downstream Julia cell for each gesture: hover tooltip and highlight, click-echo wash, click or Enter commit, ROI or threshold in-drag, ROI or threshold release, view pan or orbit in-drag, view release, and empty-space click.\">\n</div>\n<script>\n(function () {\n  var wrap = document.currentScript.previousElementSibling;\n  if (!wrap || !wrap.classList.contains(\"masque-diagram\")) return;\n  var link = document.querySelector('link[href*=\"masque-embed.css\"]');\n  var base = \"assets/\";\n  if (link) {\n    base = (link.getAttribute(\"href\") || \"assets/masque-embed.css\")\n      .replace(/masque-embed\\.css(?:\\?.*)?$/, \"\");\n  }\n  var imgs = wrap.querySelectorAll(\"img\");\n  for (var i = 0; i < imgs.length; i++) {\n    var src = imgs[i].getAttribute(\"src\") || \"\";\n    imgs[i].src = base + src.replace(/^.*?assets\\//, \"\");\n  }\n})();\n</script>\n\nEach pointer gesture uses a different mix of overlay chrome, the @bind bond, the gesture channel, and a downstream Julia cell.\n\nThe following table is the same data, so the figure is not the only source.\n\nGesture Overlay Bond Gesture channel Downstream cell\nHover tooltip / highlight Yes No No No\nClick-echo wash Yes No No No\nClick / Enter commit Echo yes One event (none on a selects target layer; the box owns it) No Re-runs if it reads the bond\nROI / threshold in-drag Box / line moves No No No\nROI / threshold release Echo if selects Bounds, scalar, or Vector No Re-runs\nView pan / orbit in-drag Readout No Yes: new frames No\nView release — Nothing Stops No\nEmpty-space click No Unchanged No No\n\nROI and threshold in-drag stay in the overlay: it already has what it needs, so the drag never leaves the browser. In-drag view frames still leave the overlay: both backends repaint over the gesture channel. CairoMakie ships a PNG; WGLMakie ships a serialized scene onto the canvas already on the page. Neither path writes @bind. On release the channel stops, and the bond still holds nothing from the pan.\n\nA click in empty space does not write the bond and does not clear a selection. Enter or Space on a focused mark commits the same way a click does.\n\nRight-click opens the context menu on the figure: the Cairo image, or the WebGL canvas. Control-click does the same on macOS. That press does not start a drag, and the bond stays unchanged.","title":"Gesture timing"},{"category":"section","location":"gestures/#Overlay,-Julia,-and-the-host","page":"Overlay, bind, and the host","text":"Live Pluto runs every @bind row except view. This docs site does not run those rows live. The quick start on Getting started is a Pluto export of the tutorial notebook, and every click swaps in a recorded readout. An overlay-only player keeps tooltip and highlight chrome. Julia stays at the default bond.\n\n<div class=\"masque-diagram\">\n  <img class=\"masque-diagram-light\" src=\"assets/diagrams/overlay-vs-host.svg\"\n       alt=\"Four hosts compared: live Pluto, a docs player that records every click and listed items, a docs player that is overlay-only, and static generate_html. Hover and click-echo run on every host. Element click bind and listed ROI, axis, or threshold values re-run Julia on live Pluto, swap snapshots on a listed player, stay at the default bond on overlay-only, and are dead on static HTML. Unlisted drags keep chrome. Heatmap inspect goes through the grids player. Cairo view frames use GIF or MP4 on this site, with no bind on view.\">\n  <img class=\"masque-diagram-dark\" src=\"assets/diagrams/overlay-vs-host-dark.svg\"\n       alt=\"Four hosts compared: live Pluto, a docs player that records every click and listed items, a docs player that is overlay-only, and static generate_html. Hover and click-echo run on every host. Element click bind and listed ROI, axis, or threshold values re-run Julia on live Pluto, swap snapshots on a listed player, stay at the default bond on overlay-only, and are dead on static HTML. Unlisted drags keep chrome. Heatmap inspect goes through the grids player. Cairo view frames use GIF or MP4 on this site, with no bind on view.\">\n</div>\n<script>\n(function () {\n  var wrap = document.currentScript.previousElementSibling;\n  if (!wrap || !wrap.classList.contains(\"masque-diagram\")) return;\n  var link = document.querySelector('link[href*=\"masque-embed.css\"]');\n  var base = \"assets/\";\n  if (link) {\n    base = (link.getAttribute(\"href\") || \"assets/masque-embed.css\")\n      .replace(/masque-embed\\.css(?:\\?.*)?$/, \"\");\n  }\n  var imgs = wrap.querySelectorAll(\"img\");\n  for (var i = 0; i < imgs.length; i++) {\n    var src = imgs[i].getAttribute(\"src\") || \"\";\n    imgs[i].src = base + src.replace(/^.*?assets\\//, \"\");\n  }\n})();\n</script>\n\nThe same gesture can run in the overlay, write @bind, swap a recorded snapshot, or stay frozen, depending on the host.\n\nThe following table is that split.\n\nGesture Live Pluto Docs player (every click / listed items) Docs player (overlay-only) Static generate_html\nHover Overlay tooltip and highlight Overlay tooltip and highlight Overlay tooltip and highlight Overlay tooltip and highlight\nClick-echo Highlight in the overlay Highlight in the overlay Highlight in the overlay Highlight in the overlay\nElement click @bind (incl. extra table cells) Bond writes; every cell that reads it re-runs Snapshot swap for every cell in the embed Overlay chrome; Julia stays at the default bond Overlay chrome; @bind dead\nListed ROI items / a few axis or threshold values Bond writes on release Snapshot swap for listed sets or values Overlay chrome; Julia stays at the default bond Overlay chrome; @bind dead\nUnlisted continuous drag Overlay chrome during the drag; Julia on ROI or threshold release; view never writes @bind Overlay chrome; Julia stays on the last listed set Overlay chrome; Julia stays at the default bond Overlay chrome; @bind dead\nHeatmap inspect Overlay tooltip; Julia on click Overlay tooltip; every cell click swaps the readout Overlay tooltip chrome if that player is present Overlay tooltip; Julia click dead\nCairo view frames (GIF/MP4, no @bind) Gesture channel frames; no @bind GIF/MP4 on this site; no @bind Overlay readout; no frames; no @bind Gesture channel dead; site uses GIF/MP4\n\nThe docs build records every element, legend, and grid-cell click a player's figure offers, so those clicks swap the readout. An axis or colorbar click, and a click on a grid that a box brushes, has no recorded snapshot. View pan never appears as an InteractionEvent. For a box that filters a table, see Brush a region. For that heatmap, see Inspect a grid. For pan and orbit, see Pan and orbit.","title":"Overlay, Julia, and the host"},{"category":"section","location":"gestures/#Common-mistakes","page":"Overlay, bind, and the host","text":"Do not write a cell that reads pick expecting it to update when you hold the pointer over a mark. That path is overlay-only.\nDo not treat a docs player as live Pluto. Axis, ROI, and threshold commits write @bind in a notebook. On this site, a listed player snapshots those commits, and view has no @bind.\nDo not design a click demo around more marks than a player can record. The docs build records every element, legend, and grid-cell click and fails a player over its size budget; use a coarser grid or fewer marks.\nDo not expect SliderServer or Bonds.possible_values to enumerate Masque bonds.","title":"Common mistakes"},{"category":"section","location":"#Masque.jl","page":"Home","text":"Masque adds an interactive layer over Makie figures inside a Pluto notebook. Add rich tooltips, hover interactions, selections, and more to your figures.","title":"Masque.jl"},{"category":"section","location":"#Rich-tooltips","page":"Home","text":"(Image: Holding the pointer over a star shows its name, spectral type, and distance)\n\nHold your pointer over a point, bar, heatmap cell, or polygon and a customizable tooltip appears on the figure. For templates and styling, see Tooltips.","title":"Rich tooltips"},{"category":"section","location":"#Dynamic-selections","page":"Home","text":"(Image: Clicking São Paulo on a cities scatter updates the bound pick cell to that city)\n\nWhen you click a mark to select it, @bind captures that selection, the same way a PlutoUI slider does. Downstream cells re-run with the selected row, bar, or cell. For more information, see Click marks and Selection.","title":"Dynamic selections"},{"category":"section","location":"#Interactive-view-controls","page":"Home","text":"(Image: Dragging the pointer on an Axis3 trefoil knot in a Pluto cell orbits the camera)\n\nDrag a 2D axis to pan, or an Axis3 to orbit. The camera stays on the figure; it is not a selection. For more information, see Pan and orbit.","title":"Interactive view controls"},{"category":"section","location":"#Highlight-from-the-legend","page":"Home","text":"(Image: Clicking a species in the legend keeps that class and fades the others)\n\nClick a legend entry to highlight the traces it labels, and a cell that reads that pick can fade the rest. See Legend.","title":"Highlight from the legend"},{"category":"section","location":"#Inspect-a-static-export","page":"Home","text":"Hover and click still work in a Pluto HTML export of the notebook. Re-running Julia cells needs a live session. CairoMakie is the default; WGLMakie is the live canvas when you want animation, large data, or 3D you can orbit. See Backends.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-home-export\" data-masque-embed=\"home_export\" title=\"New York City boroughs with overlay-only hover\" style=\"width:100%;height:520px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>","title":"Inspect a static export"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Getting started — install, overlay a figure, and read a click\nConcepts — how hover, clicks, payloads, and @bind fit together\nConstructors — every built-in kind, its constructor, and its default payload\nSelection — reacting to clicks, linking plots, persisting a highlight\nLegend — hover/click a Makie.Legend entry to highlight the trace(s) it labels\nTooltips — masque\"...\" templates and styling\nCustom hits — RegionInteractable / FunctionInteractable\nBackends — :cairo vs :webgl, and when to reach for which\nTroubleshooting — common errors and what causes them\nExamples — worked examples and a page per plot type\nAPI — full docstrings","title":"Where to go next"},{"category":"section","location":"grids/#Inspect-a-grid","page":"Inspect a grid","text":"A heatmap or an image is a grid of cells rather than a list of marks. Hover a cell and the card reads (i,j) = value; click it and your notebook gets the cell's column, row, and value.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-grids-player\" data-masque-embed=\"grids_heatmap\" title=\"Tiny heatmap with overlay cell inspection\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a cell. The last cell reads its column, row, and value.Draw the heatmap the way you already draw it, and pass that plot to RectInteractable so each cell is a hit. Hover reads the cell; the click is what this notebook stores.begin\n    z = [Float64(i + 3j) for i in 1:4, j in 1:3]\n    fig = Figure(size = (560, 320))\n    ax = Axis(fig[1, 1]; xlabel = \"column\", ylabel = \"row\")\n    p = heatmap!(ax, 1:4, 1:3, z)\n    cells = RectInteractable(ax, p)\n    nothing\nend@bind pick stores a click in pick. Hover updates the tooltip and does not change pick.@bind pick masque(fig, cells)(Image: The figure before any interaction)Before a click, pick is nothing. pick.i is the column and pick.j is the row, both 1-based. z[pick] is the same cell. This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"click a cell\"\nelse\n    \"column $(pick.i), row $(pick.j) — value $(pick.value)\"\nendclick a cell\n\n","title":"Inspect a grid"},{"category":"section","location":"grids/#Hover-and-click-cells","page":"Inspect a grid","text":"masque(fig) picks up every heatmap! and image! on its own. To choose the grid yourself — for example to give it an id when a figure has two — pass the plot to RectInteractable:\n\ncells = RectInteractable(ax, p)\n\nA click makes pick a GridCellEvent. pick.i is the column and pick.j the row, both 1-based, so they index the matrix you plotted: z[pick] is the same as z[pick.i, pick.j], and pick.value is the cell's value. As with other marks, the clicked cell stays highlighted, and a click outside the grid leaves the selection alone.\n\nIf you have edges and values rather than a plot, pass them as a grid. Each edge vector must be monotonic (ascending or descending), and the values form a matrix of size (length(xedges) - 1, length(yedges) - 1):\n\ncells = RectInteractable(ax; grid = (0.5:1:4.5, 0.5:1:3.5, z), id = :cells)\n\nA cell's payload is always its i, j, and value; grids do not take payloads. Keep per-cell data in Julia and look it up with pick.","title":"Hover and click cells"},{"category":"section","location":"grids/#Large-grids","page":"Inspect a grid","text":"A heatmap can have far more cells than the screen has pixels, and shipping every value to the browser would make the notebook slow. When cells are smaller than a screen pixel, Masque sends one sample per pixel instead: hovering shows the cell under the pointer and its value, and a click still reports the true i and j. A 4000 × 4000 heatmap works this way without any setting.\n\nColour images have no single value per cell, so hovering or clicking one reports i and j with value = nothing, whatever its size. To read a number, pass a RectInteractable grid with the values you want, such as each pixel's intensity.","title":"Large grids"},{"category":"section","location":"grids/#Brush-a-block-of-cells","page":"Inspect a grid","text":"Add an ROIInteractable with selects naming the grid, and a drag returns one GridWindowEvent for the block of cells under the box: A[win] is that sub-matrix. A box that misses the grid still returns one GridWindowEvent, with empty ranges (win.i1:win.i2 is 1:0), so A[win] is an empty matrix rather than an error. Once a box brushes the grid, clicking a cell no longer commits a GridCellEvent: the cells still show their tooltip, and the value stays the box's window. See Brush a region.","title":"Brush a block of cells"},{"category":"section","location":"grids/#What-grids-cannot-do","page":"Inspect a grid","text":"Heatmap cells cannot be reached with the keyboard — arrowing through thousands of cells is not useful — and they cannot start selected with selected=. Heatmaps and images on an Axis3 or a PolarAxis are skipped with a warning. A colorbar next to a heatmap is a separate readout of values; see Read coordinates.","title":"What grids cannot do"},{"category":"section","location":"scope/#What-Masque-does-not-do","page":"What Masque does not do","text":"Masque makes a static Makie figure respond to the pointer and hands the reader's choices to Pluto. Some things that interactive plotting libraries do are left out on purpose, and a few have rough edges today. Knowing them up front saves a search.","title":"What Masque does not do"},{"category":"section","location":"scope/#By-design","page":"What Masque does not do","text":"Hover never reaches Julia. Hovering is instant and works in a static export because it stays in the browser. To react to a mark in Julia, click it. See Concepts.\nOne selection at a time. A click replaces the selection; there is no Shift- or Ctrl-click to add to it. To pick several marks, brush them with an ROIInteractable.\nRectangles only. Brushes are boxes; there is no lasso, and one widget brushes one layer.\nThe figure is never redrawn by the overlay. Highlights are drawn on top of the picture, so the overlay cannot hide a series, recolour a mark, or zoom to a box. Do those in Julia from the @bind value — for example, fade the other series after a legend click (see Legend).\nNo double-click to reset the view. A pan or orbit stays where you left it until the figure is rebuilt; see Pan and orbit.\nNo automatic linking between plots. Masque does not treat the same row in two plots as one observation; link them in Pluto cells (see Linked views).\nClicks need a running notebook. In a static HTML export, hover and highlights still work, but nothing reaches @bind and the view cannot pan. PlutoSliderServer cannot enumerate Masque's values ahead of time, so it does not make clicks work in an export either.\nThe keyboard reaches marks, not handles. Arrow keys walk points, bars, polygons, lines, and legend entries, but cannot move a brush box or a threshold line; see Keyboard and screen readers.","title":"By design"},{"category":"section","location":"scope/#Overlapping-marks","page":"What Masque does not do","text":"When two interactive layers overlap, the pointer hits the one listed first. Legend entries always come first and pan or orbit always comes last; everything else keeps the order of the interactables you passed, and masque(fig) lists plots in the order you drew them.\n\nThat means a point drawn on top of a filled polygon is hidden from the pointer by the polygon drawn before it. Pass the interactables yourself, points first:\n\nbegin\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    p = poly!(ax, Point2f[(0, 0), (3, 0), (3, 3), (0, 3)])\n    s = scatter!(ax, [1.0, 2.0], [1.0, 2.0]; markersize = 16)\n    nothing\nend\n\n@bind pick masque(fig, [PointInteractable(ax, s), PolygonInteractable(ax, p)])\n\nIn 3D there is no depth test either. Which mark the pointer hits depends on the same layer order, not on distance from the camera: a point on the far side of an Axis3 scene can be hit through a nearer object in a later layer, and a nearer mark in a later layer can be hidden by a farther one in an earlier layer.","title":"Overlapping marks"},{"category":"section","location":"scope/#Current-rough-edges","page":"What Masque does not do","text":"These are known problems rather than design choices:\n\nSeveral plot types are skipped on Axis3 and PolarAxis, Surface plots are not hit-tested, and LScene is not supported. Supported plots and axes has the full list.","title":"Current rough edges"},{"category":"section","location":"view/#Pan-and-orbit","page":"Pan and orbit","text":"Sometimes the point of interaction is just to look closer: drag a 2D plot to pan it, scroll to zoom, or drag an Axis3 to turn it. A ViewInteractable on an axis enables that. Moving the view is about looking, not choosing, so it never changes a @bind value and never re-runs a cell.\n\n<video id=\"masque-view-clip\" title=\"CairoMakie in-drag pan frames on a 2D scatter\"\n       controls muted loop playsinline autoplay\n       style=\"width:100%;max-width:640px;height:auto;border:0;background:transparent;\"></video>\n<script>\n(function () {\n  var pretty = /\\/$/.test(location.pathname) || /\\/index\\.html$/.test(location.pathname);\n  var el = document.getElementById(\"masque-view-clip\");\n  if (!el) return;\n  el.src = (pretty ? \"../assets/\" : \"assets/\") + \"view-pan.mp4\";\n})();\n</script>","title":"Pan and orbit"},{"category":"section","location":"view/#Pan-and-zoom-a-2D-axis","page":"Pan and orbit","text":"masque(fig) does not add panning on its own; pass a ViewInteractable for the axis you want to move, along with any other interactables:\n\nbegin\n    xs = Float64[1, 2, 3, 4, 5, 6]\n    ys = Float64[1, 4, 9, 16, 25, 36]\n    fig = Figure(size = (560, 320))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\")\n    s = scatter!(ax, xs, ys; color = :dodgerblue, markersize = 18)\n    pan = ViewInteractable(ax)\n    pts = PointInteractable(ax, s)\n    nothing\nend\n\n@bind pick masque(fig, [pan, pts])\n\nDrag the plot to pan and use the scroll wheel to zoom about the pointer. The axis frame stays put while the data slides inside it, and the points stay hoverable and clickable wherever they end up. pick still changes only when you click a point.\n\nWhile you drag, Julia re-renders the view and streams it to the page, so panning needs a running notebook: CairoMakie sends images, WGLMakie updates its live canvas. In a static HTML export the view cannot move. Backends has the details.\n\nIf the same axis has a threshold line or a brushing box, a plain drag moves that handle and Shift+drag pans.","title":"Pan and zoom a 2D axis"},{"category":"section","location":"view/#Orbit-an-Axis3","page":"Pan and orbit","text":"On an Axis3, dragging turns the camera around the plot (azimuth and elevation) instead of panning. It works on both backends, and marks on the 3D axis stay hoverable and clickable as the view turns.\n\nbegin\n    fig = Figure(size = (560, 360))\n    ax = Axis3(fig[1, 1])\n    s = scatter!(ax, Makie.Point3f[(1, 2, 3), (4, 5, 6), (7, 8, 2)]; markersize = 16)\n    orbit = ViewInteractable(ax)\n    nothing\nend\n\n@bind pick masque(fig, [orbit, PointInteractable(ax, s)])","title":"Orbit an Axis3"},{"category":"section","location":"view/#When-the-figure-is-rebuilt","page":"Pan and orbit","text":"A pan or orbit is written onto the axis itself: Julia updates the axis's limits (or its azimuth and elevation) as you drag. So calling masque again on the same figure mounts at the view you dragged to. When the cell that draws the figure re-runs, it creates a new Axis, which starts from whatever limits or angles your code gives it. To keep a view across such a rebuild, set those values in the figure code yourself, for example from a slider bound to the limits.","title":"When the figure is rebuilt"},{"category":"section","location":"view/#Where-it-works","page":"Pan and orbit","text":"Panning needs a 2D Axis with numeric limits on an identity, log10, or log scale; orbiting needs an Axis3. A PolarAxis, a Colorbar, a categorical axis, or another scale raises an ArgumentError when masque runs, and an LScene is not supported. See Supported plots and axes.","title":"Where it works"},{"category":"section","location":"gallery/cluster/#Compare-a-cluster","page":"Compare a cluster","text":"A brush is the natural way to ask \"what is different about these points?\". Drag the box over a cluster and release; the histogram under the scatter compares that cluster's z with every sample. Hover any point to read its measurements.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-ex-cluster\" data-masque-embed=\"example_cluster\" title=\"Scatter of 150 samples with a box; a histogram compares z inside the box with all samples\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nDrag the box over a cluster and release. The histogram below compares that cluster's z with every sample.Scatter two measurements, colour the points by a third, z, and keep all three on each point's payload so a hover reads them. ROIInteractable brushes the points.begin\n    # Deterministic noise, so the example looks the same every time it runs.\n    u(k) = mod(sin(k * 12.9898) * 43758.5453, 1.0)\n    g(k) = sqrt(-2log(u(k) + 1.0e-9)) * cos(2π * u(k + 0.5))\n    n = 150\n    xs = [i <= 80 ? 3.0 + 0.9g(i) : 7.0 + 0.9g(i) for i in 1:n]\n    ys = [i <= 80 ? 3.0 + 0.9g(i + 1000) : 6.0 + 0.8g(i + 1000) for i in 1:n]\n    zs = [i <= 80 ? 1.2 + 0.3g(i + 2000) : 2.8 + 0.35g(i + 2000) for i in 1:n]\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\", title = \"drag the box over a cluster\")\n    s = scatter!(ax, xs, ys; color = zs, colormap = :viridis, markersize = 9)\n    samples = [(; sample = i, x = round(xs[i]; digits = 2), y = round(ys[i]; digits = 2), z = round(zs[i]; digits = 2)) for i in 1:n]\n    pts = PointInteractable(ax, s; id = :pts, payloads = samples)\n    roi = ROIInteractable(ax; bounds = (5.2, 9.2, 4.4, 7.8), selects = :pts)\n    nothing\nend@bind picks stores the points inside the box when you release.@bind picks masque(fig, [pts, roi])(Image: The figure before any interaction)This cell reads picks, so it redraws the comparison each time you release the box. zs[picks] is the z of the points inside.begin\n    edges = range(minimum(zs), maximum(zs); length = 21)\n    inside = picks === nothing ? Float64[] : zs[picks]\n    cmp = Figure(size = (560, 260))\n    cax = Axis(\n        cmp[1, 1]; xlabel = \"z\", ylabel = \"samples\",\n        title = isempty(inside) ? \"all samples\" : \"$(length(inside)) samples in the box, against all $(length(zs))\"\n    )\n    hist!(cax, zs; bins = edges, color = (:gray, 0.45), label = \"all\")\n    isempty(inside) || hist!(cax, inside; bins = edges, color = (:darkorange, 0.85), label = \"in the box\")\n    axislegend(cax; position = :rt)\n    cmp\nend\n\n\n\nOn this page the two clusters are recorded as snapshots; in your own notebook, every release recomputes the histogram.","title":"Compare a cluster"},{"category":"section","location":"gallery/cluster/#How-it-works","page":"Compare a cluster","text":"Each point's payload carries its three measurements, so the tooltip shows them without any extra code. The ROIInteractable names the points' layer with selects = :pts, so releasing the box makes picks a vector with one event per point inside. That vector indexes your data directly — zs[picks] is the z of the points in the box — and the last cell draws an ordinary Makie figure from it.","title":"How it works"},{"category":"section","location":"gallery/cluster/#Variations","page":"Compare a cluster","text":"Keep your samples in a DataFrame and pass it as payloads; then df[picks, :] is the rows inside the box, ready for any summary. Guard it for the state before the first release, when picks is nothing: picks === nothing || isempty(picks) ? df[1:0, :] : df[picks, :].\nReplace the histogram with whatever the comparison needs: a table of means, a second scatter of two other columns, or a model fitted to the selected points only.\n\nBrush a region covers the box itself; Linked views covers driving other plots from a selection.","title":"Variations"},{"category":"section","location":"gallery/heatmap-trace/#From-a-heatmap-cell-to-its-trace","page":"From a heatmap cell to its trace","text":"A heatmap summarizes. Clicking a cell should take you back to the data behind the summary. Here each cell is a station's mean temperature for one day of a week; click one and the plot below shows that day's hourly readings, with the daily mean as a dashed line.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-ex-heatmap-trace\" data-masque-embed=\"example_heatmap_trace\" title=\"Heatmap of daily mean temperature by station and day; clicking a cell plots that day's hourly readings\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a cell. The plot below shows that station's readings, hour by hour, on the day you clicked.The heatmap is a summary: one daily mean per station and day. temp is the underlying hourly model, so a click can go back to the detail behind the summary. RectInteractable makes each cell clickable.begin\n    stations = [\"North\", \"East\", \"South\", \"West\"]\n    hours = 0:23\n    temp(d, s, h) = 12 + 4 * sin(2π * (h - 9) / 24) + 0.6 * d * (s == 3 ? 1 : 0.3) + 1.5 * cos(d / 2 + s)\n    daily = [round(sum(temp(d, s, h) for h in hours) / 24; digits = 1) for d in 1:7, s in eachindex(stations)]\n    fig = Figure(size = (560, 320))\n    ax = Axis(fig[1, 1]; xlabel = \"day\", yticks = (eachindex(stations), stations), title = \"daily mean temperature: click a cell\")\n    hm = heatmap!(ax, 1:7, eachindex(stations), daily; colormap = :thermal)\n    Colorbar(fig[1, 2], hm; label = \"°C\")\n    cells = RectInteractable(ax, hm)\n    nothing\nend@bind pick stores the clicked cell. pick.i is the day and pick.j the station, both 1-based, and pick.value is the daily mean.@bind pick masque(fig, cells)(Image: The figure before any interaction)This cell reads pick, so a click redraws the day behind the cell, with the daily mean as a dashed line.begin\n    trace = Figure(size = (560, 240))\n    if pick === nothing\n        Axis(trace[1, 1]; xlabel = \"hour\", ylabel = \"°C\", title = \"click a cell to see that day\")\n    else\n        tax = Axis(\n            trace[1, 1]; xlabel = \"hour\", ylabel = \"°C\",\n            title = \"$(stations[pick.j]), day $(pick.i): mean $(pick.value) °C\"\n        )\n        lines!(tax, hours, [temp(pick.i, pick.j, h) for h in hours]; linewidth = 2)\n        hlines!(tax, [pick.value]; linestyle = :dash, color = :gray)\n    end\n    trace\nend\n\n\n\nOn this page every cell's trace is recorded as a snapshot; in your own notebook, every click redraws the day.","title":"From a heatmap cell to its trace"},{"category":"section","location":"gallery/heatmap-trace/#How-it-works","page":"From a heatmap cell to its trace","text":"RectInteractable makes each heatmap cell clickable. A click makes pick a GridCellEvent: pick.i is the column (the day) and pick.j the row (the station), both 1-based, so they index the same data the heatmap was built from, and pick.value is the cell's value. The last cell uses pick.i and pick.j to recompute the detail — here from the hourly model, in practice from your raw measurements.","title":"How it works"},{"category":"section","location":"gallery/heatmap-trace/#Variations","page":"From a heatmap cell to its trace","text":"Look the detail up instead of recomputing it: filter a table of raw readings by the clicked day and station.\nShow a table or summary statistics for the clicked cell instead of a line plot.\nBrush a block of cells with an ROIInteractable whose selects names the grid; the value is a GridWindowEvent, and A[win] is that block of the matrix.\n\nInspect a grid covers hovering and clicking cells in detail.","title":"Variations"},{"category":"section","location":"custom/#Custom-hits","page":"Custom hits","text":"Sometimes the thing you want people to click is not a mark Makie drew: regions of interest on a microscope image, zones on a map, the arms of a diagram. Sometimes it is, but a click should return something richer than an ElementEvent. Masque has three levels for this, each more work and more control than the last:\n\nRegionInteractable — list circles, rectangles, and polygons placed in data coordinates. Enough for most cases.\nFunctionInteractable — compute hit geometry yourself from the figure's layout, for shapes the first level cannot express.\nA subtype of AbstractInteractable — also choose what a click returns, with an event type of your own.\n\nNone of them draws anything: the figure is still whatever Makie drew, and a custom interactable only says where the pointer can hit and what each hit means. Draw a visible outline with Makie if the regions should be visible.","title":"Custom hits"},{"category":"section","location":"custom/#Regions-over-a-figure","page":"Custom hits","text":"Pass a list of shapes and one payload per shape. Hovering a region shows its payload, and a click returns an ElementEvent that carries the payload's fields (pick.name):\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-custom-regions\" data-masque-embed=\"custom_regions\" title=\"Three RegionInteractable hits over an image, overlay-only\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHold the pointer over a region to read its name, then click it. The last cell names the region.The image is the figure. The circle, the rectangle, and the triangle are extra hit regions, with a name on each one.begin\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\")\n    img = [Float32(sin(i / 12) * cos(j / 10)) for i in 1:100, j in 1:100]\n    image!(ax, img)\n    regions = [\n        (:circle, (20.0, 20.0), 14.0),\n        (:rect, (60.0, 60.0), 20.0, 14.0),\n        (:polygon, [(30.0, 70.0), (50.0, 90.0), (20.0, 90.0)]),\n    ]\n    names = [(; name = \"cell A\"), (; name = \"cell B\"), (; name = \"cell C\")]\n    hits = RegionInteractable(\n        ax; regions, payloads = names, id = :cells,\n        tooltip = masque\"<b>$(name)</b>\",\n    )\n    nothing\nend@bind ev stores a click in ev. Hover updates the card and does not change ev.@bind ev masque(fig, hits)(Image: The figure before any interaction)Before a click, ev is nothing. ev.name is the region's name. This cell reads ev, so it re-runs on the click.if ev === nothing\n    \"click a region\"\nelse\n    \"$(ev.name) selected\"\nendclick a region\n\n\n\nEach region is one of\n\n(:circle,  (cx, cy), r)            # r in logical pixels\n(:rect,    (cx, cy), w, h)         # w, h in data space\n(:polygon, [(x, y), ...])          # a ring in data space\n\nRectangles and polygons are in data coordinates, so they stay on the features they outline. A circle's centre is in data coordinates too, but its radius is in logical pixels, like a scatter marker's size: r = 8 is an eight-pixel target however wide the axis is. Pass the logical size; Masque scales it for the figure's resolution itself. payloads is required — a region has no data of its own — and tooltip works as elsewhere (see Tooltips).\n\nMasque groups the regions by shape, so one RegionInteractable with id = :cells produces up to three layers: :cells_c (circles), :cells_r (rectangles), and :cells_p (polygons). pick.layer reports those ids, and selected= uses them.","title":"Regions over a figure"},{"category":"section","location":"custom/#Compute-hit-geometry","page":"Custom hits","text":"When the shapes are not circles, rectangles, or polygons — a set of line segments, say, or regions on several axes at once — build the hit layers yourself. FunctionInteractable takes a function that receives the figure's InteractionContext and returns a vector of HitLayers. data_to_image_px converts a data point on an axis to the image pixels the overlay works in, and Masque.axis_id(ctx, ax) names the axis a layer belongs to.\n\nbegin\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    verts = [(0.0, 0.0), (2.0, 1.0), (3.0, 2.0), (5.0, 0.5)]\n    linesegments!(\n        ax, first.(verts), last.(verts);\n        color = :firebrick, linewidth = 4,\n    )\n\n    track = FunctionInteractable() do ctx\n        geom = Float64[]\n        for p in verts\n            q = data_to_image_px(ctx, ax, p)\n            push!(geom, q[1], q[2])\n        end\n        nseg = length(verts) ÷ 2\n        [\n            HitLayer(\n                :track,\n                :segments,\n                geom,\n                [(; i = k) for k in 1:nseg],\n                Masque.axis_id(ctx, ax),\n                (:click, :hover),\n            ),\n        ]\n    end\nend\n\n@bind pick masque(fig, track)\n\nEach layer's geometry is in image pixels, laid out according to its kind — a flat vector for most kinds, a vector of paths or rings for :lines and :polygons:\n\nKind geometry One element is\n:circles [cx, cy, r, cx, cy, r, …] one circle\n:segments [x0, y0, x1, y1, …] one segment (a pair of points)\n:polyline [x, y, x, y, …] (NaN starts a gap) one edge between consecutive points\n:lines a vector of paths, each [x, y, …] one whole path\n:rects [cx, cy, w, h, …] one rectangle\n:polygons one ring [x, y, …] per element, or [exterior, hole, …] for an element with holes one polygon\n\nHeatmap-style :grid layers use a different, edge-based layout; build those with RectInteractable's grid= keyword rather than by hand. payloads has one entry per element. A click on a layer returns an ElementEvent, with the payload's fields on it. Because the function sees the whole figure, one FunctionInteractable can return layers on several axes.","title":"Compute hit geometry"},{"category":"section","location":"custom/#A-custom-interactable","page":"Custom hits","text":"To make a click return a struct of your own, subtype AbstractInteractable and implement three methods: hitlayers (the hit geometry, as above), bondtype (the type a click produces), and transform_bond (build that value from the clicked element). The event type subtypes InteractionEvent. Put the type definitions in their own cell, so rebuilding the figure does not redefine them:\n\nbegin\n    struct CityPick <: InteractionEvent\n        layer::Symbol\n        index::Int\n        city::String\n        pop::Int\n    end\n\n    struct Cities <: AbstractInteractable\n        ax\n        pts\n        rows\n        id::Symbol\n    end\n\n    Masque.bondtype(::Cities) = CityPick\n\n    function Masque.hitlayers(i::Cities, ctx)\n        geom = Float64[]\n        for p in i.pts\n            q = data_to_image_px(ctx, i.ax, p)\n            r = 8 * ctx.scaling\n            push!(geom, q[1], q[2], r)\n        end\n        [\n            HitLayer(\n                i.id,\n                :circles,\n                geom,\n                Vector{Any}(i.rows),\n                Masque.axis_id(ctx, i.ax),\n                (:click, :hover),\n            ),\n        ]\n    end\n\n    function Masque.transform_bond(i::Cities, layer::HitLayer, index, js_payload)\n        row = i.rows[index]\n        CityPick(i.id, index, row.city, row.pop)\n    end\nend\n\nbegin\n    fig = Figure()\n    ax = Axis(fig[1, 1]; xlabel = \"lon\", ylabel = \"lat\")\n    rows = [\n        (; city = \"Tokyo\", pop = 37),\n        (; city = \"Delhi\", pop = 32),\n        (; city = \"Shanghai\", pop = 27),\n    ]\n    pts = [(139.7, 35.7), (77.2, 28.6), (121.5, 31.2)]\n    scatter!(ax, first.(pts), last.(pts); markersize = 16)\n    cities = Cities(ax, pts, rows, :cities)\nend\n\n@bind pick masque(fig, cities)\n\npick === nothing ? \"click a city\" : \"$(pick.city), $(pick.pop) million\"\n\nindex arrives at transform_bond already 1-based. A type that is happy with the default ElementEvent only needs hitlayers.","title":"A custom interactable"},{"category":"section","location":"gallery/#Examples","page":"Examples","text":"Each example is a small notebook you can copy. On an example's page, hovering works as it does in Pluto, every click swaps in a recorded result, brushes swap in results recorded for a few boxes, and the notebook follows the player as text. Pan and orbit are recorded clips instead.","title":"Examples"},{"category":"section","location":"gallery/#Analyses","page":"Examples","text":"Worked examples of an interaction feeding an analysis: a brush that compares a cluster, a heatmap cell that opens the data behind it, a click mirrored into a second plot.\n\n<div class=\"masque-gallery\">\n<a class=\"masque-gallery-card\" data-page=\"cluster\">\n  <img alt=\"A scatter of two clusters with a box and a comparison histogram\" src=\"assets/gallery/cluster.png\">\n  <span>Compare a cluster</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"heatmap-trace\">\n  <img alt=\"A heatmap of daily temperature by station\" src=\"assets/gallery/heatmap-trace.png\">\n  <span>From a heatmap cell to its trace</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"selection\">\n  <img alt=\"Two scatters; the right figure highlights the mark clicked on the left\" src=\"assets/gallery/selection.png\">\n  <span>Selection round-trip</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"boxselect\">\n  <img alt=\"A two-group scatter with a selection box\" src=\"assets/gallery/boxselect.png\">\n  <span>Box-select scatter</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"image\">\n  <img alt=\"An RGB image with a region box\" src=\"assets/gallery/image.png\">\n  <span>Image ROI</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"tooltips\">\n  <img alt=\"Four city markers with a template tooltip\" src=\"assets/gallery/tooltips.png\">\n  <span>Tooltip templates</span>\n</a>\n</div>","title":"Analyses"},{"category":"section","location":"gallery/#Plot-types","page":"Examples","text":"One page per kind of plot, showing what a hover or click on it returns. Pan and orbit are recorded clips, since moving the view needs a running notebook.\n\n<div class=\"masque-gallery\">\n<a class=\"masque-gallery-card\" data-page=\"bars\">\n  <img alt=\"Histogram, waterfall, crossbar, and bars with spans\" src=\"assets/gallery/bars.png\">\n  <span>Bars and areas</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"polygons\">\n  <img alt=\"Band, density, contour, violin, Voronoi, and box plots\" src=\"assets/gallery/polygons.png\">\n  <span>Polygons</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"colorbar\">\n  <img alt=\"A gaussian heatmap with a colorbar\" src=\"assets/gallery/colorbar.png\">\n  <span>Colorbar</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"text\">\n  <img alt=\"Scatter points with Alpha, Beta, Gamma, a tilted label, and an annotation\" src=\"assets/gallery/text.png\">\n  <span>Text labels</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"polar\">\n  <img alt=\"Four points on a polar axis\" src=\"assets/gallery/polar.png\">\n  <span>Polar points</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"limits\">\n  <img alt=\"A scatter clipped by axis limits\" src=\"assets/gallery/limits.png\">\n  <span>Limits slider</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"pan\">\n  <img alt=\"A scatter you drag to pan\" src=\"assets/gallery/pan.png\">\n  <span>Drag to pan</span>\n</a>\n<a class=\"masque-gallery-card\" data-page=\"orbit\">\n  <img alt=\"Three markers on an Axis3\" src=\"assets/gallery/orbit.png\">\n  <span>Drag to orbit</span>\n</a>\n</div>\n<script>\n(function () {\n  var pretty = /\\/$/.test(location.pathname) || /\\/index\\.html$/.test(location.pathname);\n  var root = pretty ? \"../\" : \"\";\n  document.querySelectorAll(\"a.masque-gallery-card\").forEach(function (a) {\n    var page = a.getAttribute(\"data-page\");\n    a.href = root + \"gallery/\" + page + (pretty ? \"/\" : \".html\");\n    var img = a.querySelector(\"img\");\n    if (img) img.src = root + \"assets/gallery/\" + page + \".png\";\n  });\n})();\n</script>","title":"Plot types"},{"category":"section","location":"gallery/orbit/#Drag-to-orbit","page":"Drag to orbit","text":"ViewInteractable on an Axis3 orbits the camera while you drag. azimuth and elevation change in the live session. The bond stays empty of any view payload, same as a 2D pan.\n\nA static Axis3 is a valid figure on CairoMakie. You do not need WGLMakie to draw the three markers. See Pan and orbit. The clip is that drag.\n\n<video id=\"masque-gal-orbit\" title=\"Drag to orbit three markers on an Axis3\"\n       controls muted loop playsinline autoplay\n       style=\"width:100%;max-width:640px;height:auto;border:0;background:transparent;\"></video>\n<script>\n(function () {\n  var pretty = /\\/$/.test(location.pathname) || /\\/index\\.html$/.test(location.pathname);\n  var el = document.getElementById(\"masque-gal-orbit\");\n  if (!el) return;\n  el.src = (pretty ? \"../../assets/\" : \"../assets/\") + \"gallery-orbit.mp4\";\n})();\n</script>\n\nbegin\n    fig = Figure(size = (500, 380))\n    ax = Axis3(fig[1, 1]; azimuth = 0.4, elevation = 0.5, title = \"drag to orbit\")\n    scatter!(\n        ax,\n        Makie.Point3f[(1, 2, 3), (4, 5, 6), (7, 8, 2)];\n        color = :crimson,\n        markersize = 16,\n    )\n    view = ViewInteractable(ax)\n    nothing\nend\n\n@bind pick masque(fig, view)","title":"Drag to orbit"},{"category":"section","location":"gallery/orbit/#Variations","page":"Drag to orbit","text":"A slider that sets azimuth and elevation and rebuilds the figure is the other way to orbit. That path re-runs the cell. This drag does not. Persisting a pose across a remount means storing the two angles yourself and rebuilding. selected= does not store a camera.\n\nnote: Note\nOrbit commits nothing on both backends. In-drag frames need a live kernel: a PNG from :cairo, a serialized scene from :webgl. The clip above is that Cairo drag. Reach for :webgl when you want the orbit on the GPU canvas. PolarAxis and a Colorbar are not orbit targets.","title":"Variations"},{"category":"section","location":"gallery/colorbar/#Colorbar","page":"Colorbar","text":"masque(fig) finds a Colorbar in the figure layout and adds a readout. Hover the bar for the data value under the pointer. A click in a live notebook round-trips a ColorbarEvent with value. The layer kind is :axis.\n\nmasque(fig) builds that ColorbarInteractable for you. Write one yourself when you are not using masque(fig). See Read coordinates.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-colorbar\" data-masque-embed=\"gallery_colorbar\" title=\"Colorbar\" style=\"width:100%;height:1100px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nHover the colorbar to read the value under the pointer.Draw the heatmap and its colorbar. masque makes the colorbar a readout.begin\n    xs = -3:0.1:3\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; title = \"hover or click the colorbar\")\n    hm = heatmap!(ax, xs, xs, [exp(-(x^2 + y^2)) for x in xs, y in xs])\n    Colorbar(fig[1, 2], hm)\n    nothing\nendHover stays on the figure. This page does not re-run a readout.@bind pick masque(fig)(Image: The figure before any interaction)\n\n","title":"Colorbar"},{"category":"section","location":"gallery/colorbar/#Variations","page":"Colorbar","text":"A click is a ColorbarEvent. pick.value is the data value under the pointer. ColorbarInteractable takes the bar and id only, so there is no value= to reopen a position. The heatmap cells are a separate layer. Hovering a cell is the grid readout, not the bar.\n\nnote: Note\nThe colorbar readout is the same on :cairo and :webgl. This player is the Cairo figure. Hover works here; the click bond is a live notebook, because a colorbar click is a value rather than a listed mark. Colorbar is not a pan or orbit target.","title":"Variations"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"Masque makes a Makie figure in a Pluto notebook respond to the pointer: hover a mark to see its data, click it to hand that mark to the rest of your notebook. This page installs Masque, overlays a figure with one call, and then attaches your own data to the marks.","title":"Getting started"},{"category":"section","location":"getting-started/#Install","page":"Getting started","text":"In a Pluto notebook, paste each snippet from these docs into its own cell. Pluto runs one top-level expression per cell. Wrap multiple statements in begin ... end, which counts as one expression.\n\nPaste this cell into the notebook. Pluto's package manager stays on:\n\nbegin\n    using Pkg\n    Pkg.add(url = \"https://github.com/jowch/Masque.jl\")\n    Pkg.add(\"CairoMakie\")\n    using Masque, CairoMakie\nend\n\nTo install from a Julia session instead, keep the notebook file closed and write the packages into that file. Pluto.activate_notebook_environment updates the environment embedded in the notebook, so package management stays on when you open it again. Pkg.activate(; temp = true) turns package management off.\n\nimport Pluto, Pkg\nPluto.activate_notebook_environment(\"path/to/notebook.jl\") do\n    Pkg.add(url = \"https://github.com/jowch/Masque.jl\")\n    Pkg.add(\"CairoMakie\")\nend\n\ninfo: Info\nMasque supports CairoMakie and WGLMakie. using Masque with no Makie backend raises ArgumentError the first time masque runs. If both CairoMakie and WGLMakie are loaded, masque defaults to CairoMakie. For more information, see Backends.","title":"Install"},{"category":"section","location":"getting-started/#Overlay-a-figure","page":"Getting started","text":"Draw the figure the way you normally would. End the cell with nothing so Pluto does not also print the plain figure:\n\nbegin\n    xs = [1.0, 2.0, 3.0, 4.0]\n    ys = [2.0, 1.0, 4.0, 3.0]\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1])\n    scatter!(ax, xs, ys; markersize = 16)\n    nothing\nend\n\nThen hand the figure to masque and bind the result:\n\n@bind pick masque(fig)\n\nHover a point and a tooltip lists its index, x, and y. Hovering never re-runs Julia; it only happens on the figure. Click a point and pick becomes that point, so a cell that reads pick re-runs:\n\npick === nothing ? \"click a point\" : \"point $(pick.index) at x = $(pick.x)\"\n\npick is an ElementEvent. pick.index is 1-based, and pick indexes your data directly: ys[pick] is the clicked point's y.\n\nmasque(fig) finds everything it knows how to overlay on its own: scatters, lines, bars, heatmaps, polygons, text, legends, and colorbars (see Recipes masque(fig) extracts).","title":"Overlay a figure"},{"category":"section","location":"getting-started/#Attach-your-own-data","page":"Getting started","text":"A tooltip that says x = 3.0 is only a start. Pass the scatter to PointInteractable with one payloads entry per point, and the tooltip and the click carry your fields instead. The notebook below does that for three named points: hover reads name and y, and a click fills sel.name.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gs-quickstart\" data-masque-embed=\"home_quickstart\" title=\"Three-point scatter with a tooltip, @bind, and a readout\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\"></iframe>\n</div>\n\ndetails: Notebook as text\nHover a point to read its name, then click it. The last cell names the point.Draw the scatter the way you already draw a Makie figure, and end the cell with nothing so this cell does not print the figure. PointInteractable takes that scatter, so the highlight sits on the marker. The tooltip reads name and y off each point.begin\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; xlabel = \"x\", ylabel = \"y\")\n    points = [\n        (name = \"one\", x = 1.0, y = 1.0),\n        (name = \"two\", x = 2.0, y = 4.0),\n        (name = \"three\", x = 3.0, y = 9.0),\n    ]\n    s = scatter!(ax, [p.x for p in points], [p.y for p in points]; markersize = 18)\n    pts = PointInteractable(ax, s; payloads = points)\n    nothing\nendmasque mounts the overlay on the figure. @bind sel stores a click in sel. Hover updates the tooltip and does not change sel.@bind sel masque(fig, pts)(Image: The figure before any interaction)Before a click, sel is nothing. A click is an ElementEvent. sel.name is the payload's name, and sel.index is 1-based. This cell reads sel, so it re-runs on the click.if sel === nothing\n    \"click a point\"\nelse\n    \"$(sel.name) selected — y = $(sel.y)\"\nendclick a point\n\n\n\nOn this site the notebook is a recording, so a click swaps in a result computed ahead of time (the Simulating @bind badge); in your own notebook the last cell re-runs. Notebook as text has the same cells to copy.","title":"Attach your own data"},{"category":"section","location":"getting-started/#Where-to-go-next","page":"Getting started","text":"Concepts — how hover, clicks, payloads, and @bind fit together\nTooltips — templates, number formatting, and a dark figure\nClick marks — bars, polygons, lines, and polar points\nBrush a region — drag a box and get the points inside it\nSelection — start with a mark selected, or keep one across a rebuild\nLinked views — drive another plot or a table from a click\nExamples — worked examples to copy\nBackends — CairoMakie or WGLMakie","title":"Where to go next"},{"category":"section","location":"accessibility/#Keyboard-and-screen-readers","page":"Keyboard and screen readers","text":"Every overlay is a keyboard-navigable, screen-reader-announced widget. No extra setup. Tab focuses the surface (tabindex=\"0\"). Then use the keys in the following table to move between interactive elements.","title":"Keyboard and screen readers"},{"category":"section","location":"accessibility/#Keys","page":"Keyboard and screen readers","text":"Key Moves to\n→ / ↓ Next element\n← / ↑ Previous element\nHome / End First / last element\nPage Down / Page Up First element of the next / previous layer\nEnter / Space Commit the focused element — the same @bind value a click would (commitClick)\nEscape Clear focus and leave the plot\n\nArrow keys and Home / End stop at the first and last element. They do not wrap.\n\nTab focuses the surface. It does not land on an element. Home and the first arrow (either direction) land on the first focusable element. If a legend exists, its entries come first, so that element is the first legend entry. The announcement numbers it \"element 1\" of its layer, the same 1-based index selected= uses. selected = 0 is out of range. Page Down then steps from the legend into the plot layers. That order matches the pointer: a legend drawn on top of a plot takes the clicks under it. Focusing an entry draws highlight in the overlay on the trace it labels, the same as holding the pointer over it. For more information, see Legend.\n\nMoving focus draws the same highlight in the overlay as holding the pointer over the mark (there is never a separate focus look). A layer that has a tooltip shows that same card on the focused element. A legend entry does not, unless you passed a template.\n\nReachable kinds: :circles, :rects, :polygons, :segments, :polyline, and :lines. That includes TextInteractable (kind :rects) and Region circle / rect / polygon layers. Not reachable: :grid (heatmap / image cells), threshold, ROI, and view. Hold the pointer or use a touch drag for those. Keyboard does not arrow through heatmap cells.","title":"Keys"},{"category":"section","location":"accessibility/#What-gets-announced","page":"Keyboard and screen readers","text":"Screen readers announce each element you land on: an optional layer name, its position within that layer, and the same content the tooltip shows, as plain text. For example:\n\nPointInteractable(\n    ax, pts;\n    label = \"Scatter\",\n    payloads = [(; x = 1.0, y = 4.0)],\n)\n\nmight announce \"Scatter, element 1 of 3: x 1.0, y 4.0\". Without label, the announcement omits the prefix: \"element 1 of 3: x 1.0, y 4.0\".\n\nlabel= is a single string per layer — set it once on the interactable, not per element. It is a screen-reader prefix on explicit element constructors: PointInteractable, SegmentInteractable, RectInteractable (list form), and PolygonInteractable. It is not accepted on plot-object constructors (PointInteractable(ax, p::Makie.Scatter) and similar) or on masque(fig) auto-extraction. Build the interactable with the keyword form to set a label.\n\nDo not confuse label= with a payload key label (for example (; label = \"Tokyo\") in a tooltip). That is per-element tooltip data. label= is one string for the whole layer.\n\nmasque(fig, [\n    PointInteractable(ax, pts; id = :cities, label = \"City\"),\n    RectInteractable(ax; rects = bars, id = :bars, label = \"Revenue by quarter\"),\n])\n\nlabel= on PointInteractable(ax, p::Makie.Scatter) is a MethodError.","title":"What gets announced"},{"category":"section","location":"accessibility/#accessibility-limitations","page":"Keyboard and screen readers","text":"Grid layers (:grid — heatmap / image cells) are not keyboard-focusable. They stay mouse- and touch-interactive.\nThreshold, ROI, and view have no keyboard equivalent. Drag them with the pointer.\nScreen-reader support for aria-live inside a shadow DOM is uneven across browser and assistive-technology combinations. If announcements are silent in your setup, the visual focus ring and tooltip still work. File an issue with the browser and AT versions.","title":"Limitations"},{"category":"section","location":"marks/#Click-marks","page":"Click marks","text":"Every plot masque(fig) recognizes is clickable, not only scatters. A click on a mark — a point, bar, polygon, line, or text label — hands it to your notebook as an ElementEvent: its 1-based pick.index, plus fields that depend on what kind of mark it is. Heatmap cells, legend entries, and colorbars return their own event types (see Concepts). This page walks through bars, polygons, lines, and points on a polar axis. Plot-object defaults lists the fields each plot type reports, and Supported plots and axes which plots work on which axes.","title":"Click marks"},{"category":"section","location":"marks/#Bars","page":"Click marks","text":"A bar reports its value (the bar's height) and its low and high ends, so a stacked or dodged bar still tells you which segment you hit. Here each click reads the quarter and the value:\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-marks-bars\" data-masque-embed=\"marks_bars\" title=\"Four-bar plot. Click a bar and the readout names its value.\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a bar. The last cell reads its value.Draw the bar plot the way you already draw it, and end the cell with nothing so this cell does not print the figure. masque makes each bar clickable.begin\n    fig = Figure(size = (560, 360))\n    ax = Axis(\n        fig[1, 1];\n        xlabel = \"Quarter\",\n        ylabel = \"Value\",\n        xticks = (1:4, [\"Q1\", \"Q2\", \"Q3\", \"Q4\"]),\n    )\n    barplot!(ax, 1:4, [2.0, 3.0, 1.5, 2.5])\n    nothing\nendmasque mounts the overlay on the figure. @bind pick stores a click in pick. Hover highlights the bar and does not change pick.@bind pick masque(fig)(Image: The figure before any interaction)Before a click, pick is nothing. A click reads the bar: pick.value is the height, and pick.index is 1-based. This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"click a bar\"\nelse\n    \"Q$(pick.index) — value $(pick.value)\"\nendclick a bar\n\n\n\nHistograms, waterfalls, and crossbars are clickable too, each with its own fields: a waterfall step reports low, high, and value; a histogram bin reports value (the bar's height, which is a count only with normalization = :none), low, and high; a crossbar reports midpoint, low, and high. A heatmap is a grid of cells rather than a list of bars; it has its own page, Inspect a grid.","title":"Bars"},{"category":"section","location":"marks/#Polygons","page":"Click marks","text":"Each polygon of a poly! is one mark, reported by its index in the order you drew them. Pair the index with your own list of names, or pass payloads to PolygonInteractable so the name travels with the click:\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-marks-poly\" data-masque-embed=\"marks_poly\" title=\"Three polygons. Click one and the readout names it.\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a polygon. The last cell names it.Draw the polygons the way you already draw them. masque makes each ring clickable.begin\n    fig = Figure(size = (560, 360))\n    ax = Axis(fig[1, 1]; aspect = DataAspect())\n    poly!(\n        ax,\n        [\n            Point2f[(0.0, 0.0), (1.0, 0.0), (0.5, 0.85)],\n            Point2f[(1.6, 0.0), (2.6, 0.0), (2.6, 1.0), (1.6, 1.0)],\n            Point2f[(0.1, 1.2), (1.1, 1.2), (0.9, 2.0), (0.3, 2.0)],\n        ];\n        color = [:steelblue, :tomato, :seagreen],\n    )\n    nothing\nend@bind pick stores a click in pick. Hover outlines the polygon and does not change pick.@bind pick masque(fig)(Image: The figure before any interaction)Before a click, pick is nothing. pick.index is 1-based. This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"click a polygon\"\nelse\n    names = [\"triangle\", \"square\", \"trapezoid\"]\n    \"$(names[pick.index]) selected\"\nendclick a polygon\n\n\n\nBands, densities, filled contours, violins, and Voronoi cells are polygons too. A filled contour reports the low and high of its level. A click inside a hole of a contour level hits whatever polygon is drawn in the hole, not the ring around it, and hits nothing if the hole is empty (a peak above the top level, for example).","title":"Polygons"},{"category":"section","location":"marks/#Lines","page":"Click marks","text":"A lines! call is a single mark: clicking anywhere along the path selects the whole line, however many vertices it has, which matches how a line plot is read: as one series. To read the value at a particular x along a line instead, use a SliceInteractable.\n\nbegin\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    xs = 0:0.1:10\n    lines!(ax, xs, sin.(xs))\n    lines!(ax, xs, cos.(xs))\n    nothing\nend\n\n@bind pick masque(fig)\n\npick.layer tells the two lines apart (:lines and :lines_2, in the order you drew them). stairs! is also one mark per call, with ids :stairs, :stairs_2, and so on, and a series! call is one layer whose pick.index is the series. Plots made of separate pieces — linesegments!, error bars, range bars, hlines!, and vlines! — make each piece its own mark instead.","title":"Lines"},{"category":"section","location":"marks/#Points-on-a-polar-axis","page":"Click marks","text":"Scatters, lines, and segments work on a PolarAxis. A point's x is its angle and its y its radius, the same order you passed them in:\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-marks-polar\" data-masque-embed=\"marks_polar\" title=\"Four polar points. Click one and the readout reads its radius.\" style=\"width:100%;height:1280px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a point on the polar axis. The last cell reads its direction and radius.Scatter the points the way you already scatter them on a PolarAxis. masque makes each point clickable.begin\n    fig = Figure(size = (480, 400))\n    ax = PolarAxis(fig[1, 1])\n    pts = Point2f[(0.0, 1.0), (π / 2, 2.0), (π, 1.5), (3π / 2, 2.5)]\n    scatter!(ax, pts; markersize = 18)\n    nothing\nend@bind pick stores a click in pick. pick.x is the angle and pick.y is the radius.@bind pick masque(fig)(Image: The figure before any interaction)Before a click, pick is nothing. This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"click a point\"\nelse\n    dirs = [\"east\", \"north\", \"west\", \"south\"]\n    \"$(dirs[pick.index]) — r = $(pick.y)\"\nendclick a point\n\n\n\nBars, heatmaps, and polygons on a polar axis are skipped with a warning, and the readouts that need a flat axis are not available there; see Supported plots and axes.","title":"Points on a polar axis"},{"category":"section","location":"gallery/text/#Text-labels","page":"Text labels","text":"masque(fig) turns data-space text! and annotation! into clickable boxes. The box comes from Makie's own string bounds. A rotated label is still one box, axis-aligned and a little loose. Click a label in a live notebook and the bond is an ElementEvent with text, index, x, and y.\n\nThe scatter on the same axes is also detected. A marker click has no text field. The readout guards on that.\n\n<div class=\"masque-embed-wrap\">\n<iframe id=\"masque-gal-text\" data-masque-embed=\"gallery_text\" title=\"Text labels\" style=\"width:100%;height:1320px;border:0;background:transparent;overflow:hidden;\" scrolling=\"no\" loading=\"lazy\"></iframe>\n</div>\n\ndetails: Notebook as text\nClick a label, including the tilted one and the annotation. The last cell quotes it.Add text! and annotation! on the scatter you already have. masque makes the labels clickable.begin\n    fig = Figure(size = (500, 350))\n    ax = Axis(fig[1, 1]; title = \"click a label\")\n    pts = [(1.0, 1.0), (2.0, 3.0), (3.0, 2.0)]\n    scatter!(ax, first.(pts), last.(pts); markersize = 16)\n    text!(\n        ax, first.(pts), last.(pts);\n        text = [\"Alpha\", \"Beta\", \"Gamma\"], offset = (8, 8), fontsize = 18,\n    )\n    text!(ax, [2.2], [1.4]; text = [\"Tilted\"], rotation = 0.5, fontsize = 18)\n    annotation!(ax, [3.3], [3.2]; text = [\"Annotated\"])\n    nothing\nend@bind pick stores the click. A marker click is a different value from a label click.@bind pick masque(fig)(Image: The figure before any interaction)This cell reads pick, so it re-runs on the click.if pick === nothing\n    \"click a label\"\nelseif hasproperty(pick, :text)\n    \"\\\"$(pick.text)\\\"\"\nelse\n    \"that was a marker — click a label\"\nendclick a label\n\n","title":"Text labels"},{"category":"section","location":"gallery/text/#Variations","page":"Text labels","text":"offset and fontsize follow the label. annotation! is the same button as text!. A label in screen space, rather than data space, is not a hit target.\n\nnote: Note\nLabel hits are the same on :cairo and :webgl. This player is the Cairo figure. The snapshots replay every label click and every marker click. masque(fig) also publishes the scatter, so in a live notebook branch on hasproperty(pick, :text) before reading the string.","title":"Variations"}]
}
